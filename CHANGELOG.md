# fish-audit-prompt changelog

v5.0.1  2026-05-09
- Versioning convention: audit-prompt version is now synchronised to the ry-install version it derives from (v5.0.1 here). Historical entries (v5.0.0 and earlier) retain their original prompt-internal numbering and are NOT retroactively renumbered — they reference audit-prompt content as it existed at release time.
- Major resync against ry-install v5.0.1 (was v4.5.8 baseline). 35+ minor source releases encoded in one pass; the v4.6.x architectural expansion (signal model rewrite, function count doubling, count-invariant runtime asserts, AUR set introduction) is the single largest discontinuity since the v4.5.0 profile-system removal.
- F-1 CRIT — Signal architecture inversion. v4.6.16 retired exit code 141 from the contract: `_cleanup_pipe` now sets `_RY_OUTPUT_BROKEN=true` and returns 0 instead of `exit 141`; `_msg_print` / `_err_loud` / `_echo` short-circuit on `set -q _RY_OUTPUT_BROKEN; and return 0` to avoid EPIPE re-entry; JSONL log is canonical. v4.6.17 dropped the unreachable `case pipe` arm from `_teardown` and the "141 SIGPIPE" row from help / README signals tables. Phase 3 checks 27/31/32 rewritten with the new signal-exit set (129/130/131/143 + 134/138/140 from new USR1/USR2/ABRT handlers via `_cleanup_other`). Phase 7 check 113 exit-code list resyncs help/README expectations. Phase 14 check 189 inverted from "exit 141 + JSONL footer + _do_cleanup" to "set _RY_OUTPUT_BROKEN + return 0". PASS 2 check 249 + SIGPIPE narrative inverted: PIPESTATUS[0]=141 from `--help | head -n 1` is now a regression signal, not an acceptable outcome. New check 390 codifies the `_RY_OUTPUT_BROKEN` short-circuit contract.
- F-2 CRIT — Count-invariant runtime asserts. v4.5.36 promoted `_ir_validate_counts` from comment-only to runtime asserts with the canonical invariant set: `KERNEL_PARAMS:15 MKINITCPIO_HOOKS:11 MKINITCPIO_MODULES:1 LOGIND_IGNORE_KEYS:9 ENV_VARS:11 SYSCTL_VALUES:16 PKGS_ADD:12 PKGS_DEL:7 AUR_PKGS:2 MASK:11`. Drift triggers `_err_loud "<NAME> count drift: got=N expected=M — README/script desync, refuse to deploy"` + `_pre_dispatch_exit $EXIT_PREFLIGHT`. New check 386 codifies. Phase 6 check 79 (MKINITCPIO_HOOKS:11 + MKINITCPIO_MODULES:1) and check 100 (SYSCTL_VALUES 21 → 16) updated; Phase 14 check 306 + 344 + 345 + 347 + 349 sync the corresponding `_ir_validate_counts` invariants for SYSCTL/PKGS_ADD/MASK/EXPECTED_SERVICES/ENV_VARS.
- F-3 CRIT — Destination set: SYSTEM count 12 → 11 (udev rqaffinity rule `/etc/udev/rules.d/99-nvme-rqaffinity.rules` removed earlier in the v4.5.x line; was already gone at v4.5.8 but check 103 still listed it — internal contradiction with Phase 6 header that already said `SYSTEM (11)`). `_RY_MANAGED_FILE_COUNT` 16 → 15. v5.0.1 reverted the v4.5.4 `(count …)` runtime-derivation in favour of a literal `15` with an immediate runtime drift guard at L695-696 (`test "$_ry_dst_count" -ne "$_RY_MANAGED_FILE_COUNT"; and echo "[ERR] _RY_MANAGED_FILE_COUNT drift: declared=$_RY_MANAGED_FILE_COUNT computed=$_ry_dst_count" >&2; and _ry_exit $EXIT_PREFLIGHT`). Checks 103/104/105/106 rewritten to reflect hand-synced literal + runtime guard model. Regression detector for udev rqaffinity added.
- F-4 HIGH — Header L3-L9 fully resynced. Version 5.0.0 → 5.0.1; "Derived from ry-install.fish v4.5.8 (2026-05-02)" → "v5.0.1 (2026-05-09)"; check count 375 → 385 (374 static + 11 runtime); Phase 14 title "v3.1.0–v4.5.8 Specifics" → "v3.1.0–v5.0.1 Specifics"; resync-note prose enumerates all 35+ minor source releases (v4.5.25 through v5.0.1) with the architectural deltas per release. v5.0.1 baseline panel added: function count 246, --description 246 (100%), --argument-names 87, LOC 5,029, set -l ~464, bare set ~115, set -g ~108, _run wrap ~52. GROUP D label, p14 data-file map, SIZE GATE phase map all synced. Per-phase tally sum 363 → 374.
- F-5 HIGH — Phase 14 check 185 + 196 function count rebaseline 137 → 246 (v5.0.1) with split-history narrative (137 → 200 v4.5.34 intermediate → 239 → 246). Check 197 `≥100 coreutils calls (~122 v4.5.8)` → `≥80 (~95 v5.0.1)` reflecting the v4.6.11 `command pacman` standardization across 8 read-only invocations. Check 198 `≥20 functions (v4.5.8 baseline ~52)` → `≥40 (v5.0.1 baseline ~87)` — substantial growth from helper extractions in v4.5.31 / v4.5.34 / v4.6.x splits.
- F-6 HIGH — Scope baselines (checks 199, 204, 213) rebaselined v4.5.8 → v5.0.1: in-function set -g ~117 → ~108 (v5.0.1); bare set ~151 → ~115; set -l ~379 → ~464 (RISEN due to helper extraction making more state explicit-locals); _run wrap ~48 → ~52. Driver narrative split into two phases: v4.1.8→v4.5.8 (profile-system removal v4.5.0) and v4.5.8→v5.0.1 (v4.5.31 + v4.5.34 + v4.6.x helper extractions). Drift tolerance ±15% retained.
- F-7 HIGH — Phase 8.2 atomic file ops (check 117/118/119) rewritten to encode the v4.5.36 `_atomic_write_file` central helper + v4.6.9 `mv -T` / `mv -Tf` (refuses target-as-directory) requirement across all atomic-rename sites (`_atomic_write_file`, `_mkinitcpio_revert`, `_fstab_atomic_replace`, `_if_write_wipe_marker`, `_acquire_lock_fresh`) + v4.6.10 `_ry_install_file` user-scope `mkdir -p` `umask 0077` wrap. Regression detectors: `mv --` inside atomic-write helpers → 0; `mv -T |mv -Tf ` → ≥4.
- F-8 HIGH — Phase 8.5 lock file (check 126/127/128/129) rewritten for v4.5.31 `_acquire_lock` split (`_acquire_lock_fresh` + `_reclaim_stale_lock`) and the v4.6.9 + v4.6.12 reclaim hardening: liveness via `test -d /proc/$old_pid` (replaces `kill -0` whose EPERM≠ESRCH conflated states); cmdline substring-match "ry-install" against `/proc/<pid>/cmdline` before refusing reclaim — closes the fish-PID-reuse window. New check 393 codifies the cmdline defence.
- F-9 HIGH — 10 NEW CHECKS added covering v4.5.25 → v5.0.1 architectural additions (Phase 14.33 subsection):
    385  _RY_POST_HOOKS dispatch table + _post_hook_for_target helper (v4.5.25)
    386  _ir_validate_counts runtime invariants (v4.5.36)
    387  _RY_BOOT_TAINTED separated from INSTALL_HAD_ERRORS (v4.6.3)
    388  _bwg_managed_only auto-ack for SDBOOT_REMOVE_EXISTING=yes (v4.6.6)
    389  _resolve_boot_path + _RY_BOOT_PATH cache + _pbs_entry_has_valid_kernel BLS Type #1 (v4.6.8)
    390  _RY_OUTPUT_BROKEN EPIPE re-entry guard (v4.6.16)
    391  AUR_PKGS as separate top-level array + paru hard prereq (v4.6.17)
    392  _verify_summary uses _msg_nocount + _msg ungated VERIFY_* increments (v4.6.5/v4.6.16 off-by-one fix)
    393  _reclaim_stale_lock cmdline-substring + test -d /proc defences (v4.6.9 + v4.6.12)
    394  RY_INSTALL_ALLOW_PARTIAL_UPGRADE + RY_INSTALL_PKG_REMOVE_CASCADE env-var runtime gates (v4.6.x)
  Total checks: 375 → 385.
- F-10 HIGH — GROUP D extractor extended with 60+ new rg/python3 patterns covering v4.5.25 → v5.0.1 (POST_HOOKS table, _ry_namespace_cleanup bail, hermetic keepalive child, _run dash-prefix reject, _chk_grep -wF, VERIFY_GEN_FAIL, sysctl generator rc=13, _vss_/_vrkg_/_vsp_/_vrsv_ helper enumeration, _err_loud / _msg_print / _msg_nocount / _fail_silent, _redact_argv_elements, _installed_bytes, _PROFILE_USES_WIFI_BACKEND rename, _RY_HOME_DIR / _RY_RUN_TIMEOUT_DEFAULT named consts, _grep_kparam / _grep_kv, _track_tmpfile / _resolve_systemd_ver, _ir_validate_counts invariant enumeration, EXIT_GEN_NOFN/NOUUID/SYSCTL detection, _RY_BOOT_TAINTED, ENABLE_OK_START_FAIL, SUDO_KEEPALIVE_ERR, nftables/libva-mesa-driver/mesa rename detection, _ntsync_per_kernel_state, _bwg_managed_only / BOOT_WIPE_AUTO_ACK markers, ufw-in-MASK, _resolve_boot_path / _RY_BOOT_PATH / bootctl -x, mv -T / -Tf, test -d /proc, PKG_REMOVE_BATCH_FAIL_DBLOCK, _check_phase_files _installed_bytes consumption, _install_finalize wipe-marker gate, _ry_install_file umask 0077, command pacman count, _reclaim_stale_lock cmdline match, MKINITCPIO_HOOKS:11 + MKINITCPIO_MODULES:1 invariant assertions, _cleanup_pipe non-fatal body inspection, _RY_OUTPUT_BROKEN short-circuits, _verify_summary _msg_nocount, _cg_access_ok no-`--`, AUR_PKGS, paru hard prereq, _teardown no `case pipe` arm, help no 141 row, PROFILE_NAME hoist, _vsp_pacman_conf explicit-if block, VERSION 5.0.1 readback, signal handler enumeration, _teardown unified, _cleanup_other USR1/USR2/ABRT, SYSCTL_VALUES count 16, SYSTEM count 11, udev rqaffinity 0-hits, _RY_MANAGED_FILE_COUNT 15 + drift guard, RY_INSTALL_ALLOW_PARTIAL_UPGRADE, RY_INSTALL_PKG_REMOVE_CASCADE).
- F-11 MED — PASS 3 SPECIAL HANDLING list extended with v4.5.25 → v5.0.1 retirements as 0-hit regression detectors: nftables / nftables.service (v4.6.4); libva-mesa-driver / lib32-libva-mesa-driver (v4.6.5); mkinitcpio-firmware-in-PKGS_ADD (v4.6.17 — relocated to AUR_PKGS); ufw-in-PKGS_DEL (v4.6.7 — relocated to MASK); 141-as-exit-code anywhere (v4.6.16/.17); `case pipe` arm in _teardown (v4.6.17); 141 SIGPIPE row in help/README (v4.6.17); PROTON_NO_WM_DECORATION (v4.5.37); ENABLE_LAYER_MESA_ANTI_LAG (v4.5.38); busy_read / busy_poll / netdev_budget / kernel.split_lock_mitigate / vm.swappiness in SYSCTL_VALUES (v4.6.x); _PROFILE_USES_NM (v4.5.33 renamed); `mv --` in atomic-write helpers (v4.6.9); `kill -0` in _reclaim_stale_lock (v4.6.9); command sed 's/^ *//' in _run (v4.5.28 split refactor).
- F-12 MED — PASS 3 summary table: Phase 14 row 176 → 186 checks; TOTAL 375 → 385. Check ranges note extended with the v5.0.1 lineage entry documenting checks 385-394 for ry-install v4.5.25 → v5.0.1 coverage.
- F-13 MED — README badge / scope / phase-coverage sync. version-5.0.0 → version-5.0.1; checks-375 → checks-385; "Derived from `ry-install.fish` v4.5.8 · 375 checks across 15 phases · Prompt v5.0.0" → "v5.0.1 · 385 checks across 15 phases · Prompt v5.0.1"; Metrics row "375 (364 static + 11 runtime)" → "385 (374 static + 11 runtime)"; Phase Coverage row 14 "176 Version-specific" → "186 Version-specific". fish prerequisite badge already at 3.6+; no further fish-version changes since v4.4.36 raised it.
- F-14 LOW — Check 216 `multi-thousand LOC; v4.5.8 baseline 5,205` → `v5.0.1 baseline 5,029`. The drop reflects v4.5.29 inline-and-or compaction (5495→5005) + v4.5.34 19-orchestrator split (5098→4849) followed by net growth in v4.6.x feature additions back to 5,029.
- Count bumps — header version 5.0.0 → 5.0.1; total checks 375 → 385 (+10 new checks; no renumbering of existing checks; +186 Phase 14 from 176); README badge 375 → 385; Phase 14 title "v3.1.0–v4.5.8 Specifics (176 checks)" → "v3.1.0–v5.0.1 Specifics (186 checks)". All existing check IDs preserved for historical cross-referencing.

Deferred (carried from v5.0.0)
- F-6 MED (v4.5.0) — Extract 9 inline `python3 -ISc` heredocs to `helpers/*.py`. Significant structural change. Re-deferred.

Unresearched (carried from v5.0.0)
- `rg 13.x` single-file `rg -c` 0-match behavior.
- `set -l` block-scope vs function-scope in fish 4.x.
- `bat --plain -r START:END` syntax stability across bat 0.24 → 0.26.
- `fish_indent --check` exit-code semantics across fish 3.7 → 4.6.
- `python3 -I` implied-flag set across Python 3.12/3.13.
- `rg --generate man / complete-fish / complete-bash` (ripgrep 14.x+).

v5.0.0  2026-05-02
- Major resync against ry-install v4.5.8 (was v4.1.8 baseline). 26 findings landed: 3 CRIT, 8 HIGH, 8 MED, 7 LOW. 23 minor versions of source drift encoded; v4.5.0 profile-system removal is the single largest discontinuity.
- F-1 CRIT — PASS 2 STEP 1: fish version gate `[ "$FISH_MINOR" -lt 4 ]` → `-lt 6`. ry-install v4.4.36 raised the requirement to fish 3.6+; under fish 3.4 / 3.5 every PASS 2 check (239–249) false-failed because ry-install exited 3 (EXIT_PREFLIGHT) before any tested behavior ran.
- F-2 CRIT — Phase 15.13 PROFILE SYSTEM INVARIANTS rewritten as PROFILE SYSTEM RETIRED (v4.5.0) regression detector. v4.5.0 removed `_load_profile`, `_validate_profile`, `_ry_profile_*`, `~/.config/ry-install/default-profile`, `_manifest_check_orphans`, `_manifest_write`; gtr9_pro defaults inlined as `set -g` block at module init. PROFILE_NAME / PROFILE_DESC retained as inlined globals only. Replaced GROUP D V3500 PROFILE SYSTEM + V3500 PROFILE GLOBALS + V3500 MANIFEST ORPHANS extractors with retirement detectors. Replaced V414 VALIDATE PROFILE COLLISION GUARD + entire V415 PROFILE GUARD SPLIT family (4 sub-extractors) with single retirement detector. Updated SPECIAL HANDLING list. Stripped "profile loading" from prompt header Scope line.
- F-3 CRIT — 9 dead helper extractors converted to regression detectors (expect 0 hits): `_check_service_active_enabled`, `_find_pacnew_files`, `_get_boot_time` (inlined v3.48.2), `_gkeyfile_escape`, `_emit_step_time`, `_content_hash`, `_parse_systemctl_show`, `_RY_MANAGED_CASE_COUNT`, plus PROGRESS_STEPS / PROGRESS_TOTAL (removed v4.1.9). All previously expected ≥1 hit; current source returns 0.
- F-4 HIGH — Header L4 `Derived from: ry-install.fish v4.1.8 (2026-04-20)` → `v4.5.8 (2026-05-02)`. Phase 14 title "v3.1.0–v4.1.8 Specifics" → "v3.1.0–v4.5.8 Specifics". GROUP D label, p14 INFO message, SIZE GATE phase map, data-file map, check 79 hook-order baseline label all synced. Phase 14 history footer extended with v5.0.0 resync line. Phase 15 check 216 `7000+ line script` → `multi-thousand LOC; v4.5.8 baseline 5,205`.
- F-5 HIGH — BASHISMS detector (PASS 1 GROUP A1, L189) exemption pattern tightened: added POSIX char classes `[[:class:]]`, `# lint:ignore` markers, fish `set -e VAR` / `set -e $var` / `set --erase`, `functions -e fn`, single-quoted printf-arg literals containing `${VAR}` or `||`, `&& / ||` inside double-quoted strings, escaped `\${VAR}`, `XDG_RUNTIME_DIR unset` keyword-in-message. Reduces false positives from ~30 to 4 against v4.5.8. Added explanatory NOTE block listing exemption taxonomy and survivor-triage requirement.
- F-6 HIGH — Check 180 `_RY_MANAGED_FILE_COUNT=16 (SYSTEM 12 + USER 3 + SERVICE 1)` literal → `runtime-derived since v4.5.4 via (count $SYSTEM_DESTINATIONS $USER_DESTINATIONS $SERVICE_DESTINATIONS); current resolved value: 15 (11+3+1)`. Check 230 `DESTINATIONS ↔ _ry_get_file_content cases match (12+3+1=16)` → runtime-equality check against `(count …)`. Phase 6 header `12+3+1=16` → `11+3+1=15` with same runtime-derived note.
- F-7 HIGH — SYSCTL_VALUES count corrections: check 100 `19 entries (v4.0.0)` → `21 entries (v4.1.10 expanded 19→21)` with full 21-key enumeration. Check 306 `19 entries` → `21 entries`. Check 344 `21→19 (v4.0.0)` → full history `pre-v4.0.0 21 → v4.0.0 19 → v4.1.10 21 (current)`.
- F-8 HIGH — Phase 6 header `3 arrays: SYSTEM (12), USER (3), SERVICE (1) = 16 files` → `SYSTEM (11), USER (3), SERVICE (1) = 15 files`. Stale `udev rqaffinity 12th added v3.51.x` and `README MANAGED_FILE_COUNT = 16` references dropped (count is now runtime-derived; see F-6).
- F-9 HIGH — Check 218 `PROGRESS_STEPS array matches _progress call arguments` → `PROGRESS_STEPS / PROGRESS_TOTAL fully removed (v4.1.9): rg → 0 hits`. Check 280 6-phase model rewritten with same regression-detector framing. GROUP_C2 PROGRESS DEFINED extractor (was `sed -n '/set -g PROGRESS_STEPS/,/^set -g PROGRESS_TOTAL/p'`) replaced with `rg PROGRESS_STEPS|PROGRESS_TOTAL → 0` regression detector. PROGRESS CALLED extractor preserved (call sites still emit phase labels).
- F-10 HIGH — Check 24 success-exit list `0: success/help/version/completions/check-clean` → strip `completions/` (mode removed v4.1.0). Check 26 `2: usage errors; 10: drift (--check); 11: lint errors` → strip `; 11: lint errors` (EXIT_LINT removed v3.48.20; check 228 already noted "No stale 11" — internal contradiction resolved).
- F-11 HIGH — Check 246 deprecated-flag handling rewritten: `All 12 deprecated flags emit specific [ERR] message via _early_usage_exit … "<flag> is no longer required/removed; <alternative>"` → argparse generic rejection with `[ERR] <argparse-msg>` (or `Invalid arguments: $_ORIG_ARGV` if argparse stderr empty) → `_pre_dispatch_exit $EXIT_USAGE`. Per-flag handler does not exist in v4.5.8. Same correction in PASS 2 STEP 1 narrative comment, check 227, and check 112.
- F-12 MED — 6 counter baselines rebaselined v4.1.8 → v4.5.8. Check 185 function count 76 → 137; check 196 same. Check 197 `≥150 coreutils calls (rg ~173 v4.1.8)` → `≥100 (~122 v4.5.8)`. Check 198 `≥20 functions (v4.1.8 baseline 21)` → `≥20 (v4.5.8 baseline ~52)`. Check 199 `set -g ~130 / bare set ~193 / ±10%` → `set -g ~117 / bare set ~151 / ±15%`. Check 213 `set -l ~590, bare set ~193, set -g ~130` → `~379 / ~151 / ~117` with v4.5.0 profile-removal driver note. Check 204 `_run wraps ≥50 (~60)` → `≥40 (~48)` with same driver note (also F-26).
- F-13 MED — Check 200 _log marker format. Was: `=== MODE START ===` / `=== MODE END ===` (v4.1.4-current); rg expectations 4 START / 9 END. Current source uses dynamic `_log "=== $name ==="` (1 parametric site + 2 literal install sites); 0 START/END suffixes anywhere. Rewritten to `rg '_log "=== ' → ≥3` with explicit retirement of paired-marker convention.
- F-14 MED — Check 195 banner enumeration. Was: 3 top-level `# UPPERCASE LABEL` blocks (PROFILE LOADER; LOGGING…; CLI ARGUMENT PARSING AND DISPATCH); rg → 3. Current: 2 banners (LOGGING…; INSTALL PIPELINE); PROFILE LOADER retired with v4.5.0 profile removal; CLI ARGUMENT PARSING AND DISPATCH absorbed into trailing dispatch block. Rewritten with actual current banner names; rg → 2. Check 185 + check 201 subsystem-marker prose synced.
- F-15 MED — Check 219 mktemp/rm-f bookkeeping. Was: `_ry_validate_configs (7/8), _ry_install_file (5/11)` per-function counts. v4.5.8 helper extraction (`_atomic_write_file` + `_rm_tmp` + `_is_symlink` + `_TRACKED_TMPFILES` registration) moved the pattern into helpers; naive grep returns 1/0 and 0/0. Rewritten as helper-based audit: `_atomic_write_file` carries 5 mktemp + ≥7 `_rm_tmp` calls; `_rm_tmp` deletes-and-untracks across ≥30 sites; `_is_symlink` 3 sites.
- F-16 MED — Check 173 `_get_boot_time + _find_pacnew_files` survives-via-stdout claim → both functions removed in later versions; rewritten as regression detector (`rg → 0` for both). Check 175 trailing clause `Callers use batch _parse_systemctl_show or individual calls` → `_parse_systemctl_show also removed in later versions (rg → 0). Service checks now use direct inline systemctl is-enabled or v4.4.27 _unit_state helper`.
- F-17 MED — Check 181 `_ry_verify_static: 4 _chk_grep checks. _ry_verify_runtime: reads /proc/sys/ for all 4 keys` → loop pattern `for entry in $SYSCTL_VALUES` at ≥3 sites (validate, verify-static, verify-runtime). No hardcoded 4-key count (count is now 21; iteration is over the array).
- F-18 MED — Header L9 drift note `ry-install v4.1.9–v4.1.14 deltas (5 items) NOT yet encoded` → comprehensive v5.0.0 resync note enumerating all v4.1.9 → v4.5.8 deltas: PROGRESS_STEPS removal; SYSCTL 19→21; ENV_VARS 12→13; sudo -n parity; null-delim boot enumeration; v4.4.27 helper introductions (_pre_dispatch_exit + 5 dedup helpers); v4.4.36 fish gate 3.4→3.6; v4.5.0 profile-system + manifest removals + _init_runtime + JSONL renames; v4.5.2 _RY_LOG_WRITE_FAIL; v4.5.4 _RY_MANAGED_FILE_COUNT runtime-derived + _RY_SECRET_FLAGS hoist + RY_INSTALL_FORCE_BOOT_REBUILD; v4.5.6 PATH pinning + sudoers regex; v4.5.7 mkinitcpio rollback gating; v4.5.8 _rm_tmp + _is_symlink + _enum_boot_entries.
- F-19 MED — Check 383 `_install_fstab_opts` tmpfstab deregistration. v4.1.8 baseline expected `Deregister deleted tmpfstab from cleanup list` comment + `string match -v -- "$tmpfstab" $_TRACKED_TMPFILES` + 2 set -ga / 1 string-match-v inside the function. v4.5.8 mechanism evolved: failure paths route through `_rm_tmp $tmpfstab true` (helper auto-untracks); success path uses explicit `_untrack_tmpfile` after atomic mv. Rewritten with v4.5.8 baselines: 1 set -ga, ≥6 _rm_tmp, ≥1 _untrack_tmpfile, ≥30 _rm_tmp source-wide.
- F-20 LOW — Check 216 `7000+ line script` → `multi-thousand LOC; v4.5.8 baseline 5,205`.
- F-21 LOW — Check 27 signal exits `130 SIGINT, 141 SIGPIPE` → full `129 SIGHUP, 130 SIGINT, 131 SIGQUIT, 141 SIGPIPE, 143 SIGTERM` (matches Phase 12 check 249 + source `_cleanup` --on-signal INT/TERM/HUP/QUIT + `_cleanup_pipe` --on-signal PIPE). Check 31 `INT, TERM, HUP, fish_exit handlers` → `INT, TERM, HUP, QUIT, PIPE, fish_exit handlers` with handler-name + signal-list parenthetical.
- F-22 LOW — Header L7 Scope `…profile loading, argparse dispatch…` → `…argparse dispatch…` (profile system retired v4.5.0).
- F-23 LOW — PASS 2 SIGPIPE comment + check 249 narrative `~1.6 KB in ry-install v4.1.8` → `~1.7 KB in ry-install v4.5.8` (v4.5.6 added RY_INSTALL_FORCE_BOOT_REBUILD + NO_COLOR + secret-flag redaction docs to help text).
- F-24 LOW — Check 175 `Callers use batch _parse_systemctl_show or individual calls` clause dropped (function removed); replaced with `direct inline systemctl is-enabled or v4.4.27 _unit_state helper` reference.
- F-25 LOW — README sync. Badge `version-4.5.0` → `version-5.0.0`; `fish-3.4%2B` → `fish-3.6%2B`. Headline `Derived from ry-install.fish v4.1.8 · 375 checks · Prompt v4.5.0` → `v4.5.8 · 375 checks · Prompt v5.0.0`. Scope sentence: stripped `external profile loading` clause. Prerequisites table fish row `3.4+` → `3.6+`. Phase Coverage row 15 `Supplemental + profile invariants` → `Supplemental deep checks`.
- F-26 LOW — Check 204 _run wrap floor `≥50 commands (v4.1.8 baseline ~60)` → `≥40 (v4.5.8 baseline ~48; profile-system removal v4.5.0 dropped this from ~60)`. Drift tolerance ±15% retained.
- Count bumps — header version 4.5.0 → 5.0.0; total checks unchanged (375); README badge 375 unchanged; checklist-save gate unchanged. No check renumbering — every fix is in-place rebaselining or wording correction, preserving check IDs for historical cross-referencing.
- SPECIAL HANDLING — appended v4.1.9 → v4.5.8 retirement-detector list (PROGRESS_STEPS/_TOTAL, profile system + manifest, _check_service_active_enabled, _find_pacnew_files, _get_boot_time, _parse_systemctl_show, _gkeyfile_escape, _emit_step_time, _content_hash, _RY_MANAGED_CASE_COUNT). Retired check 379 (legacy 'Profile destination key collision' label) folded into the v4.5.0 profile-removal entry.

Deferred (carried from v4.5.0)
- F-6 MED (v4.5.0) — Extract 9 inline `python3 -ISc` heredocs to `helpers/*.py`. Significant structural change. Re-deferred.

Unresearched (carried from v4.5.0)
- `rg 13.x` single-file `rg -c` 0-match behavior.
- `set -l` block-scope vs function-scope in fish 4.x.
- `bat --plain -r START:END` syntax stability across bat 0.24 → 0.26.
- `fish_indent --check` exit-code semantics across fish 3.7 → 4.6.
- `python3 -I` implied-flag set across Python 3.12/3.13.
- `rg --generate man / complete-fish / complete-bash` (ripgrep 14.x+).


v4.5.0  2026-04-22
- Self-audit fix 1 CRIT + 3 HIGH + 7 MED + 10 LOW against v4.4.5. 21 of 25 findings landed; 4 deferred or unresearched.
- F-1 CRIT — PASS 2 STEP 1 (L2180–2181): `chmod -R 0777 "$PASS2_HOME"` → `chown -R "$PASS2_USER:" "$PASS2_HOME"; chmod -R 0700 "$PASS2_HOME"`. fish sources `~/.config/fish/config.fish` even non-interactively (verified fish 3.7.0); 0777 opened local-attacker race between mkdir (L2180) and first fish spawn (L2199) for arbitrary code execution as `$PASS2_USER` when audit runs as root. chown + 0700 closes the race with no write access for others.
- F-2 HIGH — SETUP STEP 2 (L44): `rg` fallback `rg() { grep -P --color=never "$@"; }` → hard-fail `exit 2`. `grep -P -F` emits "conflicting matchers specified" (verified Ubuntu 24.04 grep 3.11); PASS 3 uniqueness verification at L2465/L2477 passes `-cF` to the shim and every Before pattern falsely reports error, blocking spec generation. STEP 1 auto-installs rg; no graceful-degrade path remains.
- F-3 HIGH — SETUP STEP 1 (L19–22): apt-get-only installer → auto-detect `apt-get` / `pacman` / `dnf`. Header promised "adapt for other distros" but body was Debian/Ubuntu only; silent failure on Arch (CachyOS host) cascaded to shim degradation. `ripgrep` added to apt install list (previously external download).
- F-4 HIGH — SETUP STEP 2 (L56): `PATH='/usr/local/bin:/usr/bin:/bin'` → `+/usr/sbin:/sbin`. `runuser(1)` lives in `/usr/sbin` on Debian/Ubuntu (verified Ubuntu 24.04); PASS 2 root-wrap at L2163 ran `command -v runuser` under sanitized PATH → exit 127 → false "[CRITICAL] PASS 2 SKIPPED" on every Debian/Ubuntu root audit.
- F-5 MED — SETUP STEP 1 (L24): regex `14\.1\.[1-9]|14\.[2-9]|1[5-9]\.|[2-9][0-9]\.` → numeric-tuple compare via `IFS=. read -r _rg_maj _rg_min _rg_pat <<<"$_ver"`. Regex rejected 100+ major; tuple compare has no upper bound and has already required one ad-hoc extension.
- F-7 MED — PASS 3 STEP 1 (L2362–2369): fish-in-bash `for sev in CRITICAL...` + separate `TOTAL` tail-1 chain → single awk `-F'[][]'` one-pass. 7× rg + fish startup (~50–80 ms × 7) → O(n) awk single scan; also auto-fixes the `tail -1` antipattern at L2369.
- F-8 MED — PASS 1 STEP 1 (L729–742): nested `while read` EMPTY SECTION GUARD with per-header `grep -nF | head -n 1 | cut` + `awk -v start=` → single-pass awk per file. O(n²) → O(n); ~240 scans collapse to 1 on p14.txt.
- F-10 MED — SETUP STEP 2 (after L49): added `[ -n "$BASH_VERSION" ] || { ... exit 2; }`. Under dash, `export -f` at L49 hard-aborts the -c script (verified `dash -c 'echo before; export -f foo 2>/dev/null; echo after; true'` prints only "before", exit 2) — hardening lines L50–62 never execute without the guard.
- F-11 MED — PASS 2 STEP 1 (11 sites): `FISH_PREFIX="runuser -u $PASS2_USER -- env HOME=$PASS2_HOME"` (word-split) → `FISH_PREFIX_ARGS=(runuser -u "$PASS2_USER" -- env "HOME=$PASS2_HOME")` array. Every `$FISH_PREFIX` use → `"${FISH_PREFIX_ARGS[@]}"`. Space in `$PASS2_HOME` now tolerated; SIGPIPE test's former `/bin/bash -c` word-split workaround no longer needed.
- F-12 LOW — SETUP STEP 1 (L25–26): rg pin `14.1.1` → `15.0.1`. Fixes parent-dir gitignore bugs, large-gitignore memory regression, adds Jujutsu repo support; version-check regex at L24 already accepts 15.x.
- F-13 LOW — PASS 1 STEP 1 GROUP A1 (L189): bashism detector piped through `rg -v '^[0-9]+:\s*#|ExecStart=|^\s*awk |<<-?\s*['\''"]?EOF'`. Pre-filters ExecStart systemd-unit `&&`, awk-body `${...}`, heredoc contents that Phase 1 L782–784 already marks exempt — exemption was post-hoc; pre-filter reduces noise.
- F-14 LOW — PASS 2 STEP 1 (L2253–2257): `/bin/bash -c '...$FP fish...${PIPESTATUS[0]}...'` wrapper → direct `timeout "$HELP_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish --help 2>&1 | head -n 1`. Wrapper existed solely to word-split `$FISH_PREFIX`; redundant after F-11. Test still catches non-{0,141} (e.g., fish crash on pipe close).
- F-15 LOW — PASS 1 STEP 1 (L698–727): serial SIZE GATE for-loop → per-file `( ... ) & done; wait`. 7 files × ~100ms each serial (~700ms) → parallel (~150ms; capped by nproc).
- F-16 LOW — PASS 1 STEP 1 (L745): `wc -l "$D"/*.txt` → `shopt -s nullglob; _files=("$D"/*.txt); shopt -u nullglob; [ ${#_files[@]} -gt 0 ] && wc -l "${_files[@]}" || echo "[INFO] no data files produced in $D"`. Default bash has nullglob off; empty glob passed literal `$D/*.txt` → wc error "No such file or directory" exit 1.
- F-17 LOW — PASS 2 STEP 1 (L2271–2275) + checklist 247: added second NO_COLOR test pass with `env NO_COLOR=` (empty string) + ANSI >0 assertion. no-color.org v1.0 (2022) explicitly requires "present AND not an empty string" — empty value must NOT suppress color. Check 247 rewritten as two-axis (suppress + not-suppress).
- F-18 LOW — GROUP D + GROUP C2 (L330, 372, 417, 427, 554): 5 `rg -nc` → `rg -c`. `-n` silently ignored under `-c`; violates user LINT rule.
- F-19 LOW — PASS 2 STEP 2 (L2290): `rg -oP 'VERSION "\K[^"]+' || rg -o ... | sed 's/VERSION "//'` → `rg -o 'VERSION "[^"]+"' | sed 's/.*VERSION "//; s/"$//' | head -n 1`. `\K` requires PCRE2; `cargo install ripgrep --no-default-features` produces rg without PCRE2, old fallback stripped only leading quote leaving trailing `"` → false MISMATCH.
- F-20 LOW — PASS 2 STEP 1 (L2182): `trap '...' EXIT` → `trap '...' EXIT INT TERM HUP`. Matches L185 outer cleanup trap pattern; guarantees tmpdir removal on Ctrl-C during 4-parallel fish spawn.
- F-21 LOW — PASS 2 STEP 1 (L2196 + help/version sites): `RUNTIME_TIMEOUT=30` uniform → split `HELP_TIMEOUT=${HELP_TIMEOUT:-5}` for `--help/-h/--version/-v` invocations. Measured `fish script.fish --help` = 0.023s warm; 30s was 1300× headroom; 5s still 200× while surfacing regressions 6× faster.
- F-24 LOW — Phase 14.1 check 171 + GROUP D (new rg pattern at L308): added sub-check 171b `KVER GATE ORDERING` verifying `_ry_check_kernel_version` definition line < first `$KVER_MAJOR` / `$KVER_MINOR` consumer line. Check 171 validated KVER top-level init + no inline `uname -r` but did not verify the kernel-version gate runs before consumers.
- F-25 LOW — PASS 1 STEP 1 (L684): `tail -1` → `tail -n 1`. Audit's own check 141 (L1081) flags `head -1`/`tail -1` as antipattern requiring `-n 1`. Second violation at L2369 auto-fixed by F-7.
- Comment-style — 5 multi-line `#` comment blocks collapsed to single-line form (project convention; matches ry-install v4.1.14 "Three multi-line `#` comment blocks collapsed to single-line form" delta).
- Count bumps — header version 4.4.5 → 4.5.0; total checks 374 → 375 (F-24 +1 sub-check); static checks 363 → 364; Phase 14 count 175 → 176; checklist-save gate 363 → 364; summary table P14 175 → 176 and TOTAL 374 → 375; README version 4.4.5 → 4.5.0 and check badge 374 → 375.
- README — added `awk` / `curl` / `zip` to Prerequisites table (F-9) with POSIX/any versions and hard-fail fallback semantics; added "Security Considerations" section (F-22) documenting SETUP STEP 2 environment hardening, bash-only contract, PASS 2 root-handling invariants, cleanup trap signal coverage, and invocation rules (never `sudo bash prompt.sh`). README also notes package installer auto-detect across apt-get/pacman/dnf.
- Drift note — header (L9) flags that ry-install v4.1.9–v4.1.14 deltas are NOT yet encoded: PROGRESS_STEPS array removal (invalidates checks 218, 280, GROUP C2 extractors at L278–279); SYSCTL_VALUES count is now 21 not 19 (check 100); ENV_VARS count is now 13 not 12; v4.1.10 added sudo -n parity in `_ry_verify_runtime`; v4.1.12 added null-delim boot-wipe marker enumeration. v4.6.0 will re-sync against ry-install v4.1.14 baseline.

Deferred
- F-6 MED — Extract 9 inline `python3 -ISc` heredocs to `helpers/*.py`. Significant structural change (adds `helpers/` subdir to packaging, updates 9 call sites, updates SETUP STEP 1). Deferred to v4.6.0 or v5.0.0 alongside the ry-install v4.1.14 baseline sync.

Unresearched (tracked for v4.6.0)
- `rg 13.x` single-file `rg -c` 0-match behavior — could not install 13.x under network allowlist to verify prior spec speculation (retracted).
- `set -l` block-scope vs function-scope in fish 4.x — scope-shadow detector at L458–498 may over-report; needs fish 3.4 / 4.0 / 4.6 cross-version test before adjusting detector.
- `bat --plain -r START:END` syntax stability across bat 0.24 → 0.26.
- `fish_indent --check` exit-code semantics across fish 3.7 → 4.6 — PASS 1 STEP 1 L190 depends on exit 0 == pass.
- `python3 -I` implied-flag set across Python 3.12/3.13 releases.
- `rg --generate man / complete-fish / complete-bash` (ripgrep 14.x+) — not leveraged; potential future improvement.

v4.4.5  2026-04-21
- 374 checks. Fix 2 HIGH + 1 LOW findings from v4.4.4 → v4.1.8 self-audit. F-1 PASS 2 STEP 1 HOME-race: 4 parallel fish invocations (--help, -h, --version, -v) shared one PASS2_HOME and raced to `mkdir -p HOME/.local/share/fish` + `HOME/.config/fish`, producing stderr "warning-path: The error was 'File exists'" (measured: help-err=257B, h-err=279B, ver-err=536B against fish 3.7.0) — false-failed checks 239/240 ("0 stderr bytes") on every root-wrapped run. Fix: pre-create both subdirs + `chmod -R 0777` before the parallel fork; verified 0 stderr across all 4.
- F-2 GROUP C2 PROGRESS DEFINED + PROGRESS CALLED extractors rewritten for ry-install v4.0.0+ bareword array (`Preflight Packages ...`) + bareword call sites (`_progress Preflight`); prior `rg -o '"[^"]*"'` / `rg '_progress "'` patterns returned 0 hits against v4.1.8, making checks 218 + 280 vacuous.
- F-3 counter baselines refreshed v4.1.6→v4.1.8 in checks 197, 198, 199, 204, 213: bare set 192→193 (3 sites), _run wraps ~56→60 (1 site), v4.1.6 baseline label → v4.1.8 (6 occurrences). GROUP D section label + SIZE GATE phase label + DATA FILE mapping + empty-guard INFO message updated from "v3.1.0–v4.1.6 specifics" → "v3.1.0–v4.1.8 specifics" (4 sites). Retained v4.1.6-specific refs in check 380 (historically correct — check added in v4.3.0 for v4.1.6 delta). No ry-install delta.

v4.4.4  2026-04-21
- 374 checks. README ↔ prompt cross-check sync. Check 79 hook order rewritten with the full 11-hook chain `base → systemd → autodetect → microcode → modconf → kms → keyboard → sd-vconsole → block → filesystems → fsck` (was a 6-hook excerpt missing autodetect/microcode/modconf/kms/block); regression note added re: absence of `resume` (sleep/hibernate masked). Checks 112 + 227 disambiguated — the README "Deprecated flags — DO NOT re-introduce" header at L246 is env-var-scoped (DXVK_ASYNC, DXVK_FRAME_RATE, WINE_FULLSCREEN_FSR; VKD3D_FRAME_RATE retained), NOT CLI flags; deprecated CLI flags (--all, --dry-run, --diff, --fix, --lint, --test-all, --completions, --logs) rejected only via `_early_usage_exit`. No ry-install delta.

v4.4.3  2026-04-21
- 374 checks. Fix 5 stale/broken regexes against ry-install v4.1.8. Check 325: `preempt=full` retired from KERNEL_PARAMS by v4.0.x; check rewritten as runtime dmesg-only verification (`Dynamic Preempt: full` → ≥1 hit). Check 356: bash-array `_ps[1]=0` syntax replaced with fish-native `test $_ps[1] -eq 0; and test $_ps[2] -eq 0` (→ 7 hits) + `-ne 0; or` failure form (→ 3 hits); stale `e3b0c44` hex literal removed; GROUP D V403 collection regex updated. Check 350: literal `MulticastDNS=resolve` → interpolation-chain check (`set -g RESOLVED_MDNS resolve` → 1 + `MulticastDNS=$RESOLVED_MDNS` → ≥2). Check 328 marked retired (somaxconn removed v4.0.0; supersedes-target check 344). Check 307 marked retired (irqbalance off MASK v4.0.0; supersedes-target check 348). SPECIAL HANDLING list extended (307, 325, 328). No ry-install delta.

v4.4.2  2026-04-21
- 374 checks. Fix 2 stale claims. Check 280 PROGRESS_STEPS rewritten for the v4.0.0 6-phase model (Preflight → Packages → Configuration → Services → Boot → Finalize); was a retired 20-step model with stale "entries 15-17"; verified 6 `_progress <phase>` calls + 1 Finalize-skip fast-path variant. Check 296 + GROUP D regex `test .* -o |test .* -a ` tightened to `test [^;]+ -[oa] [^;]+` — eliminates 4 false positives on `set --append` lines while preserving detection of genuine `test EXPR -[oa] EXPR`. No ry-install delta.

v4.4.1  2026-04-21
- 374 checks. Fix 5 corrections. Check 180 SYSCTL.D INTEGRATION: `MANAGED_FILE_COUNT=15` → `_RY_MANAGED_FILE_COUNT=16`, filename `99-ry-sysctl.conf` → `99-cachyos-sysctl.conf`, position `(12th)` → `(11th)`. Check 379/369: 'Profile destination key collision' label retained as v4.1.5 prefix (1 hit expected, not 0). Check 384: side-note attribution corrected — L5951 is top-level log-rotation, not `_acquire_lock` (which spans L377-452). PASS 2 STEP 1: root-rejection guard added (auto-detect, PASS2_FISH_USER override defaulting to `nobody`, runuser wrap, mktemp HOME with EXIT-trap cleanup) — closes false-fail when audit runs as root since ry-install exits 2 for `(id -u)==0`. Check 249 SIGPIPE: relaxed to `{0, 141}` — help (~1.6 KB) fits 64 KB pipe buffer so PIPESTATUS=0 is normal; static checks 189 + 321 remain authoritative. No ry-install delta.

v4.4.0  2026-04-21
- 374 checks (363 static + 11 runtime). Sync ry-install v4.1.8. +4 (381–384): Phase 14.32 HYGIENE + HARDENING — `_install_packages` pipeline-phase comments normalized (sync _info dropped, phase-4 cross-reference); `_ry_show_help` `--` description reflects dispatcher positional rejection + NO_COLOR added to ENVIRONMENT block; `_install_fstab_opts` deregisters intermediate `$tmpfstab` from `_TRACKED_TMPFILES` after atomic rename; `_preflight_boot_sanity` 3 find enumerations (vmlinuz/initramfs/loader-entries) switched to `-print0 | string split0` for `\n`-in-filename safety. +14 GROUP D rg patterns (V418 *). v4.1.7 (README/CHANGELOG-only) carries no auditable script delta. PASS 3: P14 171→175, TOTAL 370→374. Checklist-save gate 359→363. No counter rebaseline needed — v4.1.8 deltas do not affect set -l / set -g / bare-set totals.

v4.3.0  2026-04-19
- 370 checks (359 static + 11 runtime). Sync ry-install v4.1.6. +1 (380): Phase 14.31 SIGNAL-SAFE JSONL REDIRECTS — all 6 JSONL append writers paired `>>"$LOG_FILE" 2>/dev/null`; closes stderr-noise window under signal-triggered log rotation. +5 GROUP D rg patterns (V416 *). Counter rebaseline 197–199, 204, 213 against v4.1.6. PASS 3: P14 170→171, TOTAL 369→370.

v4.2.0  2026-04-19
- 369 checks (358 static + 11 runtime). Sync ry-install v4.1.5. +1 (379): Phase 14.30 DIAGNOSTICS REFINEMENT — `_validate_profile` destination guard split into duplicate-source + key-collision checks. Rewrite 369; legacy message added to "Expect 0 hits". +4 GROUP D rg patterns. Phase count header corrected 16→15 (profile system is 15.13, not separate). PASS 3: P14 169→170, TOTAL 368→369. Prose strip across PURPOSE/SETUP/EXECUTION RULES; every check, command, regex preserved.

v4.1.0  2026-04-19
- 368 checks (357 static + 11 runtime). Sync ry-install v4.1.4. +11 (368–378): Phase 14.29 PARITY + HARDENING — _tmpfile_key sha256 prefix retired, _validate_profile collision guard, merge/hash-worker simplified, Job 2 svc_dsts guard, pkill descendant reap, _detect_lvm 5→10s, curl --connect-timeout, _run stderr trim, boot-time LC_ALL=C. Rewrite 200, 355, 365. Stale-counter sweep (185, 195–199, 201, 204, 212, 213). +13 GROUP D rg patterns. PASS 3: P14 158→169, TOTAL 357→368.

v4.0.0  2026-04-19
- 357 checks (346 static + 11 runtime). Sync ry-install v4.1.3. Retire 168–170 (backup/rollback/--dry-run) and 227–230 (completions/test-all); slots reused for flag coverage, exit-code help-text, profile-system invariants. Phase 12 runtime 15→11 (drop removed-mode probes; add argparse --exclusive, deprecated-flag rejection, --check 3/10, SIGPIPE help-text). +34 (334–367): Phase 14.25–14.28 v3.47.0–v4.1.3 covering source-safety, profile system, BOOT_WIPE/SYU acks, kernel/sysctl/pkg overhaul, nftables, RADV, _tmpfile_key, argparse --exclusive dispatcher, _write_footer/BLS/exit-triple-form. Phase 6 (73–76, 99–100), Phase 7 (103–106) updated. +45 GROUP D rg patterns. PASS 2 collapsed A–E → 4 subgroups.

v3.9.2  2026-04-06
- 330 checks. Fix 8 Phase-14 rg patterns with escaped `\|` in single quotes (silent false negatives); SYSTEM=12/USER=3/SERVICE=1=16 reconciliation; P14 range 254-324→254-333; BASHISMS rg completion; STEP 4 zip + placeholder guards.

v3.9.1  2026-04-06
- 330 checks. Counter fixes (306→315, 321→330, P14 115→124); PASS 3 table reorder + PASS column; SPECIAL HANDLING expansion; per-phase tally; UNIQUENESS VERIFICATION documented bash-only.

v3.9.0  2026-04-06
- 330 checks (315 static + 15 runtime). Sync ry-install v3.46.0. +9 (325–333): preempt=full, page_lock_unfairness removal, netdev_max_backlog, somaxconn, sysctl priority-99 header, string split -m1, noatime dedup, usbhid.mousepoll removal, coredump.conf.d semantic verify.

v3.8.1  2026-04-05
- 321 checks. Sync ry-install v3.44.0. +20 (305–324): fstab, sysctl, service removals, sudo -n, IPv6, SSID, LVM mask, quoted destinations. +17 GROUP D rg patterns.

v3.8.0  2026-04-05
- 301 checks. +7 Fish 4.0–4.6 compat (298–304). Pin rg ≥ 14.1.1 (bug #2884). Add EVIDENCE RULE, set -uo pipefail, LC_ALL=C.UTF-8, PATH sanitization, umask 077.

v3.7.32  2026-03-18
- 294 checks. +1: sudo -n credential safety in _find_pacnew_files.

v3.7.31  2026-03-18
- 293. Deduplicate _py_extract calls; VERSION guard.

v3.7.30  2026-03-18
- 293. Fix grep -c double-output; remove PCRE2 flag.

v3.7.29–v3.7.23  2026-03-17
- 293 checks. Accumulated fixes: grep fallback, stdout modes, glob handling, budget + timeout tuning, SIZE GATE, dry-run FS monitoring, GROUP E dedup, data mapping, GROUP_EXIT, severity labels, BASH AND-OR rg pattern. v3.7.23 +8 (289–296): fish -c injection, brace expansion, parallel status, glob, wait guard, worker cap, symlink, deprecated test operators.

v3.7.13  2026-03-16
- 285 checks. Unified version scheme (31.x → 3.7.x). 12 rg pattern fixes. Prior lineage: v25 (254 checks) → v31.2.1 (285 checks), 22 releases.
