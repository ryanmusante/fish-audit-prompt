FISH SHELL SCRIPT AUDIT PROMPT
===============================
Version: 7.3.5
Derived from: ry-install.fish v7.3.5 (2026-05-17)
Checks: 431 (420 static + 11 runtime)
Phases: 15 (P1-P11 static · P12 runtime · P13 gap · P14 v3.1.0–v7.3.5 specifics · P15 supplemental)
Scope: production fish scripts (system config, sudo, credentials, embedded configs, argparse dispatch, verification).
Policy: ZERO-SKIP. Every source line. Trace error branches, unreachable code, early returns. No sampling.
Resync note: v7.3.5 fully resyncs against ry-install v7.3.5 (delta from v7.3.3 baseline: 7 polish/forensics changes, LOC 5136 → 5143; invariants unchanged at all 15 entries — see v7.3.5 narrative addendum appended at the end of the historical resync paragraph below).
   The v5.0.1 → v6.0.0 transition is a REDUCTION (5994→4985 L, -1009): the v6.0.0 release deliberately retired entire subsystems documented in earlier Phase-14 subsections — source-mode scaffolding (_RY_INSTALL_SOURCED, _ry_namespace_cleanup, _ry_exit triple-form), the sudo-keepalive child (_start_sudo_keepalive, _kill_sudo_keepalive, SUDO_KEEPALIVE_ERR), JSONL progress events, log rotation, LVM detection (_detect_lvm), boot-wipe ack gates (_bwg_managed_only, BOOT_WIPE_AUTO_ACK*, RY_INSTALL_CONFIRM_BOOT_WIPE, .boot-wipe-acknowledged marker), atomic-write TOCTOU re-stat as a separate pass, parallel-child PID guards, _ntsync_per_kernel_state, _reclaim_stale_lock as a separate function, and test -d /proc/$pid liveness (reverted to kill -0). Every check referencing those symbols is now a 0-hit regression detector. Original v5.0.1 resync note follows for historical lineage.

   Original v5.0.1 resync note (historical): Encoded since v4.5.8 (35+ minor releases): v4.5.25 _RY_POST_HOOKS table + _post_hook_for_target + _ry_namespace_cleanup bail wiring + hermetic keepalive child (fish --no-config -c); v4.5.27 _run dash-prefix rejection + _chk_grep grep -wF whole-word + _verify_summary VERIFY_GEN_FAIL + _do_cleanup two-pass tmpfile sweep + 7 verify/install orchestrator splits + _resolve_esp /boot fallback + ping soft-dep + sysctl generator rc=13; v4.5.28 _run / _verify_static_boot / _install_fstab_opts / _preflight_boot_sanity splits; v4.5.29 137 if-blocks → inline X; and Y (LOC 5495→5005); v4.5.31 _verify_static_system 5×_vss_* + _acquire_lock fresh+reclaim split + _vrk_gpu_state 3×_vrkg_* + _msg_print/_msg_nocount/_fail_silent helpers; v4.5.32 _err_loud (preflight errs surface regardless of QUIET); v4.5.33 _redact_argv_elements + _installed_bytes rc 0/1/2 + _PROFILE_USES_NM→_PROFILE_USES_WIFI_BACKEND + _RY_HOME_DIR + _RY_RUN_TIMEOUT_DEFAULT named constants + _grep_kparam + _grep_kv argv-count guard + _configure_services_preset split; v4.5.34 19 oversized orchestrators split (function count 200→239; LOC 5098→4849); v4.5.35 _untrack_tmpfile erase-when-empty (sourced-caller scope leak fix); v4.5.36 tmpfile registration moved before validation + _track_tmpfile + _resolve_systemd_ver helpers + _ir_validate_counts runtime asserts; v4.5.37 ENV_VARS 13→12 (PROTON_NO_WM_DECORATION); v4.5.38 ENV_VARS 12→11 (ENABLE_LAYER_MESA_ANTI_LAG); v4.6.0 minor consolidation; v4.6.1 _ry_show_help hoisted above early-arg loop + EXIT_GEN_NOFN=11 / EXIT_GEN_NOUUID=12; v4.6.2 EXIT_GEN_SYSCTL=13 + sudo/df/mkdir/rmdir hard-deps; v4.6.3 _start_sudo_keepalive child-fish drops -- against fish builtin test (CRITICAL FIX) + _RY_BOOT_TAINTED separated from INSTALL_HAD_ERRORS + _cse_batch_enable ENABLE_OK_START_FAIL marker + keepalive child stderr capture (SUDO_KEEPALIVE_ERR); v4.6.4 nftables dropped from PKGS_ADD (14→13) + nftables.service from EXPECTED_SERVICES (4→3) + PKGS_ADD:13 invariant; v4.6.5 _msg no longer gates VERIFY_* on VERIFY_MODE (real install footer counts) + ERR bumps VERIFY_FAIL + libva-mesa-driver→mesa, lib32-libva-mesa-driver→lib32-mesa + _ntsync_per_kernel_state + _ntsync_check_installed_kernels (advisory) + _rot_pipe_ok pipestatus[1]-only; v4.6.6 _bwg_managed_only auto-ack for SDBOOT_REMOVE_EXISTING=yes (BOOT_WIPE_AUTO_ACK / BOOT_WIPE_AUTO_ACK_DECLINE); v4.6.7 ufw out of PKGS_DEL (8→7) + ufw.service into MASK (10→11); v4.6.8 _resolve_boot_path (cached _RY_BOOT_PATH via bootctl -x) + _pbs_entry_has_valid_kernel BLS Type #1 linux= resolution; v4.6.9 mv -T / mv -Tf atomic write + _reclaim_stale_lock liveness via test -d /proc/$old_pid (replaces kill -0 EPERM≠ESRCH) + _csp_remove_pkgs/_ip_pacman_invoke db.lck retest (PKG_REMOVE_BATCH_FAIL_DBLOCK) + _install_aur_packages skips per-pkg retry when count $AUR_PKGS ≤ 1 + _check_phase_files consumes _installed_bytes rc directly + _install_finalize gates _if_write_wipe_marker on INSTALL_HAD_ERRORS=false; v4.6.10 _ry_install_file user-scope mkdir -p wrapped in umask 0077; v4.6.11 8 read-only pacman -Q*/-T/-Qi gain command prefix + keepalive child drops 2>/dev/null on breaking sudo -n -v; v4.6.12 _reclaim_stale_lock /proc/<pid>/cmdline substring match (defence-in-depth against fish-PID reuse) + MKINITCPIO_HOOKS:11 + MKINITCPIO_MODULES:1 invariants; v4.6.13–.15 README admonition + table trims; v4.6.16 _cleanup_pipe NO LONGER EXITS 141 — sets _RY_OUTPUT_BROKEN, returns; JSONL log canonical + _msg_print/_err_loud/_echo short-circuit on _RY_OUTPUT_BROKEN + _verify_summary uses _msg_nocount (off-by-one fix) + _cg_access_ok / _mkinitcpio_revert drop -- from sudo -n test -f / test -L (coreutils 3-arg form parses as binary-op rc=2); v4.6.17 mkinitcpio-firmware moved from PKGS_ADD to AUR_PKGS (paru is now HARD prereq) + drop unreachable case pipe arm from _teardown + drop 141 SIGPIPE row from help / README signals tables + PKGS_ADD:12 AUR_PKGS:2 invariants + PROFILE_NAME/PROFILE_DESC/_RY_MANAGED_FILE_COUNT hoisted above _ry_show_help; v4.6.18 _ir_precompute_caches one-statement-per-line; v4.6.19 _vsp_pacman_conf explicit `if not test -f` block (broken or-chain fix); v4.6.20 GTR9_PRO BUILT-IN DEFAULTS banner collapsed; v5.0 release milestone (4.6.20 → 5.0; no functional changes); v5.0.1 verbose-comment trims at L210/L2068 (technical signal preserved). Historical v5.0.1 invariants asserted at runtime via `_ir_validate_counts`: KERNEL_PARAMS:15 MKINITCPIO_HOOKS:11 MKINITCPIO_MODULES:1 LOGIND_IGNORE_KEYS:9 ENV_VARS:11 SYSCTL_VALUES:16 PKGS_ADD:12 PKGS_DEL:7 AUR_PKGS:2 MASK:11. CURRENT v7.3.3 invariants (15 entries — up from 10 in v5.0.1): KERNEL_PARAMS:15 MKINITCPIO_HOOKS:11 MKINITCPIO_MODULES:1 LOGIND_IGNORE_KEYS:9 ENV_VARS:11 SYSCTL_VALUES:16 PKGS_ADD:15 PKGS_DEL:8 AUR_PKGS:2 MASK:12 EXPECTED_VULKAN_PKGS:3 EXPECTED_SERVICES:3 _RY_PKG_MANAGED_SERVICES:1 _RY_POST_HOOKS:14 _RY_BOOT_CRITICAL_DSTS:4. v5.0.1 baselines: function count 246 (was 137), --description 246 (100%), --argument-names 87 (was ~52), LOC 5,029 (was ~5,205), set -l ~464, bare set ~115, set -g ~108, _run wrap ~52. SYSTEM destinations 11 (was 12 at v5.0.1 era); _RY_MANAGED_FILE_COUNT 12 at v7.3.3 (was 15 at v5.0.1 era — drift: v6.5.14 added /etc/tmpfiles.d/99-cachyos-thp.conf (12→13); v7.2.0 dropped /etc/drirc + added /etc/default/cpupower-service.conf (net 13→13, composition changed); v7.2.1 dropped cpupower-epp.service + emptied SERVICE_DESTINATIONS (13→12); USER destinations 1 (the env file) — the v5.0.1 USER=3 baseline was wrong: v5.0.1 had USER=1 already, the prompt narrative was stale on that point). Signal architecture: SIGPIPE non-fatal (sets _RY_OUTPUT_BROKEN, returns 0) — preserved across v5.0.1→v7.3.3. _cleanup_pipe gained _RY_HEADER_WRITTEN gate at v6.5.11 (suppresses SIGPIPE log when no header was written yet). USR1/USR2/ABRT handlers via _cleanup_other delegate to _cleanup (138/140/134). Architectural reversions to be aware of: v6.0 reduction release dropped source-mode scaffolding (_RY_INSTALL_SOURCED, _ry_namespace_cleanup, triple-form `_ry_exit \$CODE; and return; or return`), the entire sudo-keepalive subsystem, LVM detection, _ntsync_per_kernel_state, the _reclaim_stale_lock helper split (reverted to inline reclaim in _acquire_lock), test -d /proc/\$pid liveness (reverted to kill -0), the BOOT_WIPE_AUTO_ACK ack subsystem, and JSONL progress events. v6.x added _RY_POST_HOOKS as a 14-row dispatch table for --install-file (replaces inline switch), _atomic_write_file post-write symlink re-check, _fstab_atomic_replace findmnt --verify hard-fail, _ip_pacman_invoke -Syyu retry gated on RY_INSTALL_ALLOW_PARTIAL_UPGRADE, _run timeout-bypass for pacman/paru/mkinitcpio/sdboot-manage/paccache, tmpfile-path redaction under \$TMPDIR, capture cap 100→500 with _TRUNCATED sentinels, _run split into 5 helpers (_run + _run_redact_cmd + _run_effective_timeout + _run_emit_stream + _run_resolve_timeout), user-bus detection via XDG_RUNTIME_DIR/bus + systemctl --user is-system-running, _vrkg_* GPU runtime checks, _csp_filter_rdeps cascade helper, _ir_validate_counts promoted to 15-invariant set. v7.0 added avahi-daemon.{service,socket} to MASK (10→12), realtime-privileges to PKGS_ADD, bolt to PKGS_DEL, RADV_PERFTEST=transfer_queue → RADV_EXPERIMENTAL=transfer_queue ENV_VARS rename, NM 1.56.0 compat drop of wifi.iwd.autoconnect=false, _csm_disable_ufw_rules helper, _ry_check_wireless_regdom + RY_INSTALL_WIRELESS_REGDOM env (v7.1.0), _ry_apply_wireless_regdom soft-fail (v7.1.1). v7.0.5 added _post_tmpfiles + */tmpfiles.d/* glob. v7.0.7 added 68 command-prefix conversions. v7.0.11 added date(1) to dep set. v7.2.0 swapped /etc/drirc → cpupower-service.conf, added cpupower to PKGS_ADD + cpupower.service to EXPECTED_SERVICES, retired _post_drirc / _grep_xml_tag / _content__etc_drirc, added _post_cpupower + _vrsv_chk_cpupower_governor. v7.2.1 dropped cpupower-epp.service entirely (SERVICE_DESTINATIONS emptied; _RY_MANAGED_FILE_COUNT 13→12; EXPECTED_SERVICES 4→3). v7.2.3 added _vmh_order_checks pair rules (systemd:autodetect, autodetect:microcode) + fsck-last check. v7.2.4 added _RY_POST_HOOKS:14 + _RY_BOOT_CRITICAL_DSTS:4 to _ir_validate_counts invariant set, unified _vre_fstab token regex to `(^|,)tok(,|$)` (fixes lazytime vs nolazytime substr false-match), added _mr_copy_size_verify cmp -s byte content check after size match. v7.3.2 promoted systemd-version <250 from _warn to _err+return 1 (matches README's documented hard-fail contract). v7.3.3 README phase-4 install-flow row realigned to actual script execution order (mask/enable order corrected). v7.3.5 (post-v7.3.3 polish; v7.3.4 skipped): `_err_loud` gains MODE=check silent-probe contract — preserves `--check` idempotency-probe silence on fatal preflight while keeping forensics in JSONL (`_log "ERR:"` still fires; `_msg_print --force` is gated on `test "$MODE" = check; and return 0`); `_acquire_lock` stale-pid regex tightened `^\d+$` → `^[1-9]\d*$` (rejects `pid=0` / leading-zero values which would falsely succeed `kill -0` as process-group signal); `_resolve_esp` / `_resolve_boot_path` 4 sites switched `echo "$_p"` / `echo "$_RY_*_PATH"` → `printf '%s' "..."` for command-substitution symmetry with `_bootctl_dir` (no trailing newline pollution in cached-return path); `_ry_do_check` 3× rc-handling blocks collapsed via for-loop over phase fn names (51 → 40 L, within ≤50 L preference); `_ry_check_network` L1600 `test ...; and X; or Y` → explicit `if/else` (removes dependency on `_ok`-returns-0 branch-selection invariant; two other `; and ...; or ...` sites at L130 TMPDIR + L403 begin/end OR-chain intentionally retained as different idioms); Bootstrap merge — L180 chmod + L181-188 perm-verify collapsed into single-pass loop over `_RY_HOME_DIR` / `_RY_HOME_DIR/logs` / `LOG_DIR`, with `LOG_DIR_PERM_FIX: <path> <pre>→<post>` notices recorded in deferred-log buffer `_RY_PERM_FIX_NOTICES` and flushed before `_init_runtime` once `_log` is callable and the JSONL header is on disk; `_ry_show_help` EXIT CODES reworded to note fish 3.x `--on-signal` handler limitation (parent `$status` may show 0 instead of 130/143/etc — canonical signal code recorded in JSONL `footer.exit_code`); `_ry_show_help` `--check` annotated with "requires functional sudo + systemctl; ERR_NO_DATA from systemctl probes is treated as preflight" — matches `_cpu_chk_expected` / `_check_phase_units` semantics; `_install_rebuild_boot` / `_post_boot` boot-taint message clarified — "intra-process flag" replaces ambiguous "(prior aborted install?)". LOC 5136 → 5143 (+7). v7.3.5 baselines (drift vs v7.3.3 — all within ±15% tolerance): set -l 477 → 478, bare set 129 → 127, in-function set -g 135 (unchanged), top-level set -g 85 (unchanged), `_run\b` 49 (unchanged), `; and ` 295 → 297, `; or ` 170 → 169, `string match -qr` 58 (unchanged), `mktemp` 28 (unchanged), command-prefix 196 → 197, function count 253 (unchanged), --description 235 (unchanged), --argument-names 84 (unchanged). Function rename map: _PROFILE_USES_NM (v4.5.33) → _PROFILE_USES_WIFI_BACKEND (current). RY_INSTALL_* env vars surface (v7.3.3): RY_INSTALL_ALLOW_PARTIAL_UPGRADE, RY_INSTALL_FORCE_BOOT_REBUILD, RY_INSTALL_PKG_REMOVE_CASCADE, RY_INSTALL_SKIP_HARDWARE_CHECK, RY_INSTALL_WIRELESS_REGDOM. Retired env vars (regression detectors): RY_INSTALL_CONFIRM_BOOT_WIPE, RY_INSTALL_CONFIRM_SYSTEM_UPGRADE — both subsystems removed.

───────────────────────────────────────────────────────────────
 SETUP
───────────────────────────────────────────────────────────────

STEP 1: COPY + TOOLS (auto-detects apt-get / pacman / dnf)

  ```bash
  mkdir -p /home/claude/audit-{src,data}
  cp /mnt/user-data/uploads/<file> /home/claude/audit-src/script.fish
  wc -l /home/claude/audit-src/script.fish

  if command -v apt-get >/dev/null; then
    apt-get update -q 2>/dev/null
    apt-get install -y fish fd-find bat ripgrep 2>/dev/null; true
  elif command -v pacman >/dev/null; then
    pacman -S --needed --noconfirm fish fd bat ripgrep 2>/dev/null; true
  elif command -v dnf >/dev/null; then
    dnf install -y fish fd-find bat ripgrep 2>/dev/null; true
  else
    echo "[CRITICAL] unsupported package manager — install fish fd bat rg manually" >&2
    exit 2
  fi
  # Pin rg >= 14.1.1 (Ubuntu 24.04 ships 14.1.0 with false-negative bug #2884); numeric-tuple compare is future-proof.
  _ver=$(rg --version 2>/dev/null | head -n 1 | awk '{print $2}')
  IFS=. read -r _rg_maj _rg_min _rg_pat <<<"$_ver"
  if [ "${_rg_maj:-0}" -lt 14 ] || { [ "$_rg_maj" -eq 14 ] && [ "${_rg_min:-0}" -lt 1 ]; } \
     || { [ "$_rg_maj" -eq 14 ] && [ "$_rg_min" -eq 1 ] && [ "${_rg_pat:-0}" -lt 1 ]; }; then
    curl -sLO https://github.com/BurntSushi/ripgrep/releases/download/15.0.1/ripgrep_15.0.1-1_amd64.deb && \
      dpkg -i ripgrep_15.0.1-1_amd64.deb && rm -f ripgrep_15.0.1-1_amd64.deb
  fi
  unset _ver _rg_maj _rg_min _rg_pat
  for pair in "fdfind fd" "batcat bat"; do
    set -- $pair; src="$1" dst="$2"
    if command -v "$src" >/dev/null 2>&1 && ! command -v "$dst" >/dev/null 2>&1; then
      ln -sf "$(command -v "$src")" "/usr/local/bin/$dst"
    fi
  done
  fish -c '
    for t in fish rg fd bat
      if command -q $t; echo "$t: OK"; else; echo "$t: missing"; end
    end
  '
  ```

STEP 2: FALLBACKS (bash-only; uses arrays, ${args[-1]}, ${range%%:*} — do not fish-ify)

  ```bash
  [ -n "$BASH_VERSION" ] || { echo "[CRITICAL] SETUP STEP 2 requires bash — shim uses arrays, \${args[-1]}, \${range%%:*}, export -f" >&2; exit 2; }

  command -v rg  >/dev/null 2>&1 || { echo "[CRITICAL] rg required; install ripgrep >= 14.1.1 via SETUP STEP 1" >&2; exit 2; }
  command -v bat >/dev/null 2>&1 || bat() {
    local args=() range=""
    while [[ $# -gt 0 ]]; do
      case "$1" in --plain) shift ;; -r) shift; range="$1"; shift ;; *) args+=("$1"); shift ;; esac
    done
    if [[ -n "$range" ]]; then sed -n "${range%%:*},${range##*:}p" "${args[-1]}"; else cat "${args[@]}"; fi
  }
  command -v fd  >/dev/null 2>&1 || fd() { local pat="$1"; shift; find "$@" -name "$pat" 2>/dev/null; }
  export -f bat fd 2>/dev/null; true

  # Hardening defaults (post-STEP 2, pre-collection)
  export LC_ALL=C.UTF-8                    # Reproducible coreutils (F-6)
  export PYTHONUTF8=1                      # UTF-8 Python I/O (F-7)
  export RIPGREP_CONFIG_PATH=""            # Ignore inherited .ripgreprc (F-13)
  export PATH='/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin' # Sanitized PATH; /usr/sbin for runuser (F-15)
  unset CDPATH BASH_ENV ENV 2>/dev/null; true
  umask 077                                # Owner-only temp files (F-18)
  ```

STEP 3: SAVE CHECKLIST TO DISK

  Copy CHECKLIST REFERENCE section (end of doc, through Phase 15) verbatim to
  /home/claude/audit-data/checklist.txt via view → create_file. No regeneration.
  Verify: grep -c '^\s*\[ \]' checklist.txt → 420 (P12's 11 runtime checks inline in PASS 2; total 431).
  Read on demand: bat --plain -r START:END /home/claude/audit-data/checklist.txt

STEP 4: REFERENCE

  FISH SYNTAX (auditor reference):
    $var (not ${var}) · (command) (not $(command)) · test/string (not [[]])
    set -gx (not export) · ; and / ; or (not &&/||) · set --erase (not set -e)
    for x in $ARRAY unquoted · "text"(cmd) is valid concat · -- before args
    read -P "prompt: " var · read -sP for hidden input
    REDIRECT BUG: < nonexistent 2>/dev/null leaks → use cat -- 2>/dev/null

  CONFIG SYNTAX: see userPreferences [AUDIT] section
  CROSS-REFS: see userPreferences [AUDIT] cross_refs

───────────────────────────────────────────────────────────────
 EXECUTION
───────────────────────────────────────────────────────────────

RULES:
  Work continuously. Batch + parallel where independent. >60 lines output → file.
  Blocked → DEFERRED, continue. Save after every pass. Max 3 retries.
  Never re-read same region. >10 items → sub-chunks of 5.
  Findings: echo "F-N ..." >> /home/claude/audit-findings.txt

EVIDENCE RULE:
  Every finding cites file:line or data excerpt. No evidence → UNVERIFIED.
  PASS 3 rejects evidence-less findings. 0 findings > 1 fabricated finding.

TOOL MANDATES:
  RUST FIRST    rg > grep, fd > find, bat > cat/less.
  PARALLEL      Independent rg/fd backgrounded with & + wait. Group into ( ... ) & subshells.
  BATCH         One bash_tool call per pass, not per check.
  PYTHON3       For function body extraction, scope/shadow, tmpfile lifecycle, comment
                coverage, numeric var init, in-function globals, command prefix, bare
                coreutils. Assumes fish_indent input (single-line begin;cmd;end breaks
                depth tracking — check 215 must pass first). Define _py_extract (STEP 1).
  PCRE2 GUARD   rg -oP/-cP needs PCRE2; fall back to rg -o + grep -oP. rg -Un needs ≥11;
                fall back to awk.
  OUTPUT CAPS   Every multi-line rg piped through | head -N (-30 high-hit, -50 medium,
                -20 low). Exception: rg -c.

CONTEXT BUDGET (3000 lines total):
  PASS 1 800 · PASS 2 600 · PASS 3 200 · Reasoning reserve 1400.
  PASS 1 > 20 findings → summarize to disk, drop caps to 20/30/10.

  Raw data → disk only. Never paste >100 lines of rg to context.
  After each collection: wc -l outputs, report counts only.
  Findings → /home/claude/audit-findings.txt (append).
  Summary → context (counts + severity + check numbers).
  Single call >200 lines: truncate, note "[truncated, full output in $D/<file>.txt]".

STALL PREVENTION (>30 tool calls or >30 messages):
  1. Save findings  2. Mark remaining phases SKIPPED  3. Jump to PASS 3
  4. Partial spec + COVERAGE line  5. Verdict "incomplete — N/15 phases completed"

  CHECKPOINT after each pass:
    "N findings [F-1:L42:C5, ...], phases [list] complete, [list] remaining.
     Carry-forward: VERSION=X, FUNC_COUNT=Y, SET_L_COUNT=Z."
    Dedup: skip if finding ID (file:line:check) exists.

PASS ORDER:
  PASS 1 — GATHER+ANALYZE  phases 1-11, 13-15  (collect → analyze → findings → summary)
  PASS 2 — RUNTIME         phase 12             (fish execution tests)
  PASS 3 — FINALIZE        —                    (summary + spec + zip)

═══════════════════════════════════════════════════════════════
 PASS 1: GATHER + ANALYZE (phases 1-11, 13-15)
═══════════════════════════════════════════════════════════════

All subshells parallel. All rg capped with | head -N. Raw data → disk.

STEP 1: DATA COLLECTION

  ```bash
  F=/home/claude/audit-src/script.fish
  D=/home/claude/audit-data
  O=/home/claude/audit-findings.txt
  mkdir -p "$D"
  # ── Resume guard: preserve partial findings from interrupted runs ──
  if [ -s "$O" ]; then
    mv "$O" "${O%.txt}-$(date +%s).bak"
    echo "[INFO] Previous findings backed up to ${O%.txt}-*.bak"
  fi
  : > "$O"

  # ── Python3 helper for function body extraction (primary; fish -c fails on some versions) ──
  _py_extract() {
    python3 -ISc "
import re, sys
target = sys.argv[2]; F = sys.argv[1]
lines = open(F).readlines()
cap = False; depth = 0
block_re = re.compile(r'^\s*(function |if |for |while |switch |begin\b)')
for line in lines:
    s = line.strip()
    if not cap:
        if re.match(r'^function ' + re.escape(target) + r'([ \-]|$)', line):
            cap = True; depth = 1
        continue
    if not s.startswith('else') and block_re.match(line):
        depth += 1
    if s == 'end':
        depth -= 1
        if depth == 0: break
        print(line.rstrip())
    else:
        print(line.rstrip())
" "$1" "$2"
  }
  export -f _py_extract

  # ── Centralized skip list for message/output functions (used by DRY GATE + BARE COREUTILS) ──
  MSG_SKIP='_run|_log|_dry|_fail|_err|_warn|_ok|_info|_echo|_msg|_ask'
  export MSG_SKIP

  # ── Cleanup trap: kill orphaned background children on interrupt (F-19) ──
  trap 'kill $(jobs -p) 2>/dev/null; wait' EXIT INT TERM

  # ── GROUP A1: P1-3 syntax, routing, errors ──
  ( set -uo pipefail; (
    echo "=== BASHISMS ===" && rg -n '\$\(|\[\[|\$\{|export |&&|\|\||set -e |functions -e|unset |local ' "$F" | rg -v '^[0-9]+:\s*#|ExecStart=|^\s*awk |<<-?\s*['"'"'"]?EOF|\[\[:[a-z]+:\]\]|# lint:ignore|set -e \$|set -e [_A-Za-z]|functions -e [_a-z]|set --erase|XDG_RUNTIME_DIR unset' | rg -v "'[^']*\\\$\{|'[^']*\\|\\||\".*&&.*\"|\".*\\|\\|.*\"" | head -30
    echo "    NOTE: BASHISMS detector intentionally coarse — exempts # comments, ExecStart=, awk lines,"
    echo "    heredocs, [[:class:]] POSIX char classes, # lint:ignore markers, fish set --erase forms"
    echo "    (set -e VAR / set -e \$var / set --erase), functions -e fn, single-quoted printf-arg"
    echo "    literals (containing \${VAR} or ||), && / || inside double-quoted strings."
    echo "    Survivors require manual triage — verify each is genuine bash, not fish or string-literal."
    echo "=== FISH_INDENT ===" && if command -v fish_indent >/dev/null 2>&1; then fish_indent --check "$F" 2>&1; echo "exit: $?"; else echo "UNAVAILABLE (fish_indent not found — check 216 DEFERRED)"; fi
    echo "=== STRING OPS ===" && rg -n 'string (match|replace|split|join|trim)' "$F" | rg -v ' -- ' | head -30
    echo "=== ARGPARSE ===" && rg -n 'argparse|argv' "$F" | head -30
    echo "=== ROUTING ===" && rg -n '>&2|>/dev/null|stderr|stdout' "$F" | head -50
    echo "=== NO_COLOR ===" && rg -n 'NO_COLOR|isatty|tput|test -t' "$F" | head -20
    echo "=== EXIT/RETURN ===" && rg -n 'exit [0-9]|return [0-9]|exit$' "$F" | head -30
    echo "=== STATUS ===" && rg -n '\$status|\$pipestatus' "$F" | head -30
    echo "=== SIGNALS ===" && rg -n 'trap|--on-signal|--on-event|fish_exit' "$F" | head -20
    echo "=== CLEANUP ===" && rg -n 'mktemp|rm -f|rm -rf|cleanup|_do_cleanup' "$F" | head -30
  ) > "$D/p1-3.txt" 2>&1; echo "GROUP_A1_EXIT=$?" >> "$D/p1-3.txt" ) &

  # ── GROUP A2: P4-5 vars, security ──
  ( set -uo pipefail; (
    echo "=== SCOPE ===" && rg -n 'set -[glx]|set --erase|set --global|set --local' "$F" | head -50
    echo "=== FOR LOOPS ===" && rg -n 'for .* in ' "$F" | head -30
    echo "=== INJECTION ===" && rg -n '\beval\b|source ' "$F" | head -20
    echo "=== CHK_GREP IMPL ===" && _py_extract "$F" "_chk_grep" | rg 'grep' | head -10
    echo "=== CREDS ===" && rg -ni 'password|passphrase|credential|secret|WIFI_PASS' "$F" | head -20
    echo "=== PERMS ===" && rg -n 'chmod|chown|0600|0644|0700|0755' "$F" | head -30
    echo "=== SUDO ===" && rg -n 'sudo |_IS_ROOT' "$F" | head -30
    echo "=== EXT CMDS ===" && rg -n 'command -q|command -v' "$F" | head -30
    echo "=== NUMERIC VAR INIT ===" && python3 -ISc "
import subprocess, sys; F = sys.argv[1]
# Excludes Fish builtins (\$status) and script globals (MAX_LOGS) — always initialized by runtime/top-level set -g
for v in '_boot_rc _flock_rc _log_count actual_count bad_perms case_count checked code dir_bad dir_checked errors failed fixed_count i idx_a idx_b install_status line_count mask_count mismatches mode_count next_i next_i2 num_workers pac_managed_bad pad perm_bad perm_checked read_ok reload_wait ret start_idx steps_count total_dsts total_err wifi_retry'.split():
    init = subprocess.run(['rg', '-c', f'set -l {v} ', F], capture_output=True, text=True)
    used = subprocess.run(['rg', '-c', 'test \$' + v + ' ', F], capture_output=True, text=True)
    print(f'{v}: init={init.stdout.strip() or \"0\"} test={used.stdout.strip() or \"0\"}')
" "$F" 2>&1
  ) > "$D/p4-5.txt" 2>&1; echo "GROUP_A2_EXIT=$?" >> "$D/p4-5.txt" ) &

  # ── GROUP B: P6-7 embedded configs, cross-refs ──
  ( set -uo pipefail; (
    echo "=== CONFIG FORMATS ===" && rg -n '\[Unit\]|\[Service\]|\[Install\]' "$F" | head -20
    echo "=== SYSTEMD ===" && rg -n 'ExecStart|After=|Type=' "$F" | head -20
    echo "=== UDEV ===" && rg -n 'ACTION==|SUBSYSTEM==|ATTR[{]|KERNEL==' "$F" | head -20
    echo "=== DESTINATIONS ===" && rg -n 'SERVICE_DESTINATIONS|cpupower-epp' "$F" | head -20
    echo "=== SDBOOT ===" && rg -n 'LINUX_FALLBACK_OPTIONS|SDBOOT' "$F" | head -20
    echo "=== VERSION ===" && rg -n 'VERSION|SYSTEM_DEST|USER_DEST|SERVICE_DEST|get_file_content|MASK' "$F" | head -30
    echo "=== DISPATCH ===" && rg -n 'do_\w+ \$|do_\w+ "\$' "$F" | rg -v 'function |#|_log' | head -20
    echo "=== SYSCTL ===" && rg -n 'sysctl|BBR|tcp_fastopen|default_qdisc|inotify' "$F" | head -30
  ) > "$D/p6-7.txt" 2>&1; echo "GROUP_B_EXIT=$?" >> "$D/p6-7.txt" ) &

  # ── GROUP C1: P8-9 architecture, patterns ──
  ( set -uo pipefail; (
    echo "=== FUNCTIONS ===" && rg -n '^function |^end$' "$F" | head -50
    echo "=== FUNC LENGTHS ===" && python3 -ISc "
import re, sys
lines = open(sys.argv[1]).readlines()
fn = None; fstart = 0; depth = 0
block_re = re.compile(r'^\s*(function |if |for |while |switch |begin\b)')
for i, line in enumerate(lines, 1):
    s = line.strip()
    if s.startswith('else'): continue
    if re.match(r'^function [a-z_]', line):
        fn = re.match(r'^function (\w+)', s).group(1)
        fstart = i; depth = 1; continue
    if not fn: continue
    if block_re.match(line): depth += 1
    elif s == 'end':
        depth -= 1
        if depth == 0:
            length = i - fstart + 1
            if length > 50:
                tag = '[>100]' if length > 100 else '[>50]'
                print(f'{fn:<40} L{fstart:<5} {length} lines {tag}')
            fn = None
" "$F" 2>&1
    echo "=== ARITY ===" && rg -n 'count \$argv|test .* -ne |test .* -lt ' "$F" | head -20
    echo "=== ATOMIC ===" && rg -n 'mv |install_file|chmod.*mv' "$F" | head -30
    echo "=== CHMOD MV ORDER ===" && { rg -Un 'chmod.*\nmv |chmod.*\nsudo mv' "$F" 2>/dev/null || awk '/chmod/{p=$0;next} p && /^ *(sudo )?mv /{print p"\n"$0;p=""} {p=""}' "$F"; } | head -10
    echo "=== LOCK ===" && rg -n 'mkdir.*lock|rmdir.*lock|kill -0|LOCK|flock|exec 9' "$F" | head -30
    echo "=== LOG ===" && rg -n '_log|_json_str|NDJSON|log_file|\.jsonl' "$F" | head -30
    echo "=== ARGPARSE TMP ===" && rg -n 'ry-argparse|_argparse_tmp' "$F" | head -10
    echo "=== ANTIPATTERNS ===" && rg -n 'cat .* \| |test .* = ""|head -1\b|tail -1\b' "$F" | head -20
    echo "=== REDIRECT BUG ===" && rg -n 'string trim.*<|string match.*<' "$F" | rg -v '#' | head -10
  ) > "$D/p8-9.txt" 2>&1; echo "GROUP_C1_EXIT=$?" >> "$D/p8-9.txt" ) &

  # ── GROUP C2: P10-11 magic numbers, docs, testing ──
  ( set -uo pipefail; (
    echo "=== MAGIC NUMS ===" && rg -n 'TEMP_|DISK_|BOOT_|MAX_|CACHE_|ROOT_AVAIL|NVME_LIFE' "$F" | head -30
    echo "=== DRY ===" && rg -n 'DRY|dry.run|--dry-run|\[DRY\]' "$F" | head -20
    echo "=== TEST_ALL ===" && rg -n 'do_test_all|verify.static|verify.runtime' "$F" | head -20
    echo "=== TEST_ALL BODY ===" && echo "NOTE: _ry_do_test_all removed v4.1.0 — expect 0 hits (regression check)"
    echo "=== TEST_ALL TMPFILES ===" && echo "NOTE: N/A since v4.1.0 removed _ry_do_test_all"
    echo "=== ISATTY ===" && rg -n 'isatty|test -t' "$F" | head -20
    echo "=== PRE-LOCK ===" && rg -n 'case install-file|case install|_acquire_lock' "$F" | head -20
    echo "=== PROGRESS_STEPS REMOVED v4.1.9 (regression — expect 0) ===" && rg -n 'PROGRESS_STEPS|PROGRESS_TOTAL' "$F" | head -3
    echo "=== PROGRESS CALLED (phase names from _progress call sites) ===" && rg -oP '^\s+_progress\s+\K[A-Z][a-zA-Z]+' "$F" | sort -u | head -30
    echo "=== COMPLETIONS REMOVED (v4.1.0) ===" && rg -n '_ry_do_completions|do_completions' "$F" | head -5
    echo "=== DISPATCH CASES (argparse-based v4.1.3) ===" && rg -n '_flag_verify_static|_flag_verify_runtime|_flag_check|_flag_install_file|_flag_help|_flag_version' "$F" | rg -v '#' | head -30
    echo "=== HELP FLAGS ===" && _py_extract "$F" "_ry_show_help" | rg -o '\-\-[a-z][-a-z]+' | rg -v '^--color$' | sort -u | head -30
    echo "=== UNREACHABLE ===" && { rg -Un '(return [0-9]+|exit [0-9]+)\n\s+[a-z_]' "$F" 2>/dev/null || awk '/^[[:space:]]*(return|exit) [0-9]+/{p=NR":"$0;next} p && /^[[:space:]]+[a-z_]/{print p"\n"NR":"$0;p=""} {p=""}' "$F"; } | rg -v 'end$|else|#|function' | head -10
    echo "=== OR RETURN ===" && rg -n 'or return [^0]' "$F" | head -10
    echo "=== DEST CROSSREF ===" && rg -o '"/(boot|etc)[^"]*"' "$F" 2>/dev/null | tr -d '"' | sort | head -30
    echo "=== PRINTF INJECTION ===" && rg -n "printf '.*\\\$" "$F" | rg -v '%s|#|%%' | head -10
    echo "=== UNQUOTED TEST ===" && rg -n 'test \$[a-z]' "$F" | rg -v '"' | head -20
  ) > "$D/p10-11.txt" 2>&1; echo "GROUP_C2_EXIT=$?" >> "$D/p10-11.txt" ) &

  # ── GROUP D: P14 v3.1.0–v5.0.1 specifics ──
  ( set -uo pipefail; (
    echo "=== KVER GLOBALS ===" && rg -n 'KVER|KVER_MAJOR|KVER_MINOR' "$F" | head -20
    echo "=== KVER GATE ORDERING ===" && rg -n '^function _ry_check_kernel_version|^set -g KVER_MAJOR' "$F" | head -5
    echo "=== _ntsync_state ===" && rg -n '_ntsync_state' "$F" | head -10
    echo "=== _verify_unit_syntax ===" && rg -n '_verify_unit_syntax' "$F" | head -10
    echo "=== _check_service_active_enabled REMOVED (regression — expect 0) ===" && rg -n '_check_service_active' "$F" | head -5
    echo "=== _find_pacnew_files REMOVED (regression — expect 0) ===" && rg -n '_find_pacnew' "$F" | head -5
    echo "=== _get_boot_time REMOVED (inlined v3.48.2 — regression — expect 0) ===" && rg -n '_get_boot_time' "$F" | head -5
    echo "=== EXISTENCE_ONLY ===" && rg -n 'existence.only' "$F" | head -10
    echo "=== MODULES-LOAD REGRESSION ===" && rg -n 'modules-load' "$F" | head -10
    echo "=== SP5100_TCO REGRESSION ===" && rg -ni 'sp5100' "$F" | rg -v '^[0-9]*:\s*#' | head -5
    echo "=== BTUSB REGRESSION ===" && rg -ni 'btusb|enable_autosuspend' "$F" | rg -v '^[0-9]*:\s*#' | head -10
    echo "=== PPD REGRESSION ===" && rg -n 'power-profiles-daemon' "$F" | head -10
    echo "=== MODPROBE_BLACKLIST ===" && rg -n 'MODPROBE_BLACKLIST' "$F" | head -10
    echo "=== HELPER BODIES ===" && rg -n '^function _ntsync_state|^function _verify_unit|^function _check_service|^function _find_pacnew|^function _get_boot_time|^function _gkeyfile_escape|^function _install_fstab' "$F" | head -15
    echo "=== UNAME INLINE ===" && rg -n 'uname -r' "$F" | rg -v 'KVER|#' | head -10
    echo "=== SYSCTL DEST ===" && rg -n 'sysctl.d' "$F" | head -10
    echo "=== SYSCTL CONTENT ===" && rg -n 'default_qdisc|tcp_congestion|tcp_fastopen|max_user_watches' "$F" | head -20
    echo "=== SYSCTL RELOAD ===" && rg -n 'sysctl --system' "$F" | head -10
    echo "=== SYSCTL VERIFY ===" && rg -n '_chk_grep.*/etc/sysctl' "$F" | head -10
    echo "=== SYSCTL RUNTIME ===" && rg -n '/proc/sys/' "$F" | head -10
    echo "=== INSTALL_HAD_ERRORS ===" && rg -n 'INSTALL_HAD_ERRORS' "$F" | head -20
    echo "=== CAT TERMINATOR ===" && rg -n 'cat --' "$F" | head -20
    echo "=== FUNCTION DESCRIPTIONS ===" && rg -c -- '--description' "$F"
    echo "=== FUNCTION COUNT ===" && rg -c '^function ' "$F"
    echo "=== VENDOR OVERLAP ===" && rg -n '70-cachyos-settings' "$F" | head -10
    echo "=== FOOTER RACE ===" && rg -n '_FOOTER_WRITTEN' "$F" | head -10
    echo "=== LOG ROTATION LOCK ===" && rg -n 'Log rotation' "$F" | head -10
    echo "=== LC_ALL SORT ===" && rg -n 'LC_ALL=C sort' "$F" | head -10
    echo "=== ORPHAN LOG ===" && rg -n 'LOG_FILE.*rm|rm.*LOG_FILE|install-.*jsonl' "$F" | head -20
    echo "=== EARLY EXIT LOG ===" && rg -n 'exit 2' "$F" | head -20
    echo "=== SIGPIPE HANDLER ===" && rg -n '_cleanup_pipe|SIGPIPE|on-signal PIPE|exit 141' "$F" | head -10
    echo "=== VALIDATE KERNEL PARAMS ===" && rg -n '_validate_kernel_params|param_config_map|/proc/config.gz' "$F" | head -10
    echo "=== BOOT SANITY ===" && rg -n '_preflight_boot_sanity|vmlinuz|initramfs.*non.zero' "$F" | head -10
    echo "=== DEAD GLOBALS ===" && rg -n 'MODULES_LOAD|_LOCK_METHOD|_GPU_BUSY_PCT' "$F" | head -5
    echo "=== V340 REMOVED ===" && rg -n 'do_clean\b|_find_steam_root|--no-color\b|--no-log\b' "$F" | rg -v '#|echo|_msg|_warn|_err|_fail|_info|printf|_log' | head -10
    echo "=== V350 BACKUP REMOVED ===" && rg -n '_backup_|BACKUP_BASE|BACKUP_DIR|MAX_BACKUPS|do_rollback' "$F" | head -10
    echo "=== DOTFILE CLEAN ===" && rg -n 'mindepth 1.*exec|find.*mindepth.*rm' "$F" | head -10
    echo "=== LOG TIMESTAMP ===" && rg -n 'date .+%[YsmH]|set -l _ts ' "$F" | head -10
    echo "=== STRING MATCH SEPARATOR ===" && rg -n "string match.*' -- '" "$F" | head -10
    echo "=== SECTION HEADERS ===" && rg -c '^# ── ' "$F"
    echo "=== SECTION HEADER LINES ===" && rg -n '^# ── ' "$F" | head -25
    echo "=== FUNCTION COMMENT COVERAGE ===" && python3 -ISc "
import re, sys
lines = open(sys.argv[1]).readlines()
n = 0; cmt = 0
for i, line in enumerate(lines, 1):
    if line.strip().startswith('#'): cmt = i
    if re.match(r'^function ', line):
        if i - cmt <= 2: n += 1
fcount = sum(1 for l in lines if re.match(r'^function ', l))
print(f'{n}/{fcount} functions with nearby comment')
" "$F" 2>&1
    echo "=== COMMAND PREFIX ===" && python3 -ISc "
import subprocess, sys; F = sys.argv[1]
for cmd in 'rm mv cp chmod chown mkdir'.split():
    r = subprocess.run(['rg', '-c', f'command {cmd}', F], capture_output=True, text=True)
    print(f'{cmd}: {r.stdout.strip() or \"0\"}')
" "$F" 2>&1
    echo "=== ARGUMENT NAMES ===" && rg -n -- '--argument-names' "$F" | head -20
    echo "=== IN-FUNCTION GLOBALS ===" && python3 -ISc "
import re, sys
lines = open(sys.argv[1]).readlines()
fn = None; depth = 0; count = 0
block_re = re.compile(r'^\s*(function |if |for |while |switch |begin\b)')
for line in lines:
    s = line.strip()
    if s.startswith('else'): continue
    if re.match(r'^function [a-z_]', line):
        fn = re.match(r'^function (\w+)', s).group(1); depth = 1; continue
    if not fn: continue
    if block_re.match(line): depth += 1
    elif s == 'end':
        depth -= 1
        if depth == 0: fn = None; continue
    if fn and re.search(r'set -g[ax]? ', line): count += 1
print(f'{count} in-function set -g')
" "$F" 2>&1
    echo "=== SHA256 VERIFY ===" && rg -n 'sha256sum' "$F" | head -10
    echo "=== WIFI CRED SCOPE ===" && rg -n 'WIFI_SSID|WIFI_PASS|wifi_pass|wifi_ssid' "$F" | head -20
    echo "=== LOG ROTATION MODE ===" && rg -n 'MAX_LOGS' "$F" | head -10
    echo "=== LOG SECTION MARKERS ===" && rg -n '=== .* START ===|=== .* END ===' "$F" | head -20
    echo "=== BLOCK COMMENTS ===" && rg -c '^# ── .*──$' "$F"
    echo "=== STDOUT LEAKS ===" && rg -n 'printf.*\\n|echo$' "$F" | rg -v '>&2|>>|_log|_msg|_ok|_fail|_warn|_err|_info|_dry|_echo|/dev/null|#|get_file_content|show_help|_ntsync_state|_verify_summary|stdout_tmp' | head -20
    echo "=== RUN SECURITY ===" && rg -n 'SECURITY INVARIANT' "$F" | head -5
    echo "=== BARE SET COUNT ===" && rg -c '^\s+set [a-z_]' "$F"
    echo "=== SET -L COUNT ===" && rg -c 'set -l ' "$F"
    echo "=== MODE DISPATCH ===" && rg -n 'set.*MODE ' "$F" | rg -v '#|set -l MODE|argparse' | head -20
    echo "=== EXIT_CODE DISPATCH ===" && rg -n 'set.*exit_code ' "$F" | rg -v '#|set -l exit_code' | head -20
    echo "=== MODE_LABEL DISPATCH ===" && rg -n 'set.*mode_label ' "$F" | head -10
    echo "=== LOGS REMOVAL REGRESSION ===" && rg -n 'do_logs\b|_logs_file_ops|_logs_journal|_logs_list' "$F" | head -10
    echo "=== LOGS RETURN STATUS ===" && rg -n 'return \$status' "$F" | head -10
    echo "=== LOGS FUZZY MATCH ===" && rg -n 'fuzzy|Did you mean|_t_prefix|proximity' "$F" | head -10
    echo "=== PATH BASENAME REGRESSION ===" && rg -n 'path basename' "$F" | head -5
    echo "=== BARE SUDO NMCLI ===" && rg -n 'sudo nmcli' "$F" | rg -v '_run|#' | head -10
    echo "=== V351 HAS_LOCK REGRESSION ===" && rg -n '_has_lock' "$F" | head -5
    echo "=== V351 LOG GUARD ===" && rg -n 'test -f.*LOG_FILE.*return 0' "$F" | head -5
    echo "=== FLOCK RECLAIM ===" && rg -n 'flock' "$F" | head -10
    echo "=== ROOT UUID CACHE ===" && rg -n '_ROOT_UUID' "$F" | head -10
    echo "=== KCONFIG CACHE ===" && rg -n '_kconfig_cache|_KCONFIG_DATA' "$F" | head -10
    echo "=== FISH VERSION GATE ===" && rg -n 'fish_major|fish_minor|fish 3\.' "$F" | head -10
    echo "=== PREGENERATE ===" && rg -n '_pregenerate_content_files' "$F" | head -10
    echo "=== PARALLEL PATTERNS ===" && rg -n 'wait \$|last_pid|&$' "$F" | head -20
    echo "=== ENSURE SUDO CACHED ===" && rg -n '_ensure_sudo_cached' "$F" | head -10
    echo "=== _parse_systemctl_show REMOVED (regression — expect 0) ===" && rg -n '_parse_systemctl_show' "$F" | head -5
    echo "=== TRACKED TMPFILES ===" && rg -n '_TRACKED_TMPFILES' "$F" | head -20
    echo "=== FUNCTIONS SERIALIZATION ===" && rg -n 'needed_funcs|funcs_file' "$F" | head -10
    echo "=== STRING JOIN0 ===" && rg -n 'string join0' "$F" | head -10
    echo "=== PARALLEL CHILD TMPDIR ===" && rg -n 'test_dir|val_dir|diff_batch_dir' "$F" | head -20
    echo "=== PARSE SYSTEMCTL BODY (REMOVED — sentinel extractor for regression) ===" && rg -n '_parse_systemctl_show' "$F" | head -3
    echo "=== V3713 FSTAB HELPER ===" && rg -n '_install_fstab_opts' "$F" | head -10
    echo "=== V3713 GKEYFILE ESCAPE REMOVED (regression — expect 0) ===" && rg -n '_gkeyfile_escape' "$F" | head -5
    echo "=== V3713 VERIFY SUMMARY ===" && _py_extract "$F" "_verify_summary" | rg 'VERIFY_MODE|set -l' | head -10
    echo "=== V3713 DEAD FUNCS ===" && rg -n '_read_installed|validate_systemd_unit|validate_fish_script|passthrough' "$F" | head -5
    echo "=== V3713 KCONFIG CLEANUP ===" && { _dca=$(_py_extract "$F" "_do_cleanup"); printf '%s\n' "$_dca" | rg '_KCONFIG_DATA' | head -5; }
    echo "=== V3713 EXIT_USAGE ===" && rg -n 'exit 2\b' "$F" | head -10
    echo "=== V3713 CHECK JOB4 FILE ===" && rg -n 'exp_svcs|mask_units' "$F" | head -10
    echo "=== V3713 TRACKED TMPDIR CLEANUP ===" && printf '%s\n' "$_dca" | rg 'test -d|rm -rf|test -f' | head -10
    echo "=== V371 KERNEL GATE ===" && _py_extract "$F" "_ry_check_kernel_version" | rg 'return' | head -5
    echo "=== V371 NPROC GUARD ===" && rg -n 'nproc_val|nproc.*lt 8|sequentially' "$F" | head -10
    echo "=== V371 SSH USER VERIFY ===" && rg -n 'systemd-analyze.*user.*verify|--user verify' "$F" | head -10
    echo "=== V371 MANIFEST GATE ===" && rg -n 'MANIFEST_SKIP|EXIT_BOOT_CRIT.*manifest' "$F" | head -5
    echo "=== V371 SYU ORDER ===" && rg -n 'SYSTEM_UPGRADED' "$F" | head -10
    echo "=== V371 FLOCK ROTATION ===" && rg -n 'flock.*xargs|flock.*_log_base' "$F" | head -5
    echo "=== V371 BATCH FALLBACK ===" && rg -n 'Batch enable failed|retrying individually' "$F" | head -5
    echo "=== V371 DEAD IDX ===" && _py_extract "$F" "_ry_do_check" | rg 'idx 0' | head -5
    echo "=== V3723 FISH-C INJECTION ===" && rg -n 'fish -c "' "$F" | head -10
    echo "=== V3723 FISH-C SAFE ===" && rg -c "fish -c '" "$F"
    echo "=== V3723 BRACE EXPANSION ===" && rg -n '{\$' "$F" | head -10
    echo "=== V3723 ECHO STATUS ===" && rg -n 'echo \$status' "$F" | rg -v '#' | head -10
    echo "=== V3723 TEST DEPRECATED ===" && rg -n 'test [^;]+ -[oa] [^;]+' "$F" | head -10
    echo "=== V3723 WAIT GUARD ===" && rg -n 'wait \$' "$F" | head -20
    echo "=== V3723 WAIT COUNT GUARD ===" && rg -n 'count.*pids.*wait|test.*count.*wait' "$F" | head -10
    echo "=== V3723 WORKER CAP ===" && rg -n 'nproc|num_workers|min.*nproc' "$F" | head -20
    echo "=== V3723 POST-WRITE SYMLINK ===" && rg -n 'test -L.*tmpfile' "$F" | head -10
    echo "=== V3723 INSTALL-FILE RELPATH ===" && rg -n 'install-file requires absolute' "$F" | head -5
    echo "=== V3723 DIFF BATCH ===" && rg -n 'diff_pids|diff.*batch' "$F" | head -10
    echo "=== V3723 AND-OR STYLE ===" && rg -c '; and |; or ' "$F"
    echo "=== V3723 BASH AND-OR ===" && rg -n '&&|\|\|' "$F" | rg -v '#|awk|/bin/sh|ExecStart|printf.*&&|echo.*&&' | head -20
    echo "=== V3723 LOCK DOUBLE VERIFY ===" && rg -n 'verify_pid2|sleep 0.1' "$F" | head -10
    echo "=== V3723 MKTEMP DEGRADED ===" && rg -n '_MKTEMP_DEGRADED_WARNED' "$F" | head -5
    echo "=== V3723 RUN STDOUT CAP ===" && rg -n 'STDOUT_TRUNCATED' "$F" | head -5
    echo "=== INLINE COMMENT DENSITY ===" && python3 -ISc "
import re, sys
lines = open(sys.argv[1]).readlines()
fn = None; depth = 0; fstart = 0; results = []
block_re = re.compile(r'^\s*(function |if |for |while |switch |begin\b)')
for i, line in enumerate(lines, 1):
    s = line.strip()
    if s.startswith('else'): continue
    if re.match(r'^function [a-z_]', line):
        fn = re.match(r'^function (\w+)', s).group(1)
        fstart = i; depth = 1; continue
    if not fn: continue
    if block_re.match(line): depth += 1
    elif s == 'end':
        depth -= 1
        if depth == 0:
            length = i - fstart + 1
            if length > 50:
                body = lines[fstart:i-1]
                inline = sum(1 for b in body if b.strip().startswith('#') and not b.strip().startswith('#!'))
                results.append((fn, length, inline))
            fn = None
for fn, l, c in sorted(results, key=lambda x: x[2]):
    print(f'{fn:<45} {l:>4}L  {c} inline comments')
print(f'Total: {len(results)} functions >50 lines')
" "$F" 2>&1
    echo "=== SCOPE SHADOW DETECTOR ===" && python3 -ISc "
import re, sys
lines = open(sys.argv[1]).readlines()
func = None; depth = 0; scope_stack = []; in_string = False; issues = []
for i, line in enumerate(lines, 1):
    s = line.strip()
    if in_string:
        if re.search(r\"(?<!\\\\)'$\", s): in_string = False
        continue
    if re.search(r\"printf '%s\\\\n' '\", s):
        after = s.split(\"printf '%s\\\\n' '\", 1)[1] if \"printf '%s\\\\n' '\" in s else ''
        if not re.search(r\"(?<!\\\\)'$\", after): in_string = True; continue
        continue
    if s.startswith('#'): continue
    if re.match(r'^function [a-z_]', line) and not line.startswith(' '):
        if depth == 0: func = re.match(r'^function (\w+)', s).group(1); depth = 1; scope_stack = [{}]
        else: depth += 1; scope_stack.append({})
        continue
    if not func: continue
    opened = False
    for kw in ['if ','for ','while ','switch ','function ']:
        if s.startswith(kw): depth += 1; scope_stack.append({}); opened = True; break
    if not opened and (s == 'begin' or s.startswith('begin ') or s.startswith('begin;')):
        depth += 1; scope_stack.append({}); opened = True
    if s == 'end':
        if depth > 1:
            if scope_stack: scope_stack.pop()
            depth -= 1
        elif depth == 1: scope_stack = []; func = None; depth = 0
        continue
    if opened: continue
    m = re.match(r'\s+set -l ([a-zA-Z_]\w*)', line)
    if m:
        var = m.group(1); cur = len(scope_stack)-1
        for si in range(cur):
            if var in scope_stack[si]:
                issues.append(f'L{i}: {func}() shadows L{scope_stack[si][var]} var \"{var}\"')
                break
        if 0 <= cur < len(scope_stack) and var not in scope_stack[cur]: scope_stack[cur][var] = i
for x in issues: print(x)
print(f'Total: {len(issues)} scope shadow(s)')
" "$F" 2>&1 | head -30
    echo "=== FISH4 QMARK GLOB ===" && rg -n '\?' "$F" | rg -v '#|string|rg|grep|awk|sed|printf|echo' | head -10
    echo "=== FISH4 BIND -K ===" && rg -n 'bind -k' "$F" | head -10
    echo "=== FISH4 COMMANDLINE TOKENIZE ===" && rg -n 'commandline.*--tokenize|commandline.* -o ' "$F" | head -10
    echo "=== FISH4 PERCENT SELF ===" && rg -n '%self' "$F" | head -10
    echo "=== FISH4 TEST -N UNQUOTED ===" && rg -n 'test -n \$[a-z]' "$F" | rg -v '"' | head -10
    echo "=== FISH4 SET -U FISH_COLOR ===" && rg -n 'set -U fish_color' "$F" | head -10
    echo "=== FISH4 BRACE COMPOUND ===" && rg -n '^\s*{$|^\s*}$' "$F" | head -10
    echo "=== V3100 FSTAB ===" && rg -n '_install_fstab_opts|fstab|noatime|lazytime' "$F" | head -20
    echo "=== V3100 EMIT STEP TIME REMOVED (regression — expect 0) ===" && rg -n '_emit_step_time' "$F" | head -5
    echo "=== V3100 SYSCTL VALUES ===" && rg -n 'SYSCTL_VALUES' "$F" | head -10
    echo "=== V3100 IRQBALANCE ===" && rg -n 'irqbalance|threadirqs' "$F" | head -10
    echo "=== V3100 AMDGPU PERF REMOVED ===" && rg -n 'amdgpu-performance\b|amdgpu_performance' "$F" | head -5
    echo "=== V3100 WDAT REMOVED ===" && rg -n 'wdat_wdt' "$F" | head -5
    echo "=== V3100 EXIT PREFLIGHT ===" && rg -n 'EXIT_PREFLIGHT' "$F" | head -10
    echo "=== V3100 MANIFEST DIFF FIX ===" && rg -n '_manifest_write.*diff|FIX.*manifest' "$F" | head -5
    echo "=== V3100 VERIFY MODE ===" && rg -n 'VERIFY_MODE' "$F" | head -10
    echo "=== V3100 SUDO -N ===" && rg -n 'sudo -n' "$F" | head -20
    echo "=== V3100 IPV6 METHOD ===" && rg -n 'method=auto|method=disabled|ipv6' "$F" | head -10
    echo "=== V3100 SSID FORBIDDEN ===" && rg -n 'SSID.*forbidden|forbidden.*char|_SSID_FORBIDDEN' "$F" | head -5
    echo "=== V3100 GREP SEPARATOR ===" && rg -n "grep -- " "$F" | head -20
    echo "=== V3100 TMPDIR GUARD ===" && rg -n 'test -n.*TMPDIR|set -q TMPDIR' "$F" | head -5
    echo "=== V3100 STRING COLLECT ===" && rg -n 'string collect' "$F" | head -10
    echo "=== V3100 ZRAM CHECK ===" && rg -n 'zram|ZRAM' "$F" | head -10
    echo "=== V3100 LVM MASK ===" && rg -n 'lvm|LVM|lvs ' "$F" | head -10
    echo "=== V3450 PREEMPT ===" && rg -n 'preempt|Dynamic Preempt' "$F" | head -10
    echo "=== V3450 PAGE LOCK REMOVED ===" && rg -n 'page_lock_unfairness' "$F" | head -5
    echo "=== V3450 NETDEV BACKLOG ===" && rg -n 'netdev_max_backlog' "$F" | head -5
    echo "=== V3450 SOMAXCONN ===" && rg -n 'somaxconn' "$F" | head -5
    echo "=== V3450 SYSCTL PRIORITY ===" && rg -n 'priority 99|priority.*override' "$F" | head -5
    echo "=== V3460 SPLIT M1 ===" && rg -n 'string split -m1' "$F" | head -10
    echo "=== V3460 NOATIME DEDUP ===" && rg -n 'noatime.*already|already.*noatime|match.*noatime' "$F" | head -10
    echo "=== V3460 USBHID REMOVED ===" && rg -n 'usbhid' "$F" | head -5
    echo "=== V3460 COREDUMP VERIFY ===" && rg -n 'Storage=none|ProcessSizeMax|coredump.*verify|verify.*coredump' "$F" | head -10
    # ── v3.47.0–v4.1.8 additions ──
    echo "=== V3480 LINT REMOVED ===" && rg -n '_ry_do_lint|do_lint\b|--lint\b' "$F" | rg -v '#|_err|_warn|_fail|deprecated|removed' | head -10
    echo "=== V410 TEST_ALL REMOVED ===" && rg -n '_ry_do_test_all|do_test_all\b|--test-all\b' "$F" | rg -v '#|removed|deprecated' | head -10
    echo "=== V410 COMPLETIONS REMOVED ===" && rg -n '_ry_do_completions|--completions\b' "$F" | rg -v '#|removed|deprecated' | head -10
    echo "=== LOGS REMOVED ===" && rg -n 'do_logs\b|_logs_file_ops|_logs_journal|_logs_list|--logs\b' "$F" | rg -v '#|removed|deprecated' | head -10
    echo "=== V3500 PROFILE SYSTEM RETIRED (v4.5.0) ===" && rg -n '_load_profile|_validate_profile|_ry_profile_|default-profile' "$F" | rg -v '^[0-9]+:\s*#' | head -10
    echo "=== V3500 PROFILE_NAME/DESC INLINED (v4.5.0) ===" && rg -n '^set -g PROFILE_NAME|^set -g PROFILE_DESC' "$F" | head -5
    echo "=== V3500 MANIFEST RETIRED (v4.5.0) ===" && rg -n '_manifest_check_orphans|_manifest_write' "$F" | head -5
    echo "=== V3500 INIT_RUNTIME (v4.5.0 replaces _load_profile) ===" && rg -n '_init_runtime' "$F" | head -5
    echo "=== V3501 FLOCK RECLAIM E5 ===" && rg -n 'flock -n -E 5' "$F" | head -5
    echo "=== V3482 GET_BOOT_TIME INLINED v3.48.2 / REMOVED (regression — expect 0) ===" && rg -n '_get_boot_time' "$F" | head -5
    echo "=== V3482 PROGRESS_SKIP MERGED ===" && rg -n '_progress_skip' "$F" | rg -v '#|merged' | head -5
    echo "=== V3482 BOOT_WIPE_MARKER ===" && rg -n 'BOOT_WIPE_MARKER|RY_INSTALL_CONFIRM_BOOT_WIPE|boot-wipe-acknowledged' "$F" | head -15
    echo "=== V3482 LINT MODE REMOVED ===" && rg -n 'do_lint|--lint' "$F" | rg -v '#|removed|deprecated|--no-lint' | head -5
    echo "=== V3482 RY_EXIT SOURCE SAFE ===" && rg -n '_ry_exit|_RY_INSTALL_BAILING|_RY_INSTALL_SOURCED|_RY_INSTALL_LAST_EXIT' "$F" | head -25
    echo "=== V3482 NAMESPACE CLEANUP ===" && rg -n '_ry_namespace_cleanup|_preserve' "$F" | head -10
    echo "=== V3485 RY_RUN_TIMEOUT ===" && rg -n 'RY_RUN_TIMEOUT' "$F" | head -10
    echo "=== V3485 RUN STDOUT STDERR ===" && rg -n 'mktemp -d|--kill-after' "$F" | head -10
    echo "=== V3516 FSTAB POST WRITE ===" && rg -n 'findmnt --verify|findmnt.*verify' "$F" | head -10
    echo "=== V3513 DETECT_LVM ===" && rg -n '_detect_lvm\b' "$F" | head -5
    echo "=== V3513 KILL KEEPALIVE ESCALATION ===" && rg -n '_kill_sudo_keepalive' "$F" | head -5
    echo "=== V3517 BOOT CASCADE ===" && rg -n '_install_rebuild_boot' "$F" | head -10
    echo "=== V400 KERNEL PARAMS 15 ===" && rg -c '^\s+(iommu=pt|amd_pstate=active|amdgpu\.cwsr_enable|amdgpu\.ppfeaturemask|loglevel=3|module_blacklist=pcspkr|nowatchdog|pcie_aspm\.policy|quiet|rd\.systemd\.show_status|rd\.udev\.log_level|split_lock_detect|tsc=reliable|usbcore\.autosuspend|zswap\.enabled)' "$F"
    echo "=== V400 SYSCTL 19 ===" && rg -n 'net\.core\.default_qdisc|net\.core\.netdev_max_backlog|tcp_congestion_control|tcp_fastopen|tcp_notsent_lowat|vm\.max_map_count|fs\.protected_fifos|vm\.compaction_proactiveness|net\.core\.busy_read|net\.core\.netdev_budget' "$F" | head -20
    echo "=== V400 NFTABLES ===" && rg -n 'nftables' "$F" | head -10
    echo "=== V400 AMD_PSTATE ===" && rg -n 'amd_pstate=active' "$F" | head -5
    echo "=== V400 RADV EXPERIMENTAL ===" && rg -n 'RADV_EXPERIMENTAL|RADV_PERFTEST' "$F" | head -10
    echo "=== V400 SOMAXCONN REMOVED ===" && rg -n 'somaxconn' "$F" | rg -v '#|removed' | head -5
    echo "=== V400 INOTIFY REMOVED ===" && rg -n 'inotify\.max_user_watches' "$F" | rg -v '#|removed' | head -5
    echo "=== V400 IRQBALANCE REMOVED FROM MASK ===" && rg -n 'irqbalance' "$F" | head -5
    echo "=== V400 COREDUMP SOCKET MASKED ===" && rg -n 'systemd-coredump\.socket' "$F" | head -5
    echo "=== V400 RESOLVED MDNS ===" && rg -n 'RESOLVED_MDNS|MulticastDNS' "$F" | head -10
    echo "=== V401 FSTAB SUDO CHMOD ===" && rg -n '\-\-reference=/etc/fstab|sudo chmod.*reference' "$F" | head -5
    echo "=== V401 LOG UMASK 0077 ===" && rg -n 'umask 0077|chmod 700' "$F" | head -10
    echo "=== V403 HASH PIPESTATUS ===" && rg -n 'test \$_ps\[1\] -eq 0; and test \$_ps\[2\] -eq 0|test \$_ps\[1\] -ne 0; or test \$_ps\[2\] -ne 0' "$F" | head -10
    echo "=== V403 CONTENT_HASH REMOVED (regression — expect 0) ===" && rg -n '_content_hash' "$F" | head -5
    echo "=== V410 DISPATCHER ARGPARSE ===" && rg -nA2 'argparse --name=ry-install' "$F" | head -15
    echo "=== V410 ARGPARSE EXCLUSIVE ===" && rg -n '\-\-exclusive=' "$F" | head -5
    echo "=== V410 DEPRECATED FLAGS ===" && rg -n '_flag_all\b.*no longer|_flag_dry_run.*removed|_flag_diff.*removed|_flag_fix.*removed' "$F" | head -10
    echo "=== V411 SIGNAL CASE ===" && rg -n 'case HUP SIGHUP|case INT SIGINT|case QUIT SIGQUIT|case TERM SIGTERM' "$F" | head -5
    echo "=== V411 SIGNAL EXIT CODES ===" && rg -n '_sig_exit 129|_sig_exit 130|_sig_exit 131|_sig_exit 143' "$F" | head -10
    echo "=== V412 SUDO TAG REGEX ===" && rg -n '!PASSWD\\b|!SETENV\\b|NOEXEC\\b|LOG_OUTPUT\\b' "$F" | head -10
    echo "=== V412 SYU GATE ===" && rg -n 'RY_INSTALL_CONFIRM_SYSTEM_UPGRADE' "$F" | head -10
    echo "=== V412 MANIFEST DECOUPLED ===" && rg -n 'MANIFEST_WRITE_FAILED' "$F" | head -5
    echo "=== V412 BOOT_CRIT SDBOOT UPDATE ===" && rg -n 'sdboot-manage update' "$F" | head -10
    echo "=== V412 FLOCK SOFT DEP ===" && rg -n 'flock.*SOFT|flock.*fallback' "$F" | head -5
    echo "=== V413 TMPFILE_KEY ===" && rg -n '_tmpfile_key' "$F" | head -15
    echo "=== V413 WRITE_FOOTER NO_FINISHED ===" && rg -n '"finished":' "$F" | head -5
    echo "=== V413 IMPLICIT_SVCS DERIVED ===" && rg -n 'implicit_svcs' "$F" | head -5
    echo "=== V413 VAL_DIR CONTENT_DIR PRUNE ===" && rg -n 'val_dir.*TRACKED|content_dir.*TRACKED|set --erase _TRACKED' "$F" | head -10
    echo "=== V413 BLS EXACT SEGMENT ===" && rg -n 'kernel\.\.custom|path-traversal|exact-segment' "$F" | head -5
    echo "=== V413 EXIT TRIPLE FORM ===" && rg -c '_ry_exit \$[A-Z_]+; and return \$[A-Z_]+; or return \$[A-Z_]+' "$F"
    echo "=== V413 DISPATCHER EXCLUSIVE ===" && rg -n '\-\-exclusive=verify-static,verify-runtime,check,install-file' "$F" | head -3
    echo "=== MANAGED_FILE_COUNT (runtime-derived since v4.5.4) ===" && rg -n '_RY_MANAGED_FILE_COUNT' "$F" | head -5
    echo "=== MANAGED_CASE_COUNT REMOVED (regression — expect 0) ===" && rg -n '_RY_MANAGED_CASE_COUNT' "$F" | head -3
    # ── v4.1.4 additions ──
    echo "=== V414 TMPFILE_KEY IMPL ===" && _py_extract "$F" "_tmpfile_key" | head -5
    echo "=== V414 TMPFILE_KEY SHA RETIRED ===" && _py_extract "$F" "_tmpfile_key" | rg 'sha256sum|string sub --length 8' | head -5
    echo "=== V414 TMPFILE_KEY SITES ===" && rg -c '_tmpfile_key' "$F"
    echo "=== V414 PROFILE GUARD RETIRED (v4.5.0) ===" && rg -n 'Profile destination key collision|Profile destination duplicated|both map to tmpfile key|_seen_keys' "$F" | head -5
    echo "=== V414 PHASE RCS REMOVED ===" && rg -n '_phase_rcs|_rc_config|_rc_units|_rc_env|_rc_sudoers|_rc_fish' "$F" | head -5
    echo "=== V414 HASH RCS REMOVED ===" && rg -n 'hash_rcs|_any_hash_worker_timeout' "$F" | head -5
    echo "=== V414 TIMEOUT SENTINEL REMOVED ===" && rg -n 'contains -- .*124 137 143|_phase_rc.*124' "$F" | head -5
    echo "=== V414 SVC_DSTS EMPTY GUARD ===" && rg -n 'count .svc_dsts. -eq 0|test .count .svc_dsts..* -eq 0' "$F" | head -5
    echo "=== V414 KEEPALIVE PKILL ===" && rg -n 'pkill -TERM -P|pkill -KILL -P' "$F" | head -10
    echo "=== V414 DETECT_LVM TIMEOUT ===" && rg -n 'timeout 10 sudo -n pvs|timeout 5 sudo -n pvs' "$F" | head -5
    echo "=== V414 NETWORK CONNECT TIMEOUT 3 ===" && rg -n '\-\-connect-timeout 3 --max-time 5' "$F" | head -5
    echo "=== V414 NEWSFEED CONNECT TIMEOUT 2 ===" && rg -n '\-\-connect-timeout 2 --max-time 5' "$F" | head -5
    echo "=== V414 RUN STDERR FISH TRIM ===" && rg -n 'string trim --left' "$F" | head -10
    echo "=== V414 RUN STDERR SED RETIRED ===" && rg -n "command sed 's/\^ \*//'" "$F" | head -5
    echo "=== V414 BOOT_TIME LC_ALL ===" && rg -n 'LC_ALL=C printf "%.0f"' "$F" | head -5
    # ── v4.1.5 additions (profile guard split — RETIRED with profile system v4.5.0) ──
    echo "=== V415 PROFILE GUARD RETIRED (v4.5.0) ===" && rg -n 'Profile destination duplicated|both map to tmpfile key|Profile destination key collision' "$F" | head -5
    # ── v4.1.6 additions ──
    echo "=== V416 JSONL REDIRECT SITES ===" && rg -n '>>"\$LOG_FILE" 2>/dev/null' "$F" | head -10
    echo "=== V416 JSONL REDIRECT COUNT ===" && rg -c '>>"\$LOG_FILE" 2>/dev/null' "$F"
    echo "=== V416 BARE JSONL APPEND ===" && rg -n '>>"\$LOG_FILE"$' "$F" | head -5
    echo "=== V416 LOG GUARD TOCTOU ===" && rg -n 'test -f "\$LOG_FILE"; or return 0' "$F" | head -5
    echo "=== V416 JSONL WRITER FUNCTIONS ===" && rg -n '^function _write_footer|^function _json_str|^function _log |^function _msg |^function _emit_step_time' "$F" | head -10
    # ── v4.1.8 additions ──
    echo "=== V418 SYNC_INFO DROPPED ===" && rg -n 'Synchronizing package databases' "$F" | head -5
    echo "=== V418 PHASE4 XREF ===" && rg -n 'PKGS_DEL removal: phase 4' "$F" | head -5
    echo "=== V418 PIPELINE PHASE COMMENTS ===" && rg -n '^# Pipeline phase [0-9]' "$F" | head -10
    echo "=== V418 HELP DASH DASH REJECT ===" && rg -n 'positional args after .--. are rejected' "$F" | head -5
    echo "=== V418 HELP NO_COLOR ===" && rg -n 'NO_COLOR=1.*Suppress ANSI color' "$F" | head -5
    echo "=== V418 HELP ENV BODY ===" && _py_extract "$F" "_ry_show_help" | rg '^  (NO_COLOR|RY_RUN_TIMEOUT|RY_INSTALL_CONFIRM)' | head -10
    echo "=== V418 FSTAB TMPFSTAB DEREGISTER ===" && rg -n 'Deregister deleted tmpfstab from cleanup list' "$F" | head -5
    echo "=== V418 FSTAB STRING_MATCH -V ===" && rg -n 'string match -v -- "\$tmpfstab" \$_TRACKED_TMPFILES' "$F" | head -5
    echo "=== V418 FSTAB ATOMIC RM_TMP COUNT ===" && _py_extract "$F" "_fstab_atomic_replace" | rg -c '_rm_tmp "\$tmpfstab" true'
    echo "=== V418 FSTAB ATOMIC TRACK/UNTRACK COUNT ===" && _py_extract "$F" "_fstab_atomic_replace" | rg -c '_track_tmpfile "\$tmpfstab"|_untrack_tmpfile "\$tmpfstab"'
    echo "=== V418 BOOT_SANITY PRINT0 SITES ===" && _py_extract "$F" "_preflight_boot_sanity" | rg 'print0.*string split0' | head -10
    echo "=== V418 BOOT_SANITY PRINT0 COUNT ===" && _py_extract "$F" "_preflight_boot_sanity" | rg -c 'print0.*string split0'
    echo "=== V418 TOTAL PRINT0 STRING_SPLIT0 ===" && rg -n '\-print0 [^|]*\| string split0' "$F" | head -10
    echo "=== V418 TOTAL PRINT0 STRING_SPLIT0 COUNT ===" && rg -c '\-print0 [^|]*\| string split0' "$F"
    # ── v4.5.25 → v5.0.1 additions ──
    echo "=== V4525 POST_HOOKS TABLE ===" && rg -n '^set -g _RY_POST_HOOKS' "$F" | head -3
    echo "=== V4525 POST_HOOK_FOR_TARGET ===" && rg -n '^function _post_hook_for_target' "$F"
    echo "=== V4525 NAMESPACE_CLEANUP BAIL ===" && rg -n '_ry_namespace_cleanup bail' "$F" | head -10
    echo "=== V4525 KEEPALIVE NO-CONFIG ===" && rg -n 'fish --no-config -c' "$F" | head -5
    echo "=== V4525 DEPLOYED_SERVICES ===" && rg -n '_RY_DEPLOYED_SERVICES' "$F" | head -10
    echo "=== V4527 RUN DASH-PREFIX REJECT ===" && rg -n "BUG: _run called with dash-prefixed argv" "$F" | head -5
    echo "=== V4527 CHK_GREP -wF ===" && _py_extract "$F" "_chk_grep" | rg -c 'grep -wF'
    echo "=== V4527 VERIFY_GEN_FAIL ===" && rg -n 'VERIFY_GEN_FAIL' "$F" | head -5
    echo "=== V4527 RESOLVE_ESP /BOOT FALLBACK ===" && rg -n '^function _resolve_esp' "$F"
    echo "=== V4527 SYSCTL GENERATOR rc=13 ===" && rg -n 'return \$EXIT_GEN_SYSCTL' "$F" | head -5
    echo "=== V4528 INSTALL_FSTAB_OPTS SPLIT ===" && rg -n '^function _install_fstab_opts' "$F"
    echo "=== V4528 PREFLIGHT_BOOT_SANITY SPLIT ===" && rg -oP '^function \K_pbs_\w+' "$F" | sort
    echo "=== V4528 RUN SPLIT ===" && rg -oP '^function \K_run_\w+' "$F" | sort
    echo "=== V4529 INLINE AND-OR (X; and Y density) ===" && rg -c '; and ' "$F"
    echo "=== V4531 VSS HELPERS ===" && rg -oP '^function \K_vss_\w+' "$F" | sort
    echo "=== V4531 VRKG HELPERS ===" && rg -oP '^function \K_vrkg_\w+' "$F" | sort
    echo "=== V4531 ACQUIRE_LOCK SPLIT ===" && rg -n '^function _acquire_lock_fresh|^function _reclaim_stale_lock' "$F"
    echo "=== V4531 MSG HELPERS ===" && rg -n '^function _msg_print|^function _msg_nocount|^function _fail_silent' "$F"
    echo "=== V4532 ERR_LOUD ===" && rg -n '^function _err_loud' "$F"
    echo "=== V4533 REDACT_ARGV ===" && rg -n '^function _redact_argv_elements|^function _run_redact_argv' "$F"
    echo "=== V4533 INSTALLED_BYTES ===" && rg -n '^function _installed_bytes' "$F"
    echo "=== V4533 PROFILE_USES_WIFI_BACKEND ===" && rg -n '_PROFILE_USES_WIFI_BACKEND|_PROFILE_USES_NM' "$F" | head -10
    echo "=== V4533 NAMED CONSTS ===" && rg -n '_RY_HOME_DIR|_RY_RUN_TIMEOUT_DEFAULT' "$F" | head -10
    echo "=== V4533 GREP_KPARAM/GREP_KV ===" && rg -n '^function _grep_kparam|^function _grep_kv' "$F"
    echo "=== V4534 ORCHESTRATOR SPLITS COUNT ===" && rg -c '^function ' "$F"
    echo "=== V4536 TRACK_TMPFILE / RESOLVE_SYSTEMD_VER ===" && rg -n '^function _track_tmpfile|^function _resolve_systemd_ver' "$F"
    echo "=== V4536 IR_VALIDATE_COUNTS RUNTIME ASSERT ===" && rg -n '^function _ir_validate_counts' "$F"
    echo "=== V4536 IR_VALIDATE_COUNTS INVARIANTS ===" && _py_extract "$F" "_ir_validate_counts" | rg 'KERNEL_PARAMS:|MKINITCPIO_HOOKS:|MKINITCPIO_MODULES:|LOGIND_IGNORE_KEYS:|ENV_VARS:|SYSCTL_VALUES:|PKGS_ADD:|PKGS_DEL:|AUR_PKGS:|MASK:' | head -5
    echo "=== V461 SHOW_HELP HOIST ===" && rg -n '^function _ry_show_help' "$F"
    echo "=== V461 EXIT_GEN_NOFN/NOUUID ===" && rg -n 'EXIT_GEN_NOFN|EXIT_GEN_NOUUID' "$F" | head -10
    echo "=== V462 EXIT_GEN_SYSCTL ===" && rg -n 'EXIT_GEN_SYSCTL' "$F" | head -5
    echo "=== V462 HARD-DEPS sudo/df/mkdir/rmdir ===" && _py_extract "$F" "_ry_check_deps" | rg 'sudo|df|mkdir|rmdir' | head -10
    echo "=== V463 KEEPALIVE-CHILD NO-DASH-DASH ===" && rg -n 'sudo -n -v; or break' "$F" | head -5
    echo "=== V463 BOOT_TAINTED ===" && rg -n '_RY_BOOT_TAINTED' "$F" | head -10
    echo "=== V463 ENABLE_OK_START_FAIL ===" && rg -n 'ENABLE_OK_START_FAIL' "$F" | head -3
    echo "=== V463 KEEPALIVE STDERR TMPFILE ===" && rg -n 'SUDO_KEEPALIVE_ERR' "$F" | head -10
    echo "=== V464 NFTABLES DROPPED (regression — expect 0) ===" && rg -n 'nftables' "$F" | rg -v '#' | head -5
    echo "=== V464 EXPECTED_SERVICES (expect 3) ===" && rg -n '^set -g EXPECTED_SERVICES' "$F" | head -3
    echo "=== V465 MSG VERIFY UNGATED ===" && _py_extract "$F" "_msg" | rg -c 'VERIFY_'
    echo "=== V465 MESA RENAME ===" && rg -n 'libva-mesa-driver' "$F" | rg -v '#|removed|deprecated' | head -3
    echo "=== V465 NTSYNC PER-KERNEL ===" && rg -n '^function _ntsync_per_kernel_state|^function _ntsync_check_installed_kernels' "$F"
    echo "=== V466 BOOT_WIPE_AUTO_ACK ===" && rg -n 'BOOT_WIPE_AUTO_ACK|_bwg_managed_only' "$F" | head -10
    echo "=== V467 UFW IN MASK ===" && rg -n 'ufw\.service' "$F" | head -5
    echo "=== V468 RESOLVE_BOOT_PATH ===" && rg -n '^function _resolve_boot_path|_RY_BOOT_PATH' "$F" | head -10
    echo "=== V468 PBS_ENTRY_HAS_VALID_KERNEL ===" && rg -n '^function _pbs_entry_has_valid_kernel' "$F"
    echo "=== V468 BOOTCTL -X ===" && rg -n 'bootctl -x' "$F" | head -5
    echo "=== V469 MV -T / -Tf ===" && rg -n 'mv -T |mv -Tf ' "$F" | head -10
    echo "=== V469 RECLAIM TEST -d /proc ===" && rg -n 'test -d /proc/' "$F" | head -5
    echo "=== V469 PKG_REMOVE_BATCH_FAIL_DBLOCK ===" && rg -n 'PKG_REMOVE_BATCH_FAIL_DBLOCK' "$F" | head -5
    echo "=== V469 INSTALLED_BYTES rc 0/1/2 ===" && _py_extract "$F" "_check_phase_files" | rg '_installed_bytes' | head -5
    echo "=== V469 FINALIZE ERR-GATE ===" && _py_extract "$F" "_install_finalize" | rg 'INSTALL_HAD_ERRORS\b' | head -5  # v6.0 retired wipe-marker subsystem; current gate is `test "$INSTALL_HAD_ERRORS" = true; and return 1`
    echo "=== V4610 INSTALL_FILE UMASK 0077 ===" && _py_extract "$F" "_ry_install_file" | rg 'umask 0077' | head -3
    echo "=== V4611 PACMAN COMMAND PREFIX ===" && rg -c 'command pacman' "$F"
    echo "=== V4612 RECLAIM CMDLINE MATCH ===" && _py_extract "$F" "_reclaim_stale_lock" | rg 'cmdline|ry-install' | head -5
    echo "=== V4612 MKINITCPIO HOOKS:11 + MODULES:1 invariants ===" && _py_extract "$F" "_ir_validate_counts" | rg 'MKINITCPIO_HOOKS:11|MKINITCPIO_MODULES:1' | head -3
    echo "=== V4616 CLEANUP_PIPE NO 141 EXIT ===" && _py_extract "$F" "_cleanup_pipe" | rg -c 'exit 141|return 0|set -g _RY_OUTPUT_BROKEN'
    echo "=== V4616 CLEANUP_PIPE BODY ===" && _py_extract "$F" "_cleanup_pipe" | head -10
    echo "=== V4616 OUTPUT_BROKEN SHORT-CIRCUITS ===" && rg -n 'set -q _RY_OUTPUT_BROKEN; and return' "$F" | head -10
    echo "=== V4616 VERIFY_SUMMARY USES MSG_NOCOUNT ===" && _py_extract "$F" "_verify_summary" | rg '_msg_nocount' | head -5
    echo "=== V4616 CG_ACCESS_OK NO -- ===" && _py_extract "$F" "_cg_access_ok" | rg 'sudo -n test -f' | head -3
    echo "=== V4617 AUR_PKGS ===" && rg -n '^set -g AUR_PKGS' "$F" | head -3
    echo "=== V4617 PARU AS HARD PREREQ ===" && rg -n 'paru ' "$F" | rg -v '#' | head -10
    echo "=== V4617 TEARDOWN NO PIPE ARM (regression — expect 0) ===" && _py_extract "$F" "_teardown" | rg 'case pipe|case PIPE' | head -3
    echo "=== V4617 HELP NO 141 ROW (regression — expect 0) ===" && rg -n '141 SIGPIPE|141 signal' "$F" | head -3
    echo "=== V4617 PROFILE_NAME HOIST ABOVE _ry_show_help ===" && rg -n '^set -g PROFILE_NAME|^function _ry_show_help' "$F" | head -3
    echo "=== V4619 VSP_PACMAN_CONF EXPLICIT IF ===" && _py_extract "$F" "_vsp_pacman_conf" | rg 'if not test -f /etc/pacman.conf' | head -3
    echo "=== V501 VERSION ===" && rg -n '^set -g VERSION' "$F" | head -3
    echo "=== V501 HELP SIGNAL EXITS ===" && _py_extract "$F" "_ry_show_help" | rg '129/130/131|134/138/140' | head -5
    echo "=== V501 SIGNAL HANDLERS ===" && rg -n -- '--on-signal' "$F" | head -10
    echo "=== V501 TEARDOWN UNIFIED ===" && rg -n '^function _teardown' "$F"
    echo "=== V501 CLEANUP_OTHER USR1/USR2/ABRT ===" && rg -nA 3 '^function _cleanup_other' "$F" | head -5
    echo "=== V501 SYSCTL_VALUES count (expect 16) ===" && rg -nA 18 '^set -g SYSCTL_VALUES' "$F" | rg -c '^[0-9]+-    "'
    echo "=== V501 SYSTEM_DESTINATIONS count (expect 11) ===" && rg -nA 14 '^set -g SYSTEM_DESTINATIONS' "$F" | sed -n '/set -g SYSTEM_DESTINATIONS/,/set -g USER_DESTINATIONS/p' | rg -c '^[0-9]+-    "/'
    echo "=== V501 UDEV RQAFFINITY (regression — expect 0) ===" && rg -n 'rqaffinity|99-nvme-rqaffinity' "$F" | head -3
    echo "=== V501 _RY_MANAGED_FILE_COUNT (expect 15) ===" && rg -n '^set -g _RY_MANAGED_FILE_COUNT' "$F" | head -3
    echo "=== V501 _RY_MANAGED_FILE_COUNT DRIFT GUARD ===" && rg -n '_RY_MANAGED_FILE_COUNT drift' "$F" | head -3
    echo "=== V501 RY_INSTALL_ALLOW_PARTIAL_UPGRADE ===" && rg -n 'RY_INSTALL_ALLOW_PARTIAL_UPGRADE' "$F" | head -5
    echo "=== V501 RY_INSTALL_PKG_REMOVE_CASCADE ===" && rg -n 'RY_INSTALL_PKG_REMOVE_CASCADE' "$F" | head -5
  ) > "$D/p14.txt" 2>&1; echo "GROUP_D_EXIT=$?" >> "$D/p14.txt" ) &

  # ── GROUP E: P13 gap analysis (race/error/edge). Independent collection → p13.txt; analysis cross-refs p14.txt for 11 shared patterns. ──
  ( set -uo pipefail; (
    echo "=== TMPFILE LIFECYCLE ===" && python3 -ISc "
import re, sys
lines = open(sys.argv[1]).readlines()
fn = None; depth = 0; body = []
block_re = re.compile(r'^\s*(function |if |for |while |switch |begin\b)')
for line in lines:
    s = line.strip()
    if depth == 0:
        m = re.match(r'^function (\w+)( |$)', line)
        if m: fn = m.group(1); depth = 1; body = []; continue
    else:
        if not s.startswith('else') and block_re.match(line): depth += 1
        if s == 'end':
            depth -= 1
            if depth == 0:
                mk = sum(1 for b in body if 'mktemp' in b)
                rm = sum(1 for b in body if 'rm -f' in b)
                if mk > 0: print(f'{fn}: mktemp={mk} rm-f={rm}')
                fn = None; body = []; continue
        body.append(line)
" "$F" 2>&1
    echo "=== DRY GATE ===" && rg -n 'sudo |pacman |systemctl |mv |tee ' "$F" | rg -v "DRY|$MSG_SKIP|#|echo|rg|string|printf|sudo -n" | head -30
    echo "=== LOCK REGRESSION ===" && rg -n 'exec 9|_LOCK_METHOD.*flock' "$F" | head -5
    echo "=== LOCK TRACE ===" && rg -n 'mkdir.*LOCK|rmdir.*LOCK|verify_pid' "$F" | head -20
    echo "=== SORT LOCALE ===" && rg -n 'sort' "$F" | rg -v '#|LC_ALL' | head -20
    echo "=== SIGPIPE BODY ===" && _py_extract "$F" "_cleanup_pipe" | head -20
    echo "=== DOTFILE GLOB ===" && rg -n 'rm -rf.*\*' "$F" | rg -v '#' | head -5
    echo "=== BARE COREUTILS ===" && python3 -ISc "
import subprocess, sys, re, os
F = sys.argv[1]
msg_skip = os.environ.get('MSG_SKIP', '').split('|')
for cmd in 'rm mv cp chmod chown mkdir'.split():
    r = subprocess.run(['rg', '-n', r'\b' + cmd + ' ', F], capture_output=True, text=True)
    for line in r.stdout.strip().split('\n'):
        if not line: continue
        skip = any(x in line for x in [f'command {cmd}', f'sudo {cmd}', '#', 'echo', 'string', 'printf'] + msg_skip)
        if not skip and not re.search(r'\bset\b.*' + cmd, line): print(line)
" "$F" 2>&1 | head -20
    echo "=== RUN QUOTING ===" && rg -n '_run ' "$F" | rg -v '#|function |_log' | rg '\$[A-Z]' | rg -v '"' | head -10
    echo "=== HARDCODED PATHS ===" && rg -n '/home/|/root/' "$F" | rg -v '#|\$HOME|HOME|getent' | head -10
    echo "=== LOG BACKUP PATHS ===" && rg -n 'set -g.*ry-install' "$F" | head -10
    echo "=== XDG USAGE ===" && rg -n 'XDG_' "$F" | head -10
    echo "=== SUDO KEEPALIVE ===" && rg -n 'SUDO_KEEPALIVE' "$F" | head -10
    echo "=== PARALLEL BARE SUDO ===" && rg -n 'sudo ' "$F" | rg -v '_run|_ensure_sudo|#|sudo true|sudo -k|sudo -n|SUDO_KEEPALIVE|keepalive' | head -20
    echo "=== PARALLEL CHILD SOURCE ===" && rg -n 'source.*funcs|functions.*needed' "$F" | head -10
  ) > "$D/p13.txt" 2>&1; echo "GROUP_E_EXIT=$?" >> "$D/p13.txt" ) &

  wait

  # ── Verify parallel group exit status ──
  for f in "$D"/*.txt; do
    fname=$(basename "$f" .txt)
    exit_val=$(grep -oP 'GROUP_\w+_EXIT=\K[0-9]+' "$f" 2>/dev/null | tail -n 1)
    if [ -n "$exit_val" ] && [ "$exit_val" != "0" ]; then
      echo "[INFO] GROUP EXIT: $fname exited $exit_val — some data collection commands failed" >> "$O"
    fi
  done

  # Post-wait data guard (D and E collect independently; E's P13 analysis reads p14.txt for 11 shared patterns).
  if [ ! -s "$D/p14.txt" ]; then
    echo "[INFO] p14.txt empty or missing — P14 v3.1.0-v5.0.1 checks will be incomplete" >> "$O"
  fi
  if [ ! -s "$D/p13.txt" ]; then
    echo "[INFO] p13.txt empty or missing — P13 gap analysis checks will be incomplete" >> "$O"
  fi

  # ── SIZE GATE: truncate any file over 500 lines (parallelized) ──
  for f in "$D"/*.txt; do
    (
      lines=$(wc -l < "$f")
      if [ "$lines" -gt 500 ]; then
        fname=$(basename "$f" .txt)
        # Map data files to phases for diagnostics
        case "$fname" in
          p1-3)   phases="P1-P3 (syntax,routing,errors)" ;;
          p4-5)   phases="P4-P5 (vars,security)" ;;
          p6-7)   phases="P6-P7 (configs,cross-refs)" ;;
          p8-9)   phases="P8-P9 (architecture,patterns)" ;;
          p10-11) phases="P10-P11 (docs,testing)" ;;
          p13)    phases="P13 (gap analysis)" ;;
          p14)    phases="P14 (v3.1.0-v5.0.1 specifics)" ;;
          *)      phases="unknown ($fname)" ;;
        esac
        echo "WARNING: $f has $lines lines, truncating to 500" >&2
        echo "[INFO] SIZE GATE: $f ($phases) truncated from $lines to 500 lines — affected phase checks may have incomplete data" >> "$O"
        # ── Recovery manifest: list lost sections for targeted re-collection ──
        grep '^=== .* ===$' "$f" > "$D/${fname}-full-sections.txt"
        # Skeleton extraction: keep all section headers + first 5 data lines per section
        awk '/^=== .* ===$/{header=1;count=0;print;next} header{count++;if(count<=5)print}' "$f" > "$f.tmp" && mv "$f.tmp" "$f"
        grep '^=== .* ===$' "$f" > "$D/${fname}-kept-sections.txt"
        comm -23 <(sort "$D/${fname}-full-sections.txt") <(sort "$D/${fname}-kept-sections.txt") > "$D/${fname}-lost.txt"
        lost_count=$(wc -l < "$D/${fname}-lost.txt")
        if [ "$lost_count" -gt 0 ]; then
          echo "[INFO] SIZE GATE RECOVERY: $lost_count sections lost in $fname — manifest at $D/${fname}-lost.txt" >> "$O"
        fi
      fi
    ) &
  done
  wait

  # ── EMPTY SECTION GUARD: single-pass awk (O(n) vs prior O(n²)) ──
  for f in "$D"/p*.txt; do
    awk '/^=== .* ===$/{if (hdr) print hdr "\t" cnt; hdr=$0; cnt=0; next} {cnt++} END {if (hdr) print hdr "\t" cnt}' "$f" \
      | awk -F'\t' -v f="$(basename "$f")" '$2<=0 {printf "[INFO] EMPTY SECTION: %s in %s — 0 results or truncated\n", $1, f}' >> "$O"
  done

  echo "=== DATA COLLECTION COMPLETE ==="
  shopt -s nullglob; _files=("$D"/*.txt); shopt -u nullglob
  [ ${#_files[@]} -gt 0 ] && wc -l "${_files[@]}" || echo "[INFO] no data files produced in $D"
  ```

STEP 2: ANALYZE ON DISK

  Read checklist + $D/*.txt via bat --plain. Compare per phase. Findings → $O.

  DATA FILE → PHASE MAPPING:
    p1-3.txt   → P1-3   (syntax, routing, errors)
    p4-5.txt   → P4-5   (vars, security)
    p6-7.txt   → P6-7   (configs, cross-refs)
    p8-9.txt   → P8-9   (architecture, patterns)
    p10-11.txt → P10-11 (docs, testing)
    p13.txt    → P13    (gap; also reads p14.txt for FOOTER_RACE, ROTATION_LOCK, LC_ALL, SIGPIPE, BOOT_SANITY)
    p14.txt    → P14    (v3.1.0-v5.0.1 specifics)
    P15 deep checks use all files above.

  Missing data:
    p14.txt missing/truncated → P13 shared-pattern checks (234-238) DEFERRED.
    p13.txt missing            → P13 DEFERRED independently.

  SIZE GATE RECOVERY: $D/<file>-lost.txt lists removed sections. Re-collect via targeted
  rg/python3 → $D/<file>-recovery.txt. Do NOT claim PASS on truncated checks without recovery.

  Report to context ONLY:
    "PASS 1 complete. N findings (N CRIT, N HIGH, N MED, N LOW, N INFO).
     Phases 1-11, 13-15 analyzed. Proceeding to PASS 2.
     Per-phase tally: P1:N P2:N ... P15:N (must sum to 420)."

  Never paste raw rg output to context.

═══════════════════════════════════════════════════════════════
 CHECKLIST REFERENCE (save to /home/claude/audit-data/checklist.txt)
═══════════════════════════════════════════════════════════════

─── Phase 1: Fish Shell Compliance (12 checks) ───────────────

  Exempt: Hits in embedded content (ExecStart, awk, heredocs, lint
  self-checkers) are NOT findings.

  1.1  BASHISMS
       [ ] 1. Syntax contamination: no $() (prefer () form), [[]], ${var}, &&/|| in fish
       [ ] 2. Scope/export: no export, set -e VAR, functions -e, unset, local
               NOTE: `set -e` hits also match Phase 4 check 43 (modern syntax).
               Categorize under P4 if used as Fish set --erase shorthand.
       [ ] 3. source usage: verify correct (not bash-style sourcing)

  1.2  VERSION GATE
       [ ] 4. Fish version checked before executing; minimum matches features

  1.3  STRING OPS
       [ ] 5. -- separator before arguments in string commands
       [ ] 6. No sed/awk/grep where string builtins suffice
       [ ] 7. string match uses -q for boolean tests

  1.4  OPTION PARSING
       [ ] 8. argparse used for option parsing
       [ ] 9. Unknown opts → error + usage
       [ ] 10. -- end-of-options handled

  1.5  SYNTAX VALIDATION
       [ ] 11. fish --no-execute passes
       [ ] 12. Embedded fish snippets valid

─── Phase 2: Stderr / Stdout Routing (11 checks) ─────────────

  2.1  OUTPUT CONTRACT
       [ ] 13. Diagnostics → stderr, machine data → stdout
       [ ] 14. --help → stdout, 0 stderr bytes
       [ ] 15. --version → stdout, 0 stderr bytes
       [ ] 16. Errors → stderr; bad option: help to stderr, 0 stdout

  2.2  MESSAGE FUNCTIONS
       [ ] 17. Central _msg routes to stderr
       [ ] 18. NO_COLOR env respected (sole color control; --no-color flag removed v3.4.0)
       [ ] 19. isatty check (test -t 2) for color decisions
       [ ] 20. Levels consistent (ok/fail/info/warn/err/dry)

  2.3  COMMAND OUTPUT
       [ ] 21. Wrapped stdout preserved + stderr captured
       [ ] 22. Quiet mode suppresses, verbose shows detail

  2.4  EXECUTION ORDER
       [ ] 23. Root check/env setup silent before --help/--version parse

─── Phase 3: Error Handling & Exit Codes (15 checks) ─────────

  3.1  EXIT CODES
       [ ] 24. 0: success/help/version/check-clean
       [ ] 25. 1: runtime errors; 3: preflight; 4: boot-critical; 5: lock
       [ ] 26. 2: usage errors; 10: drift (--check)
       [ ] 27. Signal exits: 129 SIGHUP (128+1), 130 SIGINT (128+2),
               131 SIGQUIT (128+3), 134 SIGABRT (128+6), 138 SIGUSR1 (128+10),
               140 SIGUSR2 (128+12), 143 SIGTERM (128+15).
               NOTE: 141 SIGPIPE retired as exit code in v4.6.16 — _cleanup_pipe
               sets _RY_OUTPUT_BROKEN and returns; JSONL log is canonical.
               v4.6.17 dropped the unreachable `case pipe` arm from _teardown
               and the "141 SIGPIPE" row from help / README signals tables.
       [ ] 28. Functions use return not exit; no bare exit; no exit inside functions

  3.2  STATUS CAPTURE
       [ ] 29. $status captured immediately, not clobbered by _log/_msg
       [ ] 30. $pipestatus used for pipes

  3.3  SIGNAL HANDLERS
       [ ] 31. INT, TERM, HUP, QUIT, USR1, USR2, ABRT, PIPE, fish_exit handlers present
               (_cleanup --on-signal INT/TERM/HUP/QUIT;
                _cleanup_other --on-signal USR1/USR2/ABRT (delegates to _cleanup);
                _cleanup_pipe --on-signal PIPE (NON-FATAL since v4.6.16 — sets _RY_OUTPUT_BROKEN, returns);
                _cleanup_on_exit --on-event fish_exit;
                _progress_on_winch --on-signal WINCH).
                _teardown is the unified cleanup orchestrator called from all signal/exit paths.
       [ ] 32. PIPE handler: _cleanup_pipe sets _RY_OUTPUT_BROKEN=true and returns 0
               (v4.6.16 inversion — was: exit 141). _msg_print / _err_loud / _echo
               short-circuit on _RY_OUTPUT_BROKEN to avoid EPIPE re-entry. JSONL log
               remains canonical when stderr/stdout consumer closes. Regression detector:
               rg 'exit 141' inside _cleanup_pipe body → 0 hits.
       [ ] 33. Handlers don't mask exit code

  3.4  CLEANUP GUARANTEE
       [ ] 34. Temp files cleaned on all paths
       [ ] 35. Lock files cleaned on all paths
       [ ] 36. Background procs + credentials + sudo keepalive cleaned
       [ ] 37. Cleanup idempotent
       [ ] 38. UID-scoped /tmp sweep

─── Phase 4: Variable Handling (12 checks) ────────────────────

  4.1  SCOPE
       [ ] 39. Globals: set -g
       [ ] 40. Locals: set -l
       [ ] 41. Exports: set -gx
       [ ] 42. No scope leaks; loop vars scoped; no flagless top-level set

  4.2  set --erase AUDIT
       [ ] 43. Modern syntax (not set -e); no unnecessary 2>/dev/null
       [ ] 44. Creds erased on ALL paths; consistent style

  4.3  FOR-LOOP VARIABLES
       [ ] 45. Unquoted $ARRAY in for-loops; never quoted; empty-safe

  4.4  UNNECESSARY REDIRECTS ON BUILTINS
       [ ] 46. No 2>/dev/null on set/command -q (OK on external-cmd capture)

  4.5  NAMING
       [ ] 47. UPPER globals, _UPPER internal, lower locals
       [ ] 48. Functions lowercase+underscore; no single-letter outside tight loops

  4.6  QUOTING
       [ ] 49. Vars quoted in test/string
       [ ] 50. Paths always quoted

─── Phase 5: Security (20 checks) ────────────────────────────

  5.1  COMMAND INJECTION
       [ ] 51. No eval
       [ ] 52. Input validated; no unchecked interpolation
       [ ] 53. -- separators in commands
       [ ] 54. No var-as-command; only $argv in _run-style wrappers

  5.2  TEMP FILES
       [ ] 55. mktemp used for temp files
       [ ] 56. TOCTOU mitigated; mktemp fail handled
       [ ] 57. Cleaned all paths; UID-owned
       [ ] 58. Unique prefix; no predictable names in /tmp

  5.3  CREDENTIAL HANDLING
       [ ] 59. read -s for credentials
       [ ] 60. Erased after use; redacted in logs
       [ ] 61. 0600 perms; not in CLI args; not exported
       [ ] 62. Special chars escaped

  5.4  FILE PERMISSIONS
       [ ] 63. System: 644, user: 600, creds: 600
       [ ] 64. Logs: 600
       [ ] 65. chmod before mv

  5.5  PRIVILEGE SEPARATION
       [ ] 66. Root warning/abort + forced dry-run
       [ ] 67. sudo least privilege + keepalive lifecycle
       [ ] 68. Restricted sudo handled

  5.6  INPUT VALIDATION
       [ ] 69. Interface names, SSIDs (1-32, no metachar/%),
               passphrases (8-63, no %/newline)
       [ ] 70. Country codes, paths (canonical, no traversal), numeric ranges

─── Phase 6: Embedded Config Content (30 checks) ─────────────

  3 arrays: SYSTEM (11), USER (1), SERVICE (0) = 12 files (v7.3.5 — SERVICE_DESTINATIONS emptied v7.2.1 when cpupower-epp.service was dropped; sysctl is 10th SYSTEM entry, tmpfiles is 11th). _RY_MANAGED_FILE_COUNT is a LITERAL 12 with a runtime drift guard at the top-level: immediately after the destination-array declarations, `_ry_dst_count` is computed via `(count $SYSTEM_DESTINATIONS $USER_DESTINATIONS $SERVICE_DESTINATIONS)` and compared against the constant; drift triggers `[ERR] _RY_MANAGED_FILE_COUNT drift: declared=N computed=M` followed by `_ry_exit $EXIT_PREFLIGHT`. The v4.5.4 → v5.0.1 `(count …)` interpolation form was reverted to a literal in v5.0.1 and remains literal at v7.3.5.

  6.1  SYSTEMD-BOOT
       [ ] 71. key<space>value (no =)
       [ ] 72. Valid keys only

  6.2  KERNEL CMDLINE
       [ ] 73. rw root=UUID=<uuid> dynamic, cached _ROOT_UUID
       [ ] 74. Space-separated, no trailing ws; 15 KERNEL_PARAMS entries (v4.0.0)
       [ ] 75. modprobe.d blacklist-only (no options since v2.5.0)

  6.3  SDBOOT-MANAGE
       [ ] 76. KEY="VALUE", required keys, FALLBACK="quiet",
               REMOVE_EXISTING=yes applied unconditionally (v6.0 retired the
               RY_INSTALL_CONFIRM_BOOT_WIPE ack gate; foreign-bootloader-entries
               coexistence is the user's responsibility, documented in README
               Bootloader section). Regression: rg 'RY_INSTALL_CONFIRM_BOOT_WIPE'
               → 0 hits.
       [ ] 77. OPTIONS ⊇ KERNEL_PARAMS

  6.4  MKINITCPIO
       [ ] 78. MODULES=() HOOKS=() COMPRESSION="" format correct
       [ ] 79. Hook order (11 hooks, runtime-asserted via `_ir_validate_counts`
               invariant `MKINITCPIO_HOOKS:11`):
               base → systemd → autodetect → microcode → modconf → kms →
               keyboard → sd-vconsole → block → filesystems → fsck.
               No `resume` hook — sleep/hibernate targets masked.
               MKINITCPIO_MODULES is single-element [amdgpu] (invariant `:1`).
               v7.2.3 _vmh_order_checks added 2 pair rules + fsck-last check;
               canonical pair set is 8: `systemd:autodetect`,
               `autodetect:microcode`, `autodetect:modconf`,
               `systemd:sd-vconsole`, `systemd:keyboard`,
               `keyboard:sd-vconsole`, `modconf:kms`, `block:filesystems`.
               Plus invariants: base first, fsck last, no duplicates.
               Regression: rg `systemd:autodetect` and `autodetect:microcode`
               in `_vmh_order_checks` body → both ≥1 hit each.
       [ ] 80. Modules validated via modinfo (current v7.3.5 form;
               v6.0 reduction dropped modprobe -n probe). Per-module probe
               via `command modinfo <mod> >/dev/null` with `command -q modinfo`
               availability gate. Used by _ry_validate_mkinitcpio_modules
               + _aur_verify_mt7925 + tcp_bbr version detect.
               Regression: rg 'modprobe -n' → 0 hits;
               rg 'command modinfo' → ≥3 hits.

  6.5  MODPROBE
       [ ] 81. No options lines (regression); blacklist format; modules validated
       [ ] 82. Cross-ref with cmdline

  6.6  MODULES-LOAD REGRESSION (v3.1.0)
       [ ] 83. No modules-load.d destinations; no dead code paths.
              CachyOS vendor ships /usr/lib/modules-load.d/ntsync.conf.

  6.7  UDEV
       [ ] 84. == for match, = for assign
       [ ] 85. ATTR{} correct; no empty match values

  6.8  SYSTEMD UNITS
       [ ] 86. [Unit] [Service] [Install] present
       [ ] 87. After= correct; ExecStart absolute paths
       [ ] 88. systemd-analyze verify; Type= matches behavior
       [ ] 89. amdgpu-performance removed (v3.38.0): 0 references in
               DESTINATIONS, _ry_get_file_content, EXPECTED_SERVICES (regression)
       [ ] 90. cpupower-epp: bash -c, EPP path, After=cpupower, multi-user

  6.9  DROP-INS
       [ ] 91. No journald.conf.d (regression); coredump.conf.d present
               with Storage=none, ProcessSizeMax=0
       [ ] 92. resolved [Resolve] not [Resolved]; logind [Login]

  6.10 IWD
       [ ] 93. [General] [DriverQuirks] [Network]; valid keys

  6.11 NETWORKMANAGER
       [ ] 94. [device] [connection] [logging] sections
       [ ] 95. wifi.powersave correct; wifi.backend matches

  6.12 WIRELESS-REGDOM + ENVIRONMENT.D
       [ ] 96. WIRELESS_REGDOM="XX"
       [ ] 97. environment.d: KEY=VALUE (no export/quotes); XDG_RUNTIME_DIR

  6.13 FISH CONFIGS
       [ ] 98. fish --no-execute valid; guards present; SSH auth sock priority

  6.14 SYSCTL.D (v3.2.0)
       [ ] 99. Format: key = value, comment header refs CachyOS vendor; no overlap;
                priority-99 override note (v3.45.0); string split -m1 '=' (v3.46.0)
       [ ] 100. SYSCTL_VALUES 16 entries (history: pre-v4.0.0 21 → v4.0.0 19 →
                v4.1.10 21 → v4.6.x reduced to 16; current count asserted at runtime
                via `_ir_validate_counts` invariant `SYSCTL_VALUES:16`):
                default_qdisc=fq, netdev_max_backlog=16384, rmem_max=134217728,
                wmem_max=134217728, tcp_congestion_control=bbr, tcp_fastopen=3,
                tcp_mtu_probing=1, tcp_notsent_lowat=131072, tcp_rmem,
                tcp_slow_start_after_idle=0, tcp_wmem, vm.max_map_count=2147483642,
                vm.watermark_boost_factor=0, fs.protected_fifos=2,
                fs.protected_regular=2, vm.compaction_proactiveness=0.
                Removed v4.0.0: inotify.max_user_watches, somaxconn,
                unprivileged_bpf_disabled, page_lock_unfairness.
                Removed in v4.6.x window (5 entries): busy_read=50, busy_poll=50,
                netdev_budget=600, kernel.split_lock_mitigate=0, vm.swappiness=100
                — all retired as the script narrowed targeting to linux-cachyos
                whose vendor 70-cachyos-settings.conf already covers the busy-poll
                tuning; split_lock_mitigate moved to KERNEL_PARAMS as
                `split_lock_detect=off`; vm.swappiness rolled back to kernel default.

─── Phase 7: Cross-References & Consistency (14 checks) ──────

  7.1  VERSION SYNC
       [ ] 101. Header = global = README version
       [ ] 102. Changelog entry exists; lint verifies auto

  7.2  FILE COUNTS
       [ ] 103. SYSTEM count=11, _ry_get_file_content system cases=11 (v7.3.5
                composition; differs from the v5.0.1-era list):
                  /boot/loader/loader.conf,
                  /etc/kernel/cmdline,
                  /etc/sdboot-manage.conf,
                  /etc/mkinitcpio.conf,
                  /etc/systemd/resolved.conf.d/99-cachyos-resolved.conf,
                  /etc/systemd/logind.conf.d/99-cachyos-logind.conf,
                  /etc/iwd/main.conf,
                  /etc/NetworkManager/conf.d/99-cachyos-nm.conf,
                  /etc/default/cpupower-service.conf  (added v7.2.0),
                  /etc/sysctl.d/99-cachyos-sysctl.conf,
                  /etc/tmpfiles.d/99-cachyos-thp.conf  (added v6.5.14).
                Retired SYSTEM entries (regression detectors):
                  /etc/udev/rules.d/99-nvme-rqaffinity.rules (v4.5.x),
                  /etc/systemd/coredump.conf.d/99-cachyos-coredump.conf (v6.x reduction),
                  /etc/drirc (v7.2.0 — replaced by cpupower-service.conf).
                Regression:
                  rg 'rqaffinity|99-nvme-rqaffinity' → 0 hits.
                  rg 'coredump\.conf\.d' → 0 hits.
                  rg '/etc/drirc\b|_content__etc_drirc|_grep_xml_tag|_post_drirc' → 0 hits.
       [ ] 104. USER count=1 (~/.config/environment.d/10-environment.conf — the
                only user-scope managed file at v7.3.5), SERVICE count=0
                (SERVICE_DESTINATIONS emptied v7.2.1 when cpupower-epp.service
                was dropped). _ry_get_file_content cases sum to 12 total.
                `_RY_MANAGED_FILE_COUNT=12` declared as top-level literal (script L15)
                and runtime-asserted against `(count $SYSTEM_DESTINATIONS
                $USER_DESTINATIONS $SERVICE_DESTINATIONS)` immediately after the
                destination-array declarations. Drift triggers `[ERR]
                _RY_MANAGED_FILE_COUNT drift: declared=N computed=M` via
                `_ry_exit $EXIT_PREFLIGHT`. Hand-synced literal — runtime drift
                guard catches array-vs-constant desync, doc-vs-source drift
                requires manual cross-check against README.
                Regression: rg '^set -g _RY_MANAGED_FILE_COUNT 12$' → 1 hit;
                rg '_RY_MANAGED_FILE_COUNT drift' → 1 hit.
       [ ] 105. Progress steps = 6 (Preflight/Packages/Configuration/Services/Boot/Finalize);
                phase labels passed inline at each `_progress <phase>` call site.
                PROGRESS_STEPS / PROGRESS_TOTAL arrays retired v4.1.9.
       [ ] 106. README count=12 total at v7.3.5; managed-files table reflects
                the 11+1+0 breakdown. _RY_MANAGED_FILE_COUNT literal in source
                must match the README total; the runtime drift guard catches
                array-vs-constant desync; doc-vs-source drift requires manual
                cross-check. README badge version + L2 header VERSION + L7
                `set -g VERSION` must all agree (3-way sync).

  7.3  PARAMETER CROSS-REFS
       [ ] 107. modprobe.d blacklist-only (regression)
       [ ] 108. MKINITCPIO_MODULES match hardware; blacklist cmdline↔modprobe
       [ ] 109. ENV_VARS match environment.d↔runtime verify

  7.4  README ↔ HELP
       [ ] 110. All --help options in README
       [ ] 111. Sub-options + modifiers documented as separate entries
       [ ] 112. --install-file path requirement documented in README. The
                README's "Deprecated flags — DO NOT re-introduce" section
                is scoped to deprecated ENVIRONMENT VARIABLES (DXVK_ASYNC,
                DXVK_FRAME_RATE, WINE_FULLSCREEN_FSR — VKD3D_FRAME_RATE
                retained), NOT CLI flags. Removed CLI flags (--all, --dry-run,
                --diff, --fix, --lint, --test-all, --completions, --logs, --force)
                are absent from the argparse spec and rejected generically by
                argparse with `[ERR] <argparse-msg>` → `_pre_dispatch_exit
                $EXIT_USAGE`. No per-flag handler. Regression: must NOT add
                a CLI-flag section to README under the same header — would
                alias the env-var section.
       [ ] 113. Exit codes in both --help and README:
                  0 ok · 1 non-critical · 2 usage · 3 preflight · 4 boot-critical ·
                  5 lock · 10 drift · 129/130/131/143 (HUP/INT/QUIT/TERM) ·
                  134/138/140 (ABRT/USR1/USR2).
                Retired:
                  - 141 SIGPIPE (v4.6.16/.17 — _cleanup_pipe non-fatal; row dropped
                    from help text + README); regression: rg '141 SIGPIPE|141 signal' → 0.
                  - Code 11 EXIT_LINT (v3.48.20 — slot reused as EXIT_GEN_NOFN per
                    v4.6.1; EXIT_GEN_NOFN/NOUUID/SYSCTL=11/12/13 are INTERNAL-ONLY
                    sentinels squashed to EXIT_PREFLIGHT at consumer site, never
                    surfaced to the user; regression: rg 'EXIT_LINT' → 0).
       [ ] 114. Prerequisites listed; mode cross-refs

─── Phase 8: Function Architecture (19 checks) ───────────────

  8.1  ARITY
       [ ] 115. Count validated at entry
       [ ] 116. Error messages show counts; return 1 on error

  8.2  ATOMIC FILE OPS
       [ ] 117. Write to tmp first; central helper `_atomic_write_file` (v4.5.8
                family) handles validate-parent → write → chmod → mv with sudo-aware
                paths and `_rm_tmp` cleanup on every failure branch.
       [ ] 118. Perms before mv; same-fs mv. ALL atomic-rename sites use `mv -T` /
                `mv -Tf` (v4.6.9) to refuse target-as-directory: _atomic_write_file,
                _mkinitcpio_revert, _fstab_atomic_replace, _if_write_wipe_marker,
                _acquire_lock_fresh. Regression: rg 'mv --' inside atomic-write
                helpers → 0; rg 'mv -T |mv -Tf ' → ≥4 hits.
       [ ] 119. Symlink detection (`_is_symlink` helper, sudo-aware probe);
                owner/group validated via `_awf_validate_parent`; _ry_install_file
                user-scope `mkdir -p` wrapped in `umask 0077` (v4.6.10) so
                intermediate dirs are 0700 even under USERGROUPS_ENAB (umask 002).

  8.3  IDEMPOTENCY
       [ ] 120. Re-run identical; diff-before-write skip
       [ ] 121. pacman --needed; service enable idempotent

  8.4  LOGGING
       [ ] 122. JSONL format + timestamps
       [ ] 123. Sensitive redacted
       [ ] 124. MAX_LOGS rotation + log perms 0600
       [ ] 125. _json_str escaping

  8.5  LOCK FILE
       [ ] 126. Prevents concurrent; mkdir-only (since v2.8.2). _acquire_lock split
                into `_acquire_lock_fresh` + `_reclaim_stale_lock` (v4.5.31).
       [ ] 127. Stale reclaim: REVERTED in v6.0 reduction to `kill -0` liveness
                (was: v4.6.9 `test -d /proc/$old_pid` + v4.6.12 cmdline-substring
                "ry-install" match). Current contract (v7.3.5 hardening): `string
                match -qr '^[1-9]\d*$' -- "$_stale_pid"; and not command kill -0
                "$_stale_pid" 2>/dev/null` → reclaim. v7.3.5 tightened the regex
                `^\d+$` → `^[1-9]\d*$` to reject `pid=0` and leading-zero values
                (`kill -0 0` is a process-group signal that would falsely succeed
                liveness probe against pid 0, blocking reclaim of a genuinely
                stale lock). Trade-off documented in v6.0 release notes: removing
                the /proc + cmdline defence shortened the function and dropped
                Linux-only dependencies, accepting the EPERM≠ESRCH edge case +
                fish-PID-reuse window as low-likelihood failure modes on the
                target single-user workstation profile. Cleaned on every exit
                path via `_dc_kill_children` keyed on `_RY_LOCK_DIR_OWNED`.
                Regression check (current state):
                  _py_extract _acquire_lock | rg 'kill -0' → 1 hit (expected).
                  _py_extract _acquire_lock | rg 'test -d /proc' → 0 hits.
                  _py_extract _acquire_lock | rg "'\^\[1-9\]\\\\d\*\\\$'" → 1 hit (v7.3.5 form).
                  _py_extract _acquire_lock | rg "'\^\\\\d\+\\\$' -- \"\\\$_stale_pid\"" → 0 hits (legacy form retired).
                  rg '^function _reclaim_stale_lock' → 0 hits (helper inlined).
                NOTE: If this script is forked for a multi-user or longer-running
                deployment, restoring the /proc + cmdline defence (per check 393's
                historical narrative) is recommended — flag as HIGH in that
                deployment context. Do NOT flag as a defect on the v7.3.5
                target profile.
       [ ] 128. PID written inside locked subshell (v3.48.16) before flock release.
       [ ] 129. LOCK METHOD: flock used for atomic stale-lock reclaim (v3.6.3);
                `exec 9` and `_LOCK_METHOD` removed. rg 'exec 9|_LOCK_METHOD' → 0 hits.

  8.6  INSTALL_FILES WRAPPER
       [ ] 133. Argparse tmp via mktemp; cleaned on both success AND error

  8.7  EXTERNAL COMMANDS + DISPATCH
       [ ] 134. command -q preflight; failure handled; no version assumptions
       [ ] 135. Pre-lock validation: usage errors BEFORE _acquire_lock
       [ ] 136. Dispatch quoting: do_func "$TARGET" quoted

─── Phase 9: Patterns & Anti-Patterns (9 checks) ─────────────

  9.1  FALSE POSITIVES (do NOT flag)
       [ ] 137. "text"(cmd) concat, unquoted $ARRAY in for-loops,
               $() inside awk/ExecStart, [[:class:]] in grep -E

  9.2  ANTI-PATTERNS
       [ ] 138. grep -q | ... (dead pipe)
       [ ] 139. cat file | grep (useless cat)
       [ ] 140. test "" → use -z; bare find without command prefix
       [ ] 141. head -1/tail -1 → -n 1; nested substitution clarity
       [ ] 142. No dead code
       [ ] 143. REDIRECT BUG: string trim < nonexistent leaks stderr →
               use cat -- 2>/dev/null. RUNTIME-only test.

  9.3  STYLE
       [ ] 144. Consistent indentation; functions ≤50 lines
       [ ] 145. Comments explain WHY

─── Phase 10: Magic Numbers & Documentation (11 checks) ──────

  10.1 MAGIC NUMBERS
       [ ] 146. All numeric constants named
       [ ] 147. Perms, thresholds, timeouts documented
       [ ] 148. Hardware values documented; 130 = SIGINT
       [ ] 149. TEMP_CPU_WARN/CRIT, TEMP_GPU_WARN/CRIT
       [ ] 150. DISK_ROOT_WARN/CRIT, BOOT_SPACE_WARN/CRIT
       [ ] 151. ROOT_AVAIL, NVME_LIFE, MAX_LOGS
       [ ] 152. BOOT_TIME_WARN/TARGET

  10.2 DOCUMENTATION
       [ ] 153. --help covers all modes/flags
       [ ] 154. Inline comments + cross-refs + completions generated
       [ ] 155. README: quick-start, options, config ref
       [ ] 156. Changelog covers versions; LICENSE present

─── Phase 11: Testing & Validation (11 checks) ───────────────

  11.1 PREFLIGHT
       [ ] 157. Deps verified + disk space checked
       [ ] 158. Network verified + HW fingerprint validated
       [ ] 159. Kernel + systemd version checked
       [ ] 160. Dep list accuracy (_ry_check_deps = actual usage, no stale)

  11.2 CONFIG VALIDATION
       [ ] 161. fish --no-execute + systemd-analyze verify on generated
       [ ] 162. modprobe -n; INI section headers; udev no empty match
       [ ] 163. Hook order programmatic validation

  11.3 VERSION SYNC + COUNT CONSISTENCY
       [ ] 164. Version sync + file/case counts + MANAGED_FILE_COUNT
                matches _RY_MANAGED_FILE_COUNT compile-time constant
       [ ] 165. Anti-pattern self-scan via rg in verification modes;
                progress steps = actual _progress calls (6 steps)

  11.4 IDEMPOTENCY + RECOVERY
       [ ] 166. Re-run identical; manifest-based orphan detection via
                _manifest_check_orphans; _manifest_write gated on success
       [ ] 167. Recovery documented in README (uninstall section);
                manifest at ~/ry-install/.manifest is source of truth.
                NOTE: --dry-run, backup system, --fix, and do_rollback
                all removed (v3.5.0/v4.1.0) — script is install-only.

  NOTE: Check numbers are non-sequential across phases. Phase order
  follows execution priority (P13 gap analysis before P14 specifics).
  Check ranges: P1-11 → #1-129,133-167, P13 → #234-238, P14 → #171-214,254-394,
  P15 → #215-233, P12 → #239-249. Checks 168-170 retired in v4.0.0 audit
  prompt (rollback/backup/--dry-run features removed from ry-install v3.5.0–v4.1.0).
  Checks 334-367 added in v4.0.0 audit prompt covering ry-install v3.47.0–v4.1.3.
  Checks 368-378 added in v4.1.0 audit prompt covering ry-install v4.1.4.
  Check 379 added in v4.2.0 audit prompt covering ry-install v4.1.5.
  Check 380 added in v4.3.0 audit prompt covering ry-install v4.1.6.
  Checks 381-384 added in v4.4.0 audit prompt covering ry-install v4.1.8.
  v5.0.0 audit prompt resyncs full Phase 14 against ry-install v4.5.8 (no new
  check numbers; existing checks rebaselined for v4.1.9-v4.5.8 deltas
  including PROGRESS_STEPS removal, profile-system removal, helper retirements,
  fish version gate raise, and runtime-derived _RY_MANAGED_FILE_COUNT).
  Checks 385-394 added in v5.0.1 audit prompt covering ry-install v4.5.25–v5.0.1
  (POST_HOOKS dispatch table, _ir_validate_counts runtime asserts, _RY_BOOT_TAINTED
  separation, _bwg_managed_only auto-ack, _resolve_boot_path/_RY_BOOT_PATH cache,
  _RY_OUTPUT_BROKEN EPIPE guard, AUR_PKGS + paru hard-prereq, _verify_summary
  off-by-one fix, _reclaim_stale_lock cmdline defence, new env-var runtime gates).
  Checks 395-410 added in v7.3.3 audit prompt covering ry-install v5.0.1 → v6.0.0
  reduction (source-mode/keepalive/LVM/boot-wipe-ack/etc retirements as 0-hit
  regression detectors).
  Checks 411-439 added in v7.3.3 audit prompt covering ry-install v6.0.0 → v7.3.3
  expansion (post-write symlink re-check, findmnt --verify, partial-upgrade gate,
  cascade gate, _run 5-helper split, content-equality string collect, RADV rename,
  avahi-daemon masking, realtime-privileges/cpupower additions, cpupower-service
  deployment, THP tmpfiles, wireless-regdom).
  Check 440 added in v7.3.3 audit prompt covering README phase-order alignment.
  Total checks: 431 (420 static + 11 runtime).

─── Phase 13: Gap Analysis (5 checks) ────────────────────────

  13.1 RACE CONDITIONS
       [ ] 234. mktemp O_EXCL, no check-use gap, no attacker substitution

  13.2 ERROR PATH COMPLETENESS
       [ ] 235. Every tmpfile cleaned on EVERY error branch;
               credentials erased on every error path
       [ ] 236. Signal cleanup covers all tmpfile patterns (incl ry-argparse.*)

  13.3 LOGIC
       [ ] 237. DRY gates: every state-changing call through _run or DRY check

  13.4 LOCK REGRESSION (v2.8.2)
       [ ] 238. mkdir + kill -0 stale reclaim (v6.0 simplification). flock(1)
               dep retired with the v6.0 reduction; check_phase reads `_acquire_lock`
               body for `mkdir`-fresh + `kill -0`-reclaim pattern. Regression:
               rg 'flock' source-wide → 0 hits (helper retired).


─── Phase 14: v3.1.0–v7.3.5 Specifics (232 checks) ───────────

  14.1 KVER GLOBALS (v3.1.0)
       [ ] 171. KVER, KVER_MAJOR, KVER_MINOR initialized at top level
               BEFORE kernel-dependent code. Non-numeric fallback to 0.
               Regression: 0 inline uname -r outside init.
       [ ] 171b. Kernel-gate ordering: `_ry_check_kernel_version` function
               definition line < first `$KVER_MAJOR` / `$KVER_MINOR` consumer
               line. Comparator in =KVER GATE ORDERING= section; on
               unsupported kernels the gate must have been declared before
               any `test $KVER_MAJOR -ge 6` can run.

  14.2 RETURN-VALUE HELPERS (v3.1.0)
       [ ] 172. _ntsync_state: 5 states via stdout. All callers capture local.
       [ ] 173. _get_boot_time + _find_pacnew_files retired: both removed
               in later versions (`rg '_get_boot_time|_find_pacnew' → 0`).
               Boot-time logic inlined v3.48.2; pacnew enumeration moved
               to inline `sudo -n find` with null-delim split0 (v4.1.12).
               Regression: any new hit on either symbol is a re-introduction.

  14.3 VERIFICATION HELPERS (v3.1.0)
       [ ] 174. _verify_unit_syntax: --user auto-detect via path match.
               Handles missing systemd-analyze with _warn + return 0.
       [ ] 175. _check_service_active_enabled removed: rg → 0. _parse_systemctl_show
               also removed in later versions (rg → 0). Service checks now use
               direct inline `systemctl is-enabled <unit>` or the v4.4.27
               `_unit_state` helper (rg `_unit_state` → ≥7).

  14.4 HELPER DEDUP + REGRESSIONS (v3.1.0)
       [ ] 176. 0 orphaned inline versions: no inline uname -r (outside
               KVER init), no inline sudo find pacnew (helper uses
               sudo -n for non-interactive operation), no
               NTSYNC_SUPPORTED remnants.
       [ ] 177. sp5100_tco + btusb removed: not in MODPROBE_BLACKLIST,
               no cmdline params, no udev rules. rg → 0 functional hits
               (inline comments documenting removal are exempt).
               v3.4.0: do_clean + _find_steam_root removed, --no-log flag
               removed, --no-color flag removed, _GPU_BUSY_PCT removed.
               v3.5.0: backup system removed (_backup_init, _backup_path,
               _backup_boot, MAX_BACKUPS, BACKUP_BASE, BACKUP_DIR,
               do_rollback). rg → 0 hits for all.
               v3.38.0: amdgpu-performance.service removed (kernel default).
               v3.41.0: wdat_wdt removed from module_blacklist (redundant
               with nowatchdog). UDEV_RULES removed (ntsync-common).
               v3.43.0: VKD3D_CONFIG=transfer_queue removed (now default).
               rg → 0 functional hits for each.
       [ ] 178. power-profiles-daemon: in MASK array, NOT in PKGS_DEL.
       [ ] 179. Hook validation: --existence-only for installed config;
               full order validation for embedded content only.

  14.5 SYSCTL.D INTEGRATION (v3.2.0)
       [ ] 180. 99-cachyos-sysctl.conf in SYSTEM_DESTINATIONS (11th entry).
               _RY_MANAGED_FILE_COUNT runtime-derived since v4.5.4 via
               `set -g _RY_MANAGED_FILE_COUNT (count $SYSTEM_DESTINATIONS $USER_DESTINATIONS $SERVICE_DESTINATIONS)`
               — no hardcoded literal. Current resolved value: 15
               (SYSTEM 11 + USER 3 + SERVICE 1). Regression detector:
               rg '_RY_MANAGED_FILE_COUNT \(count \$SYSTEM' → ≥1.
               _ry_get_file_content case matches all 15.
       [ ] 181. _ry_verify_static + _ry_verify_runtime iterate `$SYSCTL_VALUES`
               (`for entry in $SYSCTL_VALUES`) at ≥3 sites (validate path,
               verify-static, verify-runtime). Runtime verify reads /proc/sys/
               with cat -- 2>/dev/null. No hardcoded key count.
       [ ] 182. _install_configure: sysctl --system reload gated by file
               check + _ask. _ry_do_install_file: string match sysctl.d → reload.

  14.6 AUDIT FIXES (v3.1.1–v3.2.10)
       [ ] 183. _install_finalize returns 1 when INSTALL_HAD_ERRORS=true.
       [ ] 184. `cat --` terminators in _run capture paths RETIRED
               (v6.2.13 _run 5-helper split). Capture now flows through
               `_run_emit_stream` which uses fish-native operations (no
               external `cat`). Regression: _py_extract _run | rg 'cat --'
               → 0 hits; _py_extract _run_emit_stream | rg 'cat --' → 0 hits.
               Original v3.1.1-era defense (cat-with-end-of-options to block
               leading-dash filename injection) is moot now that input flows
               through capture-tmpfile redirection (`2>"$stderr_tmp"`), not
               through cat's argv.
       [ ] 185. --description + comment coverage: function count = 246 =
               description count (100% --description coverage; verified via
               `rg -c '^function ' == rg -c -- '--description'`). Section
               markers: 2 top-level `# UPPERCASE LABEL` blocks
               (LOGGING, MESSAGE OUTPUT, AND VERIFICATION COUNTERS;
               INSTALL PIPELINE). PROFILE LOADER banner retired with
               profile system v4.5.0; CLI ARGUMENT PARSING AND DISPATCH
               banner absorbed into trailing dispatch block (no banner).
               Box-drawing `# ── X ──` style from v3.2.6 era retired.
               GTR9_PRO BUILT-IN DEFAULTS banner collapsed to single-line
               (v4.6.20). Function count drove from 137 (v4.5.8) to 200
               (v4.5.34 split intermediate) to 239 (post-v4.5.34) to
               246 (v5.0.1 — additional v4.6.x splits).

  14.7 ORPHAN LOG + RACE FIXES (v3.2.2–v3.2.3)
       [ ] 186. Orphan log cleanup: every exit 2 preceded by LOG_FILE guard.
               Logging is always-on (v3.4.0: --no-log removed).
       [ ] 187. Footer race: _FOOTER_WRITTEN set BEFORE printf.
               Log rotation unconditional (v3.4.0: rm -f idempotent,
               concurrent race yields at most one extra log surviving).
       [ ] 188. LC_ALL=C on log rotation sort.

  14.8 SIGPIPE + BOOT SAFETY (v3.2.5)
       [ ] 189. _cleanup_pipe (v4.6.16 NON-FATAL inversion): --on-signal PIPE,
               sets `_RY_OUTPUT_BROKEN=true`, logs `SIGPIPE_RECEIVED: stderr/stdout
               consumer closed; continuing with JSONL log only`, returns 0.
               Does NOT exit 141, does NOT call _do_cleanup, does NOT write
               JSONL footer. _msg_print / _err_loud / _echo short-circuit on
               `set -q _RY_OUTPUT_BROKEN; and return 0` to avoid EPIPE re-entry.
               JSONL log is canonical. _ry_show_help and fish_exit handler do
               NOT enumerate 141 (v4.6.17 dropped the row from help/README).
               Regression checks:
                 _py_extract _cleanup_pipe | rg 'exit 141' → 0 hits.
                 _py_extract _cleanup_pipe | rg 'set -g _RY_OUTPUT_BROKEN' → 1 hit.
                 rg 'set -q _RY_OUTPUT_BROKEN; and return' source-wide → ≥2 hits.
       [ ] 190. _validate_kernel_params: cmdline param→CONFIG_ map,
               reads /proc/config.gz, graceful skip if unavailable.
               Called from _ry_verify_runtime.
       [ ] 191. _preflight_boot_sanity: vmlinuz + initramfs non-zero,
               boot entry refs existing kernel. Failure →
               INSTALL_HAD_ERRORS + "DO NOT REBOOT".

  14.9 FEATURE EXTRACTION (v3.2.5–v3.7.13)
       [ ] 192. _btrfs_pre_snapshot removed (ext4-only system): 0 references.
               rg '_btrfs_pre_snapshot|\.snapshots|ry-snapshots' → 0 hits.
       [ ] 193. Dead globals removed: MODULES_LOAD, _LOCK_METHOD,
               _GPU_BUSY_PCT → 0 hits.
               find -mindepth 1 (not glob). _log: direct date.
       [ ] 194. String match -- separator: outside quoted string as Fish
               end-of-options marker in _ry_verify_runtime cmdline check.

  14.10 CODE QUALITY (v3.2.6–v3.2.10)
       [ ] 195. Section headers: 2 top-level `# UPPERCASE LABEL` blocks
                (`LOGGING, MESSAGE OUTPUT, AND VERIFICATION COUNTERS`;
                `INSTALL PIPELINE`). Legacy `# ── Description ──` box-drawing
                markers (v3.2.6 convention) fully retired; current style
                is unadorned uppercase capsule comments. PROFILE LOADER
                banner retired with v4.5.0 profile-system removal.
                rg '^# [A-Z][A-Z ,]{5,}$' → 2.
       [ ] 196. Function comment coverage: 253 functions at v7.3.5
                (v5.0.1 baseline 246). --description coverage 235/253 (≈93%);
                the ~18 functions without --description are one-line emit
                helpers (`_ok`, `_fail`, `_warn`, `_info`, `_err`,
                `_fail_silent`) and the 12 `_content_*` generators (dispatched
                by name via `_tmpfile_key`, descriptions on the dispatcher
                instead). A free-form `# ...` comment within 2 lines above
                the function header is conventional but not universal.
       [ ] 197. `command` prefix on ≥80 coreutils calls (v7.3.5 baseline ~197
                across rm/mv/chmod/chown/mkdir/cat/sed/grep/awk/find/pacman/sleep/stat;
                v5.0.1 was ~95 — v6.5/v7.0 added 68+ command-prefix conversions
                across system-cmd invocations to shadow-immunize against
                autoloaded user functions).
                No bare coreutils outside sudo / message-function context.
                v4.6.11 added `command` prefix to 8 read-only `pacman -Q*` /
                `-T` / `-Qi` invocations for shadow-immunity against autoloaded
                `pacman.fish`; rg 'command pacman' → ≥8 hits.
       [ ] 198. --argument-names on ≥40 functions (v7.3.5 baseline ~84;
                v5.0.1 was ~87 — slight decline reflects v6.0 reduction
                folding some single-purpose helpers back into call sites,
                offset by v6.x/v7.x re-expansion. Drift indicator only,
                not a pass gate).

  14.11 SCOPING + ROLLBACK (v3.2.9–v3.2.10)
       [ ] 199. In-function set -g: ~108 (intentional VERIFY_*/flag globals;
                v5.0.1 baseline 108 — down from v4.5.8 ~117 reflecting helper
                extraction making more state explicit-locals). Cross-block bare
                set: ~115 (v5.0.1 baseline 115). The v4.5.34 19-orchestrator
                split + v4.6.x splits drove additional reductions on top of
                the v4.5.0 profile-system removal. Ratios drift ±15%
                release-to-release; absolute counts are trend indicators,
                not pass gates.

  14.12 LOGGING + VERIFICATION (v3.2.8–v3.2.10)
       [ ] 200. _log section markers on surviving modes. v4.5.8 format:
               `_log "=== $name ==="` (single-line, dynamic interpolation,
               no START/END suffixes). Three call sites in current source:
               one parametric site inside the verify driver (passes mode
               name as `$name`), and two literal sites inside install
               (`=== INSTALL SYSTEM FILES ===`, `=== INSTALL USER FILES ===`).
               Expected hit count: rg '_log "=== ' → ≥3. The earlier
               `=== MODE START ===` / `=== MODE END ===` paired-marker
               convention from v4.1.4-era was retired; do not regress.
       [ ] 201. In-code section markers organize major subsystems
                (logging/counters, install pipeline, verification modes,
                signal/cleanup chain, tmpfile lifecycle, parallel batch jobs).
                Style is uppercase-led capsule comments immediately above the
                subsystem functions (no box-drawing). Exact count
                drifts release-to-release; the invariant is readability,
                not a fixed number. Profile-loader subsystem retired v4.5.0
                (gtr9_pro defaults inlined; see check 232).
       [ ] 202. Post-write sha256: _ry_install_file verifies destination hash
               after atomic mv. Failure → INSTALL_HAD_ERRORS.
       [ ] 203. WiFi credentials: set before use, erased after iwd config.
               Not stored as globals beyond pipeline.
       [ ] 204. Unconditional log rotation for all modes. Scope invariants:
                set -l for true locals, bare set for cross-block updaters,
                set -g only for intentional globals. v5.0.1 baselines:
                ~464 set -l, ~115 bare set, ~108 in-function set -g.
                _run wraps ≥40 commands for logging (v7.3.5 baseline ~48;
                v5.0.1 was ~52 — v6.0 reduction consolidated several `_run`
                call sites into helper functions, offset by v6.x/v7.x
                additions in _csp_filter_rdeps / _install_aur_packages /
                _post_* hooks).
                These are drift indicators (±15% normal between releases),
                not pass gates — sudden large deltas warrant investigation.

  14.13 STDOUT + SECURITY (v3.2.10)
       [ ] 205. All UI output → stderr. Intentional stdout: return-value
               functions, _ry_get_file_content, _ry_show_help.
       [ ] 206. SSID/credential pathway RETIRED (v6.0 reduction). The
               v3.2.10 "SECURITY INVARIANT at _run" comment and WIFI_SSID /
               WIFI_PASS globals are absent from v7.3.5 — Wi-Fi credential
               collection was removed when the script narrowed to
               unattended-install scope. All current `_run` callers pass
               hardcoded commands with at most `DESTINATIONS` file paths;
               there is no runtime user-input that reaches argv.
               Regression: rg 'WIFI_SSID|WIFI_PASS' → 0 hits; rg
               'SECURITY INVARIANT' → 0 hits.

  14.14 DISPATCH + EXIT CODE FIXES (v3.2.11–v3.2.13)
       [ ] 207. MODE variable: bare set (not set -g on locally-declared).
               Dispatch routes to correct mode handler. 0 set -g MODE
               inside functions. Regression: all modes ran install path.
       [ ] 208. exit_code variable: bare set (not set -g on locally-declared).
               All 14 mode exit paths propagate correctly. NDJSON footer
               exit_code accurate. Regression: every mode exited 0.
       [ ] 209. _has_lock variable: bare set. Lock released on exit for
               write modes. Regression: lock never released, blocked
               subsequent runs until stale lock cleared.
       [ ] 210. mode_label variable: bare set. Log filenames include
               modifier suffixes (-dry, -fix, -all, sub-target).

  14.15 SCOPE REMEDIATION (v3.3.0–v3.3.7)
       [ ] 211. 186 set -l → bare set across 49 functions (124 in v3.3.0 +
               19 in v3.3.2 + 28 in v3.3.5 + 15 in v3.3.7).
               Note: --lint mode removed v3.48.20 — scope shadow detection
               now relies entirely on GROUP D python3 scope-stack detector;
               no script-side checker remains.
       [ ] 212. Cross-block vars use bare set in surviving functions
                (removed modes excluded): _ry_verify_runtime (perms),
                _ry_validate_configs (errors), _progress (bar/desc),
                _preflight_boot_sanity (errors),
                _install_configure_services (to_del),
                _ry_install_file (_cur_content),
                _ry_do_install_file (target resolution state),
                _install_finalize (reload_wait, wifi_retry).
                NOTE: _ry_do_diff/_ry_do_lint/_ry_do_test_all function
                examples from v3.3.x audit era retired with those modes
                (v3.48.20 + v4.1.0); _install_collect_wifi inlined into
                preflight path.
       [ ] 213. v7.3.5 scope baselines: in-function set -g ~135
                (v5.0.1 was ~108 — RISEN as v6.x/v7.x helper extractions
                widened the explicit-globals surface for cross-function
                state like _RY_BOOT_TAINTED, _RY_HEADER_WRITTEN,
                _RY_LOCK_DIR_OWNED). Bare set ~127 (v5.0.1 ~115; -2 vs v7.3.3 ~129 from v7.3.5 idiom-tightening). set -l
                ~478 (v5.0.1 ~464; +1 vs v7.3.3 ~477 from v7.3.5 bootstrap-loop locals; cross-version helper-extraction trend
                continues).
                Driver narrative: v4.5.0 profile-system removal drove the
                v4.1.8→v4.5.8 drop (set -l ~590→379, bare set ~193→151,
                set -g ~130→117); v4.5.31/v4.5.34/v4.6.x helper extractions
                drove the v4.5.8→v5.0.1 redistribution (more set -l from
                helper-local state; bare set ~151→115; set -g ~117→108).
                Ratios matter more than absolute counts (see check 204).
                Regression signal: set -g count rising sharply without new
                global introductions in changelog.
       [ ] 214. _analyze_log removed: 0 hits. _emit_step_time helper also
               removed in later versions (rg → 0); step timing now logged
               inline at relevant phase boundaries.

  14.16 COMPLETIONS VALIDATION FIX (v3.4.1) — RETIRED
       NOTE: This subsection described the v3.4.1 fix for --completions
       stdout-capture vs file-write mismatch in _ry_do_test_all. Both
       --completions and --test-all modes were removed in v4.1.0 — the
       entire validation path no longer exists. See checks 357 and 358
       for the current 0-hit regression verification.

  14.17 DECOMPOSITION + CLEANUP (v3.5.0–v3.7.13)
       [ ] 254. _gkeyfile_escape: consolidated WiFi GKeyFile escaping
               (backslash, semicolon, leading #, leading/trailing space).
               NM write path calls helper instead of inline escaping.
       [ ] 255. Dead function removal (v3.7.13): _read_installed,
               validate_systemd_unit, validate_fish_script → 0 hits.
               _run --passthrough dead code path → 0 hits.
       [ ] 256. do_logs removal: _logs_file_ops, _logs_journal, _logs_list,
               do_logs → 0 functional hits. Entire --logs mode and
               decomposed helpers removed. rg regression check.
       [ ] 257. `path basename` regression: 0 hits. Replaced with
               `string replace -r '.*/' ''` for Fish 3.3+ compat.
       [ ] 258. do_logs fuzzy-match removal: _t_prefix, proximity,
               "Did you mean" → 0 hits (removed with do_logs).
       [ ] 259. Backup system removal: 0 hits for _backup_, BACKUP_BASE,
               BACKUP_DIR, MAX_BACKUPS, do_rollback. No stale comments
               or _warn lines referencing removed backup directory.
       [ ] 260. Bare sudo nmcli: 0 bare `sudo nmcli` outside _run. All
               state-changing sudo calls wrapped in _run for audit trail.
       [ ] 261. set -l next_arg / argparse=VALUE pattern in --install-file
               arg consumption (only surviving arg-taking flag post-v4.1.0).
               Consistent with argparse `install-file=` spec syntax.
       [ ] 262. Inline comment density: all 26 functions >50 lines have
               ≥2 inline comments (v3.5.0 decomposition reduced count from
               34 to 27; v3.7.x batch/parallel additions grew it to 33).
               27 comments added in v3.5.0 to 19 previously under-documented
               functions. v3.5.1: 5 more section comments in
               _ry_validate_configs (3) and _install_configure_services (2).

  14.18 DEAD CODE + LOG GUARD (v3.5.1)
       [ ] 263. _has_lock variable removed: 0 hits for _has_lock. Lock
               release uses set -q LOCK_DIR (not _has_lock). Regression
               check after dead code removal (v3.5.1 F-1).
       [ ] 264. _log guard: test -f "$LOG_FILE" || return 0 at entry.
               Prevents orphan log recreation after _acquire_lock
               deletes LOG_FILE on contention. Verified: lock
               contention test → 0 orphan stubs (v3.5.1 F-2).
       [ ] 265. _ry_validate_configs: ≥5 inline comments (3 section markers
               added v3.5.1: systemd units, INI configs, simple configs).
       [ ] 266. _install_configure_services: ≥4 inline comments (2 section
               markers added v3.5.1: LVM mask filter, ssh-agent user svc).

  14.19 STRUCTURAL IMPROVEMENTS (v3.6.2–v3.6.4)
       [ ] 267. flock(1) atomic lock reclaim in _acquire_lock (v3.6.3):
               flock -n -E 5 on parent dir wraps rmdir+mkdir+write;
               falls back to PID-re-verify when flock absent. rg 'flock'
               → ≥10 hits in _acquire_lock context.
       [ ] 268. _ROOT_UUID cached once in `_ir_resolve_root_uuid`
               (v4.5.0 inlined replacement for the retired `_load_profile`
               chain): `findmnt -no UUID /` called once, validated against
               UUID-shape regex `^[0-9a-fA-F]{8}-[0-9a-fA-F]{4}-...$`,
               stored as `_ROOT_UUID` global. Failure path runs mode-aware
               error (silent for --check, _err_loud for install/verify modes)
               then `_pre_dispatch_exit $EXIT_PREFLIGHT`.
               `_content__etc_kernel_cmdline` reads cached `$_ROOT_UUID`;
               returns `$EXIT_GEN_NOUUID` (11) if empty.
               Regression: rg '^function _ir_resolve_root_uuid' → 1 hit;
               rg '_load_profile' → 0 hits (retired v4.5.0);
               0 inline `findmnt -no UUID` in `_content_*` generators.
       [ ] 269. _kconfig_cache lazy loader (v3.6.3): reads /proc/config.gz
               once on first call, caches in _KCONFIG_DATA global.
               _ntsync_state and _validate_kernel_params use cache.
       [ ] 270. Fish version gate 3.4+ (v3.6.4): string join0 requires
               3.4+. Gate, error message, upper-bound warning updated.
               Regression: no 3.3 references in gate logic.

  14.20 BATCH & PARALLEL PROCESSING (v3.7.0)
       [ ] 271. `_pregenerate_content_files` RETIRED (v6.0 reduction). Pre-
               generation of expected-content tmpfiles for parallel consumers
               was removed alongside the parallel-children + sudo-keepalive
               infrastructure. Current verify path is sequential: each `dst`
               iteration calls `_ry_content_bytes "$dst"` inline (function
               dispatch via `_tmpfile_key` → `_content_*` generator). No
               $val_dir / $content_dir pre-populated.
               Regression: rg '_pregenerate_content_files' → 0 hits (retired);
               rg '_ry_content_bytes' → ≥3 hits (inline replacement).
       [ ] 272. Parallel patterns: all batch jobs use wait after fork;
               deterministic output ordering via result files; sudo cached
               once via _ensure_sudo_cached before forking. No bare sudo
               in parallel children.
       [ ] 273. _TRACKED_TMPFILES lifecycle: initialized as empty global,
               set -ga accumulates paths from mktemp calls and pre-generate
               dirs. _do_cleanup iterates all paths with rm -rf, then
               set --erase _TRACKED_TMPFILES. 0 leaked tmpfiles after
               cleanup on every exit path (normal, signal, error).
       [ ] 274. _verify_summary counter integrity: VERIFY_FAIL/WARN/OK
               counters snapshotted into locals before _fail/_warn/_ok call
               to prevent N+1 off-by-one. VERIFY_MODE=false before summary
               emission so _msg increment is skipped. CI line, stderr, and
               JSONL footer all agree on counts.
       [ ] 275. _ry_do_check Job 4 file-based serialization: EXPECTED_SERVICES
               and MASK read from $result_dir files instead of interpolated
               into fish -c string. Prevents breakage from external profiles
               with quotes/backslashes in service names.
       [ ] 276. string join0 | xargs -0 (B-9 log rotation): null-terminated
               pipeline, -- before args in both string join0 and xargs.
               Requires Fish 3.4+ (already gated by check 270).
       [ ] 277. _parse_systemctl_show: handles empty input (returns nothing),
               multi-record blank-line separator, and missing fields.
               Callers (B-8 _ry_verify_runtime, B-3 _ry_do_check) validate output
               before using parsed records.
       [ ] 278. Parallel result files: each parallel child writes to unique
               path ($dir/$phase.{ext}), parent reads in deterministic loop
               order after wait. Missing result file → graceful skip or
               safe default (test -n guard on read).

  14.21 v3.7.13 FIXES (10 checks)
       [ ] 279. _ry_check_kernel_version returns 1 on kernel < 6.14 (was silently
               returning 0). Callers (_install_preflight) capture failure,
               set INSTALL_HAD_ERRORS, and warn. Regression: 0 code paths
               where < 6.14 gate silently passes.
       [ ] 280. PROGRESS_STEPS array retired v4.1.9; phase labels now passed inline at each `_progress <phase>` call site. 6-phase model preserved: Preflight → Packages → Configuration → Services → Boot → Finalize. `_progress` is called once per phase (≈6 sites; verify via `rg -oP '^\s+_progress\s+\K[A-Z][a-zA-Z]+' | sort -u`). syu-before-mkinitcpio ordering (v3.7.13) preserved inside the Boot phase via `_install_rebuild_boot` (see check 287). Regression detector: rg `PROGRESS_STEPS|PROGRESS_TOTAL` → 0.
       [ ] 281. B-5 _ry_do_test_all nested parallelism guard: on systems with
               < 8 cores (nproc), all 14 read-only modes run sequentially
               instead of parallel forking. 8-15 cores: batch throttle in
               groups of nproc with wait between batches. 16+ cores: full
               parallel. Prevents oversubscription from nested parallel
               children (verify-static: 17, check: 4, diff: 17 forked jobs).
       [ ] 282. B-1 _ry_validate_configs ssh-agent user unit: systemd-analyze
               --user verify for ssh-agent.service. Previously ran without
               --user flag (system scope verify on user unit).
       [ ] 283. B-3 _ry_do_check dead variable: unused `set -l idx 0` in Job 4
               inline parser removed. rg 'idx 0' in do_check → 0 hits.
       [ ] 284. _ensure_sudo_cached consistent usage: _ry_verify_static and
               _ry_do_test_all use _ensure_sudo_cached helper instead of ad-hoc
               `sudo true`. All parallel-forking modes use same pattern.
               rg '_ensure_sudo_cached' → ≥4 call sites.
       [ ] 285. B-7 batch enable per-unit fallback: _install_configure_services
               in --all mode retries each unit individually on batch
               `systemctl enable --now` failure. Identifies which unit failed.
       [ ] 286. _manifest_write gated on EXIT_BOOT_CRIT: _ry_do_install skips
               manifest write on boot-critical failure to avoid recording
               partially-deployed state. rg 'MANIFEST_SKIP.*boot' → 1 hit.
       [ ] 287. Syu before mkinitcpio in _install_rebuild_boot: system upgrade
               runs before initramfs rebuild so new kernels are present for
               mkinitcpio -P. SYSTEM_UPGRADED check prevents double-upgrade.
               Pacman hooks provide backup coverage.
       [ ] 288. Log rotation flock serialization: xargs rm wrapped in
               flock -n when flock available. Eliminates last-write-wins
               race between concurrent read-only instances. Falls back
               to bare xargs when flock absent.

  14.22 v3.7.14+ REGRESSION FIXES (9 checks)
       [ ] 289. fish -c injection safety: 0 double-quoted fish -c bodies
               with variable interpolation (rg 'fish -c "' → 0 hits).
               All 13 fish -c calls use single-quoted bodies with $argv
               positional args via -- separator. Regression from v3.7.19
               (6 sites) + v3.7.21 (6 sites) + v3.7.14 (1 site).
       [ ] 290. Fish brace expansion: 0 {$var} patterns (rg '{\$' → 0).
               String concatenation used instead. Regression from v3.7.17
               _ry_do_check false-positive drift.
       [ ] 291. Parallel status capture: 0 bare `echo $status` in parallel
               children (rg 'echo \$status' excluding comments → 0). All
               use `set -l rc $status; printf '%d\n' $rc`. Regression
               from v3.7.21.
       [ ] 292. Fish glob cancellation: 0 `count "$dir"/*` patterns that
               fail on empty directories. Find-based count with numeric
               validation used instead. Regression from v3.7.20.
       [ ] 293. wait guard: all wait $pids calls on potentially-empty pid
               lists preceded by `count $pids` guard. Prevents blocking
               on unrelated background jobs (sudo keepalive). Named PIDs
               (unconditionally forked) exempt. Regression from v3.7.15.
       [ ] 294. Parallel worker cap: all parallel-forking functions use
               min(N, nproc) bounded workers. _ry_verify_static: min(4, nproc).
               _ry_do_diff: nproc batch throttle. _ry_do_test_all: 3-tier
               (nproc<8 sequential, 8-15 batch, 16+ full). Regression
               from v3.7.15 (_ry_verify_static), v3.7.17 (_ry_do_diff),
               later _ry_verify_static nproc cap.
       [ ] 295. _ry_install_file post-write symlink re-check: test -L after tee
               and before chmod/mv in both sudo and user paths (4 total
               check sites). Closes TOCTOU between pre-write guard and
               write. Regression from _ry_install_file refactor.
       [ ] 296. Deprecated test operators: 0 `test EXPR -a EXPR` /
               `test EXPR -o EXPR` usage (rg 'test [^;]+ -[oa] [^;]+' → 0
               — tightened regex; the previous `test .* -o |test .* -a `
               form produced false positives on `test ...; and set -a` /
               `; or set -a` lines where the trailing `-a` belongs to
               `set --append`, not `test`). Fish idiomatic "; or" /
               "; and" used instead. Regression from v3.7.21.

       [ ] 297. _find_pacnew_files uses sudo -n (non-interactive): no
               credential prompts in dry-run or read-only contexts.
               rg 'sudo -n' in _find_pacnew_files body → 1 hit.
               rg 'sudo [^-]' in _find_pacnew_files body → 0 hits.
               Regression from v3.7.32.

  14.23 FISH 4.0–4.6 COMPATIBILITY (7 checks)
       NOTE: Gate on detected Fish version. Fish 4.0 (2025-02) is a Rust
       rewrite with breaking changes. These checks apply only when auditing
       scripts targeting Fish 4.x or when the audit environment runs Fish 4.x.

       [ ] 298. qmark-noglob: Fish 4.0 enables qmark-noglob by default.
               Bare ? in glob patterns is treated as literal, not single-char
               wildcard. rg '\?' in glob context → flag for 4.0+ compat.
       [ ] 299. bind -k removal: Fish 4.1 fully removed bind -k terminfo
               key names. rg 'bind -k' → 0 hits required on 4.1+ targets.
               Replacement: Fish key notation (e.g., bind ctrl-right).
       [ ] 300. commandline --tokenize deprecated: Fish 4.0 deprecated
               --tokenize/-o in favor of --tokens-expanded/-x.
               rg 'commandline.*--tokenize|commandline.*-o ' → 0 hits.
       [ ] 301. %self deprecated: Fish 4.0 feature flag remove-percent-self.
               rg '%self' → 0 hits. Replacement: $fish_pid.
       [ ] 302. test -n arg requirement: Fish 4.0 test-require-arg flag
               changes unquoted test -n $var behavior. Bare test -n $var
               (without quotes) is now a bug. rg 'test -n \$[a-z]' → 0 hits.
       [ ] 303. fish_color_* scope change: Fish 4.3 changed fish_color_*
               from universal to global scope. set -U fish_color_* in
               config is now ineffective. rg 'set -U fish_color' → 0 hits.
       [ ] 304. Brace compound commands: Fish 4.1 added { } compound
               commands. {echo,echo} is no longer brace expansion.
               Scripts relying on brace expansion with single-word items
               adjacent to { } blocks may break.

  14.24 v3.10.0–v3.46.0 CHANGES (29 checks)
       [ ] 305. _install_fstab_opts: atomic fstab modification. Reads
               /etc/fstab, adds noatime,lazytime to ext4 entries via sed,
               writes to tmpfile, validates with findmnt --verify, then mv.
               No data loss on failure. Idempotent (skip if already present).
       [ ] 306. SYSCTL_VALUES global: 16 entries cross-ref with
               99-cachyos-sysctl.conf content in _ry_get_file_content / _content_*
               sysctl helper. Runtime verify iterates the array (`for entry in
               $SYSCTL_VALUES` at ≥3 sites: validate, verify-static, verify-runtime).
               Whitespace normalization for multi-value keys (tcp_rmem, tcp_wmem)
               via `string split -m1 '='`. Count enforced at preflight via
               `_ir_validate_counts` invariant `SYSCTL_VALUES:16`.
       [ ] 307. irqbalance.service RETIRED from MASK (v4.0.0): originally
               added to MASK in v3.10.0-era to prevent cache-thrashing with
               threadirqs, removed v4.0.0 after upstream kernel irq affinity
               improvements made masking unnecessary on the target platform.
               threadirqs kernel param retained. Retained as regression
               detector only — see check 348 for the authoritative 0-hit
               assertion (rg 'irqbalance' in MASK context → 0).
       [ ] 308. amdgpu-performance.service removal (v3.38.0): 0 references
               in SERVICE_DESTINATIONS, EXPECTED_SERVICES,
               _ry_get_file_content, _ry_verify_static, _ry_verify_runtime.
               parsed[] index shift verified (6→5 for runtime).
       [ ] 309. wdat_wdt removal from module_blacklist (v3.41.0): 0
               references. Redundant with nowatchdog kernel param.
       [ ] 310. ZRAM check flip: systemd-zram-setup@zram0.service unmasked
               (v3.41.0). Verify runtime check expects ZRAM active/enabled,
               not masked. ZRAM device and swap checks added.
       [ ] 311. EXIT_PREFLIGHT(3) for --check infra failures (v3.37.0):
               sudo unavailable, content pregeneration, mktemp failures
               return 3 not 10. CI can distinguish prereq from drift.
       [ ] 312. _manifest_write gate (v3.37.0 origin, migrated in v4.1.0):
               call site moved out of --diff --fix (both removed) into
               _install_finalize success path. v4.1.2 decoupled from
               INSTALL_HAD_ERRORS — failure logs MANIFEST_WRITE_FAILED only.
               rg '_manifest_write' → finalize call + helper definition.
       [ ] 313. VERIFY_MODE lifecycle (v3.37.0 origin, migrated in v4.1.0):
               --diff removed; VERIFY_MODE now scoped to --verify-static
               and --verify-runtime. Set true at entry with counter reset,
               false before return. JSONL footer reports accurate
               pass/fail/warn for verify runs.
       [ ] 314. sudo -n convention expansion (v3.37.2): 4 bare sudo cat →
               sudo -n cat in _atomic_write_file (2), _ry_install_file (1),
               _ry_verify_static (1). All read-only sudo uses sudo -n.
       [ ] 315. IPv6 method=auto in .nmconnection (v3.44.0): method=disabled
               breaks IPv6-only networks. method=auto is NM default.
       [ ] 316. SSID '&' in forbidden characters (v3.44.0): _run metachar
               rejection blocks '&' but SSID validation must also reject.
               Help text and _info messages updated.
       [ ] 317. '--' separator in grep patterns (v3.44.0): defense-in-depth
               for blacklisted modules and coredump config checks.
       [ ] 318. set -q + test -n TMPDIR guard (v3.40.0): set -q succeeds
               for set-but-empty. Fallback sweep needs test -n to avoid
               find "" skipping ry-* tmpfile cleanup.
       [ ] 319. string collect before \n escape in _json_str (v3.14.0):
               4 pre-\n-escape calls. Without string collect, multi-line
               input silently loses newlines in JSONL logs.
       [ ] 320. _emit_step_time helper (v3.40.0): replaces 3 identical
               7-line step timing blocks. Single call site in _progress,
               _progress_skip, _progress_done. -10 lines.
       [ ] 321. Parallel child stderr capture 15 lines (v3.14.0):
               6 sites expanded from 5→15 lines. Diagnostic warnings
               visible in terminal via _warn (v3.37.0).
       [ ] 322. LVM-aware mask verification (v3.14.0): pre-filter MASK
               for LVM services before batch enable/disable check.
               lvs command availability gated.
       [ ] 323. Parallel validation child stderr to terminal (v3.37.0):
               first 5 lines of systemd-analyze/fish diagnostic warnings
               displayed via _warn instead of JSONL-only.
       [ ] 324. All SYSTEM_DESTINATIONS and case patterns quoted (v3.37.2):
               16 destinations + case branches consistently quoted.
               coredump.conf.d/sysctl.d destinations present (v3.32.0+).
       [ ] 325. preempt=full runtime-only verification (v3.45.0 added to
               KERNEL_PARAMS; retired from the array by v4.0.x when the
               script narrowed its target to linux-cachyos, which defaults
               to full preempt). Runtime verify greps 'Dynamic Preempt: full'
               in dmesg; if absent, _warn advises adding preempt=full to
               cmdline for non-cachyos kernels. NOT in KERNEL_PARAMS array.
               rg '^\s+preempt=full' in KERNEL_PARAMS block → 0.
               rg 'Dynamic Preempt' → ≥1 hit (runtime warn helper).
       [ ] 326. vm.page_lock_unfairness=1 removal (v3.45.0): 0 references
               in SYSCTL_VALUES, _ry_get_file_content sysctl section,
               and /proc/sys/ runtime checks. Kernel default is optimal.
       [ ] 327. netdev_max_backlog value 300000→16384 (v3.45.0): verify
               SYSCTL_VALUES entry matches _ry_get_file_content output.
               Runtime verify reads /proc/sys/net/core/netdev_max_backlog.
       [ ] 328. somaxconn=8192 RETIRED from SYSCTL_VALUES (v4.0.0): originally
               added v3.45.0, removed v4.0.0 alongside unprivileged_bpf_disabled,
               inotify.max_user_watches, page_lock_unfairness. Retained as
               regression detector only — see check 344 for the authoritative
               0-hit assertion (rg 'somaxconn' → 0 functional).
       [ ] 329. sysctl.conf header priority-99 override note (v3.45.0):
               _ry_get_file_content sysctl case includes comment
               "priority 99 — overrides" in generated header.
       [ ] 330. string split -m1 '=' in sysctl content and verify
               (v3.46.0): prevents splitting on values containing '='.
               3 call sites: _ry_get_file_content sysctl loop,
               _ry_verify_static env check, _ry_verify_runtime sysctl.
       [ ] 331. noatime dedup in _install_fstab_opts (v3.46.0): checks
               existing noatime before appending. Prevents double
               noatime on re-runs. string match -q '*noatime*' guard.
       [ ] 332. usbhid.mousepoll=1 removal (v3.46.0): 0 references in
               KERNEL_PARAMS. Current KERNEL_PARAMS count: 15 (see check
               343 for the v4.0.0 rework net delta).
       [ ] 333. coredump.conf.d semantic checks in --verify-static
               (v3.46.0): Storage=none and ProcessSizeMax=0 verified
               against deployed file content. Catch silent regression
               if conf.d file overwritten by package update.

  14.25 v3.47.0–v3.51.15 CHANGES (9 checks)
       [ ] 334. --lint mode removal (v3.48.20): _ry_do_lint, --lint flag,
                --no-lint → 0 functional hits. Exit code 11 retired.
                Deprecated-flag handler emits usage error on --lint.
                rg '_ry_do_lint|--no-lint' → 0 hits; rg 'exit.*11\b|EXIT_LINT' → 0.
       [ ] 335. _get_boot_time inlined (v3.48.22): 0 hits for the helper.
                _progress_skip merged into _progress (v3.48.22): 0 hits.
                _ry_count_managed_cases replaced by compile-time constant
                _RY_MANAGED_FILE_COUNT (v3.51.12): rg '_ry_count_managed_cases' → 0.
       [ ] 336. Source-safety subsystem (v3.48.20): _ry_exit helper,
                _RY_INSTALL_BAILING, _RY_INSTALL_SOURCED, _RY_INSTALL_LAST_EXIT
                globals. Every top-level exit wrapped as
                `_ry_exit $CODE; and return $CODE; or return $CODE`.
                Signal handlers gate `exit` behind _RY_INSTALL_SOURCED check.
       [ ] 337. _ry_namespace_cleanup (v3.51.8): erases every script-set
                `set -g` on sourced runs, preserves caller HOME (v4.0.2 fix).
                Called unconditionally from _ry_exit (v3.51.11).
                rg '_ry_namespace_cleanup' → ≥3 hits (helper + _ry_exit + source-guard).
       [ ] 338. BOOT_WIPE_MARKER ack gate (v3.48.16): SDBOOT_REMOVE_EXISTING=yes
                requires RY_INSTALL_CONFIRM_BOOT_WIPE=1 on first run;
                marker at ~/ry-install/.boot-wipe-acknowledged.
                Re-prompts if entry count or basename-set grows (v3.50.2).
                rg 'RY_INSTALL_CONFIRM_BOOT_WIPE|boot-wipe-acknowledged' → ≥5.
       [ ] 339. RY_RUN_TIMEOUT env (v3.48.24 + v6.2.13 refactor):
                `_run` wraps via `command timeout --foreground --kill-after=10
                "$_run_timeout" $argv` (note: --preserve-status was retired
                in the v6.0 reduction along with the wider parallel-children
                infrastructure — see check 354). Default $_RY_RUN_TIMEOUT_DEFAULT
                = 3600. v7.3.5 regex form is `string match -qr '^[0-9]+$'`
                (accepts non-negative integers including 0; 0 is then mapped
                to empty-string = disable via an explicit `test $_t -eq 0;
                and echo ""` branch in `_run_resolve_timeout`). Invalid
                values → one-shot _warn (gated on `_RY_RUN_TIMEOUT_WARNED`
                sentinel to avoid spam), fallback to default.
                Regression: _py_extract _run_resolve_timeout | rg
                "^\[0-9\]\+\$" → 1 hit; rg "^\[1-9\]\\d\*\$" → 0 hits
                (legacy form retired).
       [ ] 340. TIMESTAMP suffixed with $fish_pid (v3.48.26): prevents
                log-file race between concurrent children.
                rg 'TIMESTAMP.*fish_pid|fish_pid.*TIMESTAMP' → ≥1.
       [ ] 341. Profile system (v3.50.0): external profiles at
                ~/.config/ry-install/profiles/<name>.fish; resolution via
                ~/.config/ry-install/default-profile → gtr9_pro fallback.
                _load_profile, _validate_profile with 26 required + 8 conditional
                globals, numeric type-check, element sanitization.
                Profile trust model: sourced with user privileges — no sandboxing.
                rg '_load_profile|_validate_profile|_ry_profile_' → ≥3 helpers + ≥1 profile.
       [ ] 342. _acquire_lock flock reclaim (v3.50.1): uses `flock -n -E 5`
                before rmdir+mkdir+PID re-verify. Writes PID inside locked
                subshell (v3.48.16). rg 'flock -n -E 5' → ≥1 hit.

  14.26 v4.0.0 CONFIG OVERHAUL (8 checks)
       [ ] 343. KERNEL_PARAMS 12→15 (v4.0.0): added amd_pstate=active,
                loglevel=3, rd.udev.log_level=3, rd.systemd.show_status=auto.
                Replaced clocksource=tsc → tsc=reliable.
                Removed nvme_core.default_ps_max_latency_us=0 (NVMe APST
                now a regression detector, not an applied param).
                Verify: rg exact-match on all 15 entries in KERNEL_PARAMS array.
       [ ] 344. SYSCTL_VALUES history: pre-v4.0.0 21 entries → v4.0.0 reduced
                to 19 (removed somaxconn=8192, unprivileged_bpf_disabled=1,
                inotify.max_user_watches, page_lock_unfairness; added
                tcp_notsent_lowat=131072) → v4.1.10 expanded to 21 → v4.6.x
                reduced to 16 (removed busy_read=50, busy_poll=50, netdev_budget=600,
                kernel.split_lock_mitigate=0, vm.swappiness=100). Audit-time count: 16.
                rg 'somaxconn|unprivileged_bpf|max_user_watches|page_lock_unfairness' → 0 functional.
                rg 'busy_read|busy_poll|netdev_budget|split_lock_mitigate|vm\.swappiness' → 0 functional in SYSCTL_VALUES context.
       [ ] 345. PKGS_ADD history: v4.0.0 15→14 (added nftables; removed vulkan-radeon
                + lib32-vulkan-radeon — chwd auto-installs); v4.6.4 14→13 (nftables
                dropped); v4.6.5 libva-mesa-driver→mesa, lib32-libva-mesa-driver→
                lib32-mesa (upstream removed standalone names around mesa 24.2.7);
                v4.6.17 13→12 (mkinitcpio-firmware moved to AUR_PKGS).
                Final v5.0.1 count: PKGS_ADD:12, AUR_PKGS:2 (mt76-mt7925-dkms,
                mkinitcpio-firmware). EXPECTED_SERVICES history: v4.0.0 3→4
                (added nftables.service); v4.6.4 4→3 (nftables.service dropped).
                Final v5.0.1 EXPECTED_SERVICES: cpupower-epp.service, fstrim.timer,
                NetworkManager.service. EXPECTED_VULKAN_PKGS retained as optional
                global (v3.48.20 onward).
                rg 'nftables' → 0 functional hits (regression);
                rg 'libva-mesa-driver' → 0 functional hits (regression).
                EXPECTED_VULKAN_PKGS now optional global (v3.48.20).
       [ ] 346. nftables firewall baseline RETIRED (v4.6.4): originally added in
                v4.0.0 as nftables in PKGS_ADD + nftables.service in EXPECTED_SERVICES;
                removed v4.6.4. Firewall coverage now expected from CachyOS vendor
                / user choice; ry-install does not enforce a firewall package.
                Retained as regression detector only — see check 345 final counts.
                rg 'nftables' → 0 functional hits (regression).
       [ ] 347. systemd-coredump.socket in MASK (v4.0.0+): complements
                Storage=none by eliminating spawn-and-discard overhead on
                Wine/Proton crashes. MASK count=11 total (v5.0.1; ufw.service
                added v4.6.7 brought it from 10 to 11). _ir_validate_counts
                invariant `MASK:11`. rg 'systemd-coredump\.socket' → ≥2 hits
                (MASK + verify).
       [ ] 348. irqbalance.service removed from MASK (v4.0.0): check 307
                inverts — MASK must NOT contain irqbalance; threadirqs
                kernel param stays. rg 'irqbalance' in MASK context → 0.
       [ ] 349. RADV env rework (v4.0.0+): RADV_EXPERIMENTAL="transfer_queue"
                (v5.0.1 — `,hic` dropped earlier in v4.6.x line),
                RADV_PERFTEST="sam,nircache". drirc Mesa min ≥22.3.
                ENV_VARS count=11 in v5.0.1 (history: v4.0.0 ENV_VARS:12 →
                v4.1.11 13 → v4.5.37 12 (PROTON_NO_WM_DECORATION dropped) →
                v4.5.38 11 (ENABLE_LAYER_MESA_ANTI_LAG dropped). _ir_validate_counts
                invariant `ENV_VARS:11`. Deprecated DO-NOT-REINTRODUCE list documented
                in README per-game-overrides section (DXVK_ASYNC, DXVK_FRAME_RATE,
                WINE_FULLSCREEN_FSR, PROTON_NO_WM_DECORATION, ENABLE_LAYER_MESA_ANTI_LAG).
       [ ] 350. RESOLVED_MDNS=resolve (v4.0.0, was `no`): fixes .local
                resolution under COSMIC DE. Profile global propagates to
                99-cachyos-resolved.conf via `MulticastDNS=$RESOLVED_MDNS`
                interpolation in _ry_get_file_content (not as a literal
                string). Verify the chain, not just the literal.
                rg 'set -g RESOLVED_MDNS resolve' → 1 hit (profile default).
                rg 'MulticastDNS=\$RESOLVED_MDNS' → ≥2 hits
                  (printf in _ry_get_file_content + _chk_grep in verify).

  14.27 v4.0.1–v4.0.4 HARDENING (6 checks)
       [ ] 351. _install_fstab_opts (v4.0.1): sudo chmod/chown
                --reference=/etc/fstab BEFORE findmnt --verify + mv;
                prevents 0644→0600 regression from sudo mktemp default.
                rg '\-\-reference=/etc/fstab' → ≥2 hits.
       [ ] 352. Log subdirs under umask 0077 (v4.0.1): chmod 700 repair
                for looser-umask dirs from older runs. mkdir path.
                rg 'umask 0077|chmod 700' in log context → ≥2.
       [ ] 353. _ry_do_install_file post-deploy branches (v4.0.1): added
                sysctl.d, coredump.conf.d, environment.d, /etc/drirc.
                Post-install verification reload path per-destination-type.
       [ ] 354. `fish -c` + `--preserve-status` pattern RETIRED (v6.0 reduction).
                v4.0.1's 11 sites added --preserve-status to `command timeout`
                wrappers around `fish -c '...'` bodies; v6.0 removed all parallel
                `fish -c` infrastructure (test-all, validate-configs parallel jobs,
                hash workers). Current `command timeout` usage is the single
                `_run` body invocation at L1395 (`command timeout --foreground
                --kill-after=10 \"$_run_timeout\" $argv`); no `--preserve-status`
                anywhere. Regression detectors (expect 0 hits):
                  rg -- '--preserve-status' → 0.
                  rg 'fish -c' → 0 (entire infrastructure retired).
       [ ] 355. _ry_verify_static terminal case '*' catch-all (v4.0.2):
                result switch has wildcard final clause; partial-write
                payloads no longer silently skip.
                NOTE: v4.0.2's per-pid wait + 124/137/143 sentinel capture
                was RETIRED in v4.1.4 (hash_rcs + _any_hash_worker_timeout
                removed; Fish's `wait` returns 0 regardless of child exit,
                making sentinels unobservable). See check 371.
                rg 'case .\*.' in _ry_verify_static body → ≥1 hit.
       [ ] 356. Hash pipeline pipestatus guard (v4.0.3): ≥7 sites capture
                raw line, snapshot $pipestatus, guard on fish-native form
                `test $_ps[1] -eq 0; and test $_ps[2] -eq 0` (success branch)
                or `test $_ps[1] -ne 0; or test $_ps[2] -ne 0` (failure branch).
                Empty-stdin SHA no longer masks cat failure: a cat-failure on
                empty stdin would produce the empty-input SHA sentinel that
                slips past `test -z` guards — now caught by pipestatus before
                the hash is consumed.
                rg 'test \$_ps\[1\] -eq 0; and test \$_ps\[2\] -eq 0' → ≥7 hits.
                rg 'test \$_ps\[1\] -ne 0; or test \$_ps\[2\] -ne 0' → ≥2 hits.
                NOTE: pre-v4.4.3 audit prompts used bash-style `_ps[1]=0`
                which is not fish syntax — false 0-hit was a regex bug,
                not a script regression.

  14.28 v4.1.0–v4.1.3 MODE SURFACE + REGRESSIONS (11 checks)
       [ ] 357. --test-all mode removal (v4.1.0): _ry_do_test_all,
                --test-all flag, EXPECTED_TEST_COUNT → 0 functional hits.
                Pre-commit `fish --no-execute` supersedes the mode.
                -169 lines. Deprecated-flag handler emits usage error.
                rg '_ry_do_test_all|--test-all' → 0 functional.
       [ ] 358. --completions mode removal (v4.1.0): _ry_do_completions,
                --completions flag, autoinstall + verify → 0 hits.
                README is authoritative for flag documentation.
                -93 lines. rg '_ry_do_completions|--completions' → 0.
       [ ] 359. Signal handler SIG-prefix matching (v4.1.1): _cleanup
                `switch "$argv[1]"` has case branches for BOTH short
                (HUP/INT/QUIT/TERM) AND SIG-prefixed (SIGHUP/SIGINT/
                SIGQUIT/SIGTERM) forms. Exit codes 129/130/131/143
                correctly distinguished (were all → 130 pre-fix).
                rg 'case HUP SIGHUP|case INT SIGINT|case QUIT SIGQUIT|case TERM SIGTERM' → 4 hits.
       [ ] 360. _install_preflight sudo-tag regex (v4.1.2): anchored
                correctly — `(\bNOEXEC\b|!PASSWD\b|!SETENV\b|\bLOG_OUTPUT\b)`.
                Previous `\b!PASSWD\b` failed (leading `!` is non-word).
                rg '!PASSWD\\b|!SETENV\\b' → ≥1 hit in regex context.
       [ ] 361. SYU gate (v4.1.2): unattended -Syu gated behind
                RY_INSTALL_CONFIRM_SYSTEM_UPGRADE=1. Without ack, fetches
                3 headlines each from arch/cachyos RSS
                (curl --max-time 5, non-blocking). Matches project rule:
                review news before -Syu.
                rg 'RY_INSTALL_CONFIRM_SYSTEM_UPGRADE' → ≥3 hits.
       [ ] 362. MANIFEST_WRITE_FAILED decoupling (v4.1.2): manifest-write
                decoupled from INSTALL_HAD_ERRORS. Failure logs
                MANIFEST_WRITE_FAILED only; does not flip global error.
                rg 'MANIFEST_WRITE_FAILED' → ≥1 hit.
       [ ] 363. SDBOOT update EXIT_BOOT_CRIT (v4.1.2): sdboot-manage update
                failure elevated to EXIT_BOOT_CRIT (symmetric with gen).
                Both paths return 4, not 1. rg 'sdboot-manage update' → ≥1 hit.
       [ ] 364. flock dependency soft (v4.1.2): _ry_check_deps reclassifies
                flock(1) HARD→SOFT; fallback to PID-re-verify exists.
                Missing flock no longer aborts.
       [ ] 365. _tmpfile_key helper (v4.1.3 introduced, v4.1.4 sha256
                prefix dropped, v6.x HOME-prefix shorthand added): body
                handles HOME-prefix paths by replacing `$HOME/...` with
                `HOME_...` (defends against per-user `$HOME` collision in
                shared key namespaces), then `string replace -a / _ --
                "$p"`. Multi-line, not single-line. Producer/consumer parity
                preserved across the 3 surviving call sites (down from 7 in
                v4.1.4 — parallel-children retirement collapsed 5 jobs into
                inline iteration; _ry_validate_configs jobs and
                _ry_verify_static hash worker removed). Collision detection
                lives in `_ir_validate_keys` (v6.2.1 invariant helper —
                replaces `_validate_profile` which retired with profile sys).
                rg '_tmpfile_key' → 6 hits total = 1 def (L916) + 2
                dispatch calls (L864 in _ry_get_file_content, L1913 in
                _ry_content_bytes) + 1 reference in `_ir_validate_keys`
                (L748) + 2 description/comment mentions.
                rg 'sha256sum.*_tmpfile_key|_tmpfile_key.*sha256' → 0 hits.
       [ ] 366. Dispatcher rewrite (v4.1.3): manual while/switch replaced
                with `argparse --exclusive=verify-static,verify-runtime,check,install-file`.
                Parity preserved across 13 flag scenarios. `--exclusive`
                enforces at-most-one mode flag; mode_count retained as
                defense-in-depth. Deprecated flags (--all, --dry-run,
                --diff, --fix) declared solely to emit specific messages
                via _early_usage_exit.
                rg '\-\-exclusive=verify-static,verify-runtime,check,install-file' → 1 hit.
                rg '_early_usage_exit' → ≥5 hits (deprecated + validation sites).
       [ ] 367. _write_footer no redundant `finished` field (v4.1.3):
                `finished` was duplicate of `ts`; dropped.
                _ry_verify_static implicit_svcs derived from SYSTEM_DESTINATIONS
                path match (not hardcoded). _ry_validate_configs prunes
                $val_dir + $content_dir from _TRACKED_TMPFILES after rm -rf.
                _preflight_boot_sanity BLS path-traversal: exact-segment
                match (accepts kernel..custom, rejects ..).
                Exit-propagation: 6 _load_profile sites normalized to triple-form
                `_ry_exit $CODE; and return $CODE; or return $CODE`.
                rg '"finished":' → 0 hits; rg 'implicit_svcs' → ≥1 derivation site.

  14.29 v4.1.4 PARITY + HARDENING (11 checks)
       [ ] 368. _tmpfile_key impl reverted (v4.1.4): dropped 8-char sha256
                prefix introduced in v4.1.3; body is a single
                `string replace -a '/' '_' -- "$path"`. Parity restored
                between producer and 8 child-side key derivations
                (5 _ry_validate_configs jobs + _ry_verify_static hash
                worker + _ry_do_check Jobs 1-2). Regression: rg in
                _tmpfile_key body 'sha256sum|string sub --length 8' → 0 hits.
       [ ] 369. _validate_profile tmpfile-key collision guard (v4.1.4,
                refined v4.1.5 — see check 379):
                iterates SYSTEM_DESTINATIONS + USER_DESTINATIONS +
                SERVICE_DESTINATIONS, computes slash→underscore key for
                each, rejects first duplicate with _err + return 1.
                Replaces the in-helper sha256 collision defense from
                v4.1.3 (now retired — see check 368).
                rg 'Profile destination duplicated|both map to tmpfile key' → ≥1 hit.
                rg '_seen_keys' in _validate_profile body → ≥2 hits.
                NOTE: "Profile destination key collision" label retained
                in v4.1.5 as the prefix of the narrower key-collision
                branch (1 hit expected). Only semantic scope changed;
                string presence is NOT a legacy-msg regression.
       [ ] 370. _ry_validate_configs merge loop simplified (v4.1.4):
                removed 5 × `_rc_<phase>` captures, `_phase_rcs` map, and
                the timeout-vs-crash branch. Fish's `wait` returns 0
                regardless of child exit, so sentinel values 124/137/143
                were never observable; the file-presence check now
                handles both cases uniformly.
                rg '_phase_rcs|_rc_config|_rc_units|_rc_env|_rc_sudoers|_rc_fish' → 0 hits.
       [ ] 371. _ry_verify_static hash-worker collector simplified (v4.1.4):
                removed `hash_rcs` array and `_any_hash_worker_timeout`;
                collapsed the phantom "hash worker timeout" branch into
                the single "no result" FAIL. Consistent with check 370
                rationale (Fish `wait` masks child rc).
                rg 'hash_rcs|_any_hash_worker_timeout' → 0 hits.
                rg 'contains -- .*124 137 143|_phase_rc.*124' → 0 hits.
       [ ] 372. _ry_validate_configs Job 2 empty svc_dsts guard (v4.1.4):
                when no `.service` destinations are present, writes
                `units.errors=0` to $val_dir and exits early. Prevents
                spurious "no units to validate" failures on profiles
                with SERVICE_DESTINATIONS empty.
                rg 'count .svc_dsts. -eq 0' or equivalent empty-guard
                pattern in Job 2 body → ≥1 hit.
       [ ] 373. _kill_sudo_keepalive pkill descendant reap (v4.1.4):
                `pkill -TERM -P <pid>` added before SIGTERM on parent,
                then `pkill -KILL -P <pid>` before SIGKILL. Reaps
                `sleep`/`sudo` children that would otherwise orphan to
                init when parent fish exits. pkill gated on `command -q`.
                rg 'pkill -TERM -P|pkill -KILL -P' in _kill_sudo_keepalive
                body → ≥2 hits.
       [ ] 374. _detect_lvm timeout bump (v4.1.4): `command timeout 5` →
                `timeout 10` for slow PAM/NSS paths on first sudo after
                keepalive expiry. rg 'timeout 10 sudo -n pvs' → 1 hit.
                No remaining `timeout 5 sudo -n pvs` in _detect_lvm.
       [ ] 375. _ry_check_network curl probe (v4.1.4): `--connect-timeout 3`
                added alongside `--max-time 5`. Separates connect
                latency from total wall-clock to distinguish offline
                vs slow-DNS vs slow-response paths.
                rg '\-\-connect-timeout 3 --max-time 5' in _ry_check_network
                body → ≥1 hit.
       [ ] 376. News-feed probe in _install_rebuild_boot (v4.1.4):
                `--connect-timeout 2 --max-time 5` on RSS fetch.
                Non-blocking (curl -sf), does not gate install.
                rg '\-\-connect-timeout 2 --max-time 5' → ≥1 hit.
       [ ] 377. _run stderr dedup fish-native (v4.1.4): `command sed 's/^ *//'`
                → `string trim --left`. Drops external sed fork in the
                hot path of every _run call with non-trivial stderr.
                rg 'string trim --left' in _run body → ≥1 hit.
                rg "command sed .s/\^ \*//" → 0 hits.
       [ ] 378. Boot-time parse LC_ALL=C (v4.1.4): `LC_ALL=C printf "%.0f"`
                in _ry_verify_runtime for locale-safe float→int conversion.
                Non-C LC_NUMERIC (e.g., de_DE.UTF-8 uses `,` as decimal
                separator) would return 0, masking real boot-time regressions.
                rg 'LC_ALL=C printf "%.0f"' → ≥1 hit in _ry_verify_runtime.

  14.30 v4.1.5 DIAGNOSTICS REFINEMENT (1 check)
       [ ] 379. _validate_profile destination guard split (v4.1.5): the
                single v4.1.4 collision guard is now two distinct
                diagnostics. Literal duplicate source paths emit
                "Profile destination duplicated: '$_d' appears more than
                once in SYSTEM/USER/SERVICE destinations"; slash→underscore
                key collisions between distinct paths emit "Profile
                destination key collision: '$_d' and
                '$_seen_owners[$_idx]' both map to tmpfile key '$_k'".
                The "Profile destination key collision:" label is retained
                from v4.1.4 as the prefix of the narrower key-collision
                branch — only its semantic scope changed. Guards live in
                _validate_profile body (ry-install v4.1.5 L917-935).
                Prior single-branch guard misreported duplicate sources
                as key collisions, masking profile authoring errors.
                Regression checks:
                  rg "Profile destination duplicated: '\\\$_d'" → 1 hit.
                  rg "both map to tmpfile key '\\\$_k'" → 1 hit.
                  rg 'Profile destination key collision:' → 1 hit
                    (NOTE: label retained as key-collision branch prefix;
                    v4.1.4 form also caught dup-source — do NOT treat
                    presence as legacy-msg regression).
                  rg '_seen_keys' in _validate_profile body → ≥2 hits (retained).

  14.31 v4.1.6 SIGNAL-SAFE JSONL REDIRECTS (1 check)
       [ ] 380. JSONL append redirects signal-safe across all 6 writers
                (v4.1.6): every `>>"$LOG_FILE"` append paired with
                `2>/dev/null`, matching the _write_footer convention
                established at L316. Closes stderr-noise window under
                signal-triggered log rotation where `rm` races the
                existence-test inside _log (TOCTOU between
                `test -f "$LOG_FILE"; or return 0` and the append).
                The 6 sites (ry-install v4.1.6 lines):
                  L316  _write_footer       — footer event (pre-existing)
                  L1356 _json_str bug path  — bug event (added v4.1.6)
                  L1408 _log main writer    — message/section events (added v4.1.6)
                  L1425 _msg invalid-level  — bug event (added v4.1.6)
                  L1577 _emit_step_time     — step_time event (added v4.1.6)
                  L5922 top-level header    — header event (added v4.1.6)
                Regression checks:
                  rg '>>"\$LOG_FILE" 2>/dev/null' → 6 hits (all writers covered).
                  rg '>>"\$LOG_FILE"$' → 0 hits (no bare append remains).
                  rg 'test -f "\$LOG_FILE"; or return 0' → ≥1 hit (the
                    _log entry guard whose TOCTOU window is closed by
                    the new redirect parity).

  14.32 v4.1.8 HYGIENE + HARDENING (4 checks)
       [ ] 381. `_install_packages` pipeline-phase comments normalized
                (v4.1.8): misleading `_info "Synchronizing package
                databases..."` dropped — the DB sync is inline in
                `pacman -Syu`, not a separate step. Stale "install then
                remove" comment replaced with an explicit phase-4
                cross-reference documenting that `PKGS_DEL` removal
                happens in `_install_configure_services` (pipeline
                phase 4), not in `_install_packages` (phase 2).
                Regression checks:
                  rg 'Synchronizing package databases' → 0 hits.
                  rg 'PKGS_DEL removal: phase 4 in _install_configure_services' → ≥1 hit.
                  rg '^# Pipeline phase 2: pacman -Syu' → 1 hit.
                  rg '^# Pipeline phase 4: daemon-reload, enable/start, mask' → 1 hit.
       [ ] 382. `_ry_show_help` dispatch-parity + NO_COLOR documentation
                (v4.1.8): the `--` option description now states that
                positional args after `--` are rejected with exit 2,
                matching the dispatcher's actual behavior (argparse
                has no positional-taking mode flag; trailing args are
                rejected via `_early_usage_exit`). `NO_COLOR` added to
                the ENVIRONMENT block — parity with README, which
                already listed it alongside `RY_RUN_TIMEOUT`,
                `RY_INSTALL_CONFIRM_BOOT_WIPE`, and
                `RY_INSTALL_CONFIRM_SYSTEM_UPGRADE`.
                Regression checks:
                  rg 'positional args after .--. are rejected with exit 2' → ≥1 hit.
                  rg 'NO_COLOR=1.*Suppress ANSI color' → ≥1 hit.
                  _py_extract _ry_show_help | rg '^  (NO_COLOR|RY_RUN_TIMEOUT|RY_INSTALL_CONFIRM)' → 4 hits (all 4 env vars in ENVIRONMENT block).
       [ ] 383. fstab tmpfile lifecycle through `_rm_tmp` / `_track_tmpfile` /
                `_untrack_tmpfile` helpers (mechanism evolved v4.1.8 → v4.5.8;
                helper-extraction continued in v6.x splitting `_install_fstab_opts`
                into a planning function and `_fstab_atomic_replace` atomic-write
                helper). At v7.3.5 baseline, `_install_fstab_opts` contains only
                the ext4-entry detection + change-needed test + `_fstab_atomic_replace`
                dispatch; the actual mktemp + tracking + cleanup + atomic mv lives
                in `_fstab_atomic_replace`. Every failure branch routes through
                `_rm_tmp "$tmpfstab" true` (helper does sudo-aware `rm -f --` then
                auto-untracks via `_untrack_tmpfile`); the success path uses an
                explicit `_untrack_tmpfile "$tmpfstab"` after the atomic `mv -T`.
                The v4.1.8 inline `string match -v --` deregistration pattern and
                "Deregister deleted tmpfstab from cleanup list" comment were retired
                with the helper extractions.
                Regression checks (v7.3.5 baseline):
                  _py_extract _fstab_atomic_replace | rg -c '_track_tmpfile "\$tmpfstab"' → 1.
                  _py_extract _fstab_atomic_replace | rg -c '_rm_tmp "\$tmpfstab" true' → 6.
                  _py_extract _fstab_atomic_replace | rg -c '_untrack_tmpfile "\$tmpfstab"' → 1.
                  _py_extract _install_fstab_opts | rg -c '_rm_tmp|_untrack_tmpfile|_track_tmpfile' → 0
                    (tmpfile lifecycle moved entirely into _fstab_atomic_replace).
                  rg -c '_rm_tmp ' source-wide → ≥30 (helper widely adopted).
       [ ] 384. `_preflight_boot_sanity` find-enumeration null-safety
                (v4.1.8): 3 `find ... -type f` enumerations
                (`vmlinuz-*`, `initramfs-*.img`,
                `loader/entries/*.conf`) switched from newline-delimited
                command substitution to `-print0 2>/dev/null |
                string split0`. Closes `\n`-in-filename hazard and
                brings parity with the pre-existing v4.1.8 site in the
                NM connection enumeration (L4456 in `_ry_verify_runtime`,
                not `_install_post_package_refresh` — the v4.1.8
                CHANGELOG mislabels the function name; the line number
                and pattern are correct). Log rotation in the top-level
                script body (L5951 in v4.1.8, post-function declarations
                — NOT inside `_acquire_lock`, which spans L377-452) uses
                the equivalent but syntactically distinct `-printf
                '...%p\0'` form — same null-safety outcome, excluded
                from the `-print0` regex below by design.
                Regression checks:
                  rg "find .* -name 'vmlinuz-\*' -type f -print0" → 1 hit.
                  rg "find .* -name 'initramfs-\*\.img' -type f -print0" → 1 hit.
                  rg "find .* -name '\*\.conf' -type f -print0" → ≥1 hit (loader/entries).
                  _py_extract _preflight_boot_sanity | rg -c 'print0.*string split0' → 3 hits.
                  rg -c '\-print0 [^|]*\| string split0' → 4 hits (1 pre-existing NM + 3 new boot-sanity).


  14.33 v4.5.25–v5.0.1 ARCHITECTURAL EXPANSION (10 checks)
       [ ] 385. _RY_POST_HOOKS dispatch table + _post_hook_for_target helper
                (v4.5.25): replaces inline switch-case dispatch in
                _ry_install_file post-deploy with a top-level `set -g _RY_POST_HOOKS`
                table of glob|tag pairs (e.g. `"/boot/*|boot"`,
                `"*.service|service"`, `"*/sysctl.d/*|sysctl"`).
                _post_hook_for_target single-iterates the table and emits the
                tag on first glob match. Single iteration site reduces
                duplication and keeps post-hook routing in one place.
                Regression checks:
                  rg '^set -g _RY_POST_HOOKS' → 1 hit.
                  rg '^function _post_hook_for_target' → 1 hit.
                  rg '_post_hook_for_target' → ≥3 hits (definition + call sites).
                  Inline `switch ... case "/boot/*"` in _ry_install_file post-deploy → 0 hits.
       [ ] 386. _ir_validate_counts runtime invariants (v4.5.36 promoted from
                comment-only): asserts the canonical count set at `_init_runtime`
                preflight time before any deploy work. Drift triggers
                `_err_loud "<NAME> count drift: got=N expected=M — README/script
                desync, refuse to deploy"` and `_pre_dispatch_exit $EXIT_PREFLIGHT`.
                v7.3.5 silent-probe contract: under `MODE=check`, `_err_loud` logs
                the ERR to JSONL but returns 0 BEFORE calling `_msg_print --force`,
                so drift under `--check` is silent on stderr but still recorded in
                the audit trail and still aborts with `$EXIT_PREFLIGHT`. Regression:
                _py_extract _err_loud | rg 'test "\$MODE" = check; and return 0' → 1 hit.
                Invariant set (v5.0.1): KERNEL_PARAMS:15 MKINITCPIO_HOOKS:11
                MKINITCPIO_MODULES:1 LOGIND_IGNORE_KEYS:9 ENV_VARS:11
                SYSCTL_VALUES:16 PKGS_ADD:12 PKGS_DEL:7 AUR_PKGS:2 MASK:11.
                Regression checks:
                  rg '^function _ir_validate_counts' → 1 hit.
                  _py_extract _ir_validate_counts | rg 'KERNEL_PARAMS:15' → 1 hit.
                  _py_extract _ir_validate_counts | rg 'AUR_PKGS:2|MASK:11|ENV_VARS:11|SYSCTL_VALUES:16|PKGS_ADD:12|PKGS_DEL:7' → 6 hits.
                  Counts must match actual array sizes (`(count $NAME)` ≡ declared invariant).
       [ ] 387. _RY_BOOT_TAINTED separated from INSTALL_HAD_ERRORS (v4.6.3):
                fine-grained gate distinguishing "package state or boot-critical
                config drift requires initramfs rebuild" (_RY_BOOT_TAINTED) from
                "any install pipeline error" (INSTALL_HAD_ERRORS). Service-runtime
                failures bump INSTALL_HAD_ERRORS but NOT _RY_BOOT_TAINTED — they
                don't require kernel/initramfs rework. _install_rebuild_boot gates
                solely on _RY_BOOT_TAINTED (with RY_INSTALL_FORCE_BOOT_REBUILD=1
                bypass for recovery). README "Initramfs rebuild" section documents
                the boot-critical config set: /etc/mkinitcpio.conf, /etc/kernel/cmdline,
                /boot/loader/loader.conf, /etc/sdboot-manage.conf.
                Regression checks:
                  rg '^set -g _RY_BOOT_TAINTED' → 1 hit (init).
                  rg '_RY_BOOT_TAINTED' source-wide → ≥8 hits.
                  _py_extract _install_rebuild_boot | rg '_RY_BOOT_TAINTED' → ≥1 hit.
                  Setting INSTALL_HAD_ERRORS without setting _RY_BOOT_TAINTED on
                  boot-critical config error sites is a regression.
       [ ] 388. _bwg_managed_only auto-ack for SDBOOT_REMOVE_EXISTING=yes (v4.6.6):
                rc=0 if every `loader/entries/*.conf` matches a `vmlinuz-*` (or
                `<name>-fallback.conf`) sdboot-manage will regenerate. Foreign
                entries (windows.conf, rescue.conf) preserve refusal — manual
                ack via RY_INSTALL_CONFIRM_BOOT_WIPE=1 still required.
                Markers: BOOT_WIPE_AUTO_ACK (entries=N managed_kernels=K),
                BOOT_WIPE_AUTO_ACK_DECLINE (foreign_entries=F managed_kernels=K).
                Regression checks:
                  rg '^function _bwg_managed_only' → 1 hit.
                  rg 'BOOT_WIPE_AUTO_ACK\b' → ≥1 hit.
                  rg 'BOOT_WIPE_AUTO_ACK_DECLINE' → ≥1 hit.
                  README RY_INSTALL_CONFIRM_BOOT_WIPE row documents the auto-ack
                  override path.
       [ ] 389. _resolve_boot_path + _RY_BOOT_PATH cache (v4.6.8): canonical
                `$BOOT` resolution via `bootctl -x`, falls back to `_resolve_esp`
                (which itself falls back to /boot with _warn). _preflight_boot_sanity
                family operates on `$BOOT` regardless of XBOOTLDR partition layout.
                _pbs_entry_has_valid_kernel handles BLS Type #1 `linux=` resolution
                per uapi-group spec (anchored on $BOOT regardless of leading slash;
                strip leading `/+` then join as `$boot/$rel`; containment check
                retained as `..`-traversal defence). v7.3.5 stdout convention: all
                4 emit sites in `_resolve_esp` (`echo "$_RY_ESP_PATH"` cached-return
                + `echo "$_p"` fresh-return) and `_resolve_boot_path` (`echo
                "$_RY_BOOT_PATH"` cached-return + `echo "$_p"` fresh-return) were
                switched to `printf '%s' "..."` for command-substitution symmetry
                with `_bootctl_dir` (which already used `printf '%s'`). Eliminates
                trailing-newline pollution in the cached-return path that would
                propagate through `(...)`-expansion when callers concatenate the
                result with additional path components.
                Regression checks:
                  rg '^function _resolve_boot_path' → 1 hit.
                  rg '^function _resolve_esp' → 1 hit.
                  rg '^function _pbs_entry_has_valid_kernel' → 1 hit.
                  rg '_RY_BOOT_PATH' → ≥3 hits (cache + readers).
                  rg 'bootctl -x|_bootctl_dir.*-x' → ≥1 hit (literal
                    `bootctl -x` retired v6.x → 0; helper `_bootctl_dir -x`
                    used at one call site in `_resolve_boot_path` body. Helper
                    definition uses `--argument-names flag` and does NOT match
                    the literal `-x` regex → exactly 1 hit on the call site
                    `_bootctl_dir -x BOOT_BOOTCTL_PIPE_FAIL ...`).
                  _py_extract _resolve_esp | rg "printf '%s'" → 2 hits (v7.3.5 form).
                  _py_extract _resolve_boot_path | rg "printf '%s'" → 2 hits (v7.3.5 form).
                  _py_extract _resolve_esp | rg 'echo "\$_(p|RY_ESP_PATH)"' → 0 hits (legacy form retired v7.3.5).
                  _py_extract _resolve_boot_path | rg 'echo "\$_(p|RY_BOOT_PATH)"' → 0 hits (legacy form retired v7.3.5).
       [ ] 390. _RY_OUTPUT_BROKEN EPIPE re-entry guard (v4.6.16): top-level
                `set -g _RY_OUTPUT_BROKEN false` (or unset). _cleanup_pipe sets
                it to `true` on SIGPIPE. _msg_print, _err_loud, _echo, and any
                stderr/stdout-emitting helper short-circuits with
                `set -q _RY_OUTPUT_BROKEN; and return 0` to avoid EPIPE re-entry
                cascade. JSONL log writer (_log) is the single writer that
                continues — log file is canonical state of record.
                Regression checks:
                  rg '_RY_OUTPUT_BROKEN' → ≥4 hits (setter + 3 guards — 2x `set -q ...; and return 0` short-circuit + 1x `not set -q ...` clause).
                  rg 'set -q _RY_OUTPUT_BROKEN; and return' → ≥2 hits.
                  _py_extract _cleanup_pipe | rg 'set -g _RY_OUTPUT_BROKEN true' → 1 hit.
                  _py_extract _msg_print | rg 'set -q _RY_OUTPUT_BROKEN' → 1 hit.
       [ ] 391. AUR_PKGS as a separate top-level array + paru hard prereq
                (v4.6.17): mkinitcpio-firmware moved out of PKGS_ADD into
                `set -g AUR_PKGS mt76-mt7925-dkms mkinitcpio-firmware`.
                paru promoted from soft-dep to hard prereq — `_ry_check_deps`
                fails preflight if `command -q paru` is false.
                _install_aur_packages routes AUR_PKGS through `paru -S --needed
                --noconfirm`; per-pkg retry skipped when count $AUR_PKGS ≤ 1
                (v4.6.9 perf optimization).
                Regression checks:
                  rg '^set -g AUR_PKGS' → 1 hit.
                  _py_extract _install_aur_packages | rg 'paru -S' → ≥1 hit.
                  _py_extract _ry_check_deps | rg 'paru' → ≥1 hit.
                  rg 'mkinitcpio-firmware' inside PKGS_ADD context → 0 hits (regression).
                  README Prerequisites table lists paru as Required.
       [ ] 392. _verify_summary uses _msg_nocount (v4.6.16 off-by-one fix) +
                _msg ungated VERIFY_* increments (v4.6.5): historic bug — _msg
                gated VERIFY_FAIL/WARN/OK increments on `VERIFY_MODE=true`,
                so the install-pipeline footer reported 0/0/0 for pass/fail/warn
                regardless of actual outcomes. Fix: increments are now
                unconditional; _verify_summary uses _msg_nocount so its own
                summary line doesn't double-count. ERR level (via _err) also
                bumps VERIFY_FAIL — surfaces hard failures in summary.
                Regression checks:
                  _py_extract _msg | rg -c 'VERIFY_' → ≥3 hits (FAIL/WARN/OK).
                  _py_extract _msg | rg 'test "\$VERIFY_MODE" = true; and set' → 0 hits (gate removed).
                  _py_extract _verify_summary | rg '_msg_nocount' → ≥1 hit.
                  _py_extract _msg | rg 'case FAIL ERR' → 1 hit (the case
                    arm where ERR shares the FAIL counter bump). `_err` itself
                    is a one-line wrapper `function _err; _msg ERR $argv; return 0; end`
                    that delegates to `_msg`; do NOT regex `_err` body — extracting
                    a one-line function yields the single line with NO `VERIFY_FAIL`
                    reference (the bump happens in `_msg`'s switch arm).
       [ ] 393. _reclaim_stale_lock cmdline-substring defence (v4.6.12): before
                refusing reclaim on `comm == fish`, additionally substring-match
                "ry-install" against `/proc/<old_pid>/cmdline`. Closes the
                fish-PID-reuse window where an unrelated fish process inherits
                the stale lock-owner PID. Combines with v4.6.9 `test -d /proc/$old_pid`
                liveness probe (replaces `kill -0` whose EPERM≠ESRCH conflated
                "process exists, different owner" with "process gone").
                Regression checks:
                  _py_extract _reclaim_stale_lock | rg 'cmdline' → ≥1 hit.
                  _py_extract _reclaim_stale_lock | rg 'ry-install' → ≥1 hit.
                  _py_extract _reclaim_stale_lock | rg 'test -d /proc' → ≥1 hit.
                  _py_extract _reclaim_stale_lock | rg 'kill -0' → 0 hits (replaced).
       [ ] 394. New env-var runtime gates (v4.6.x cumulative):
                  - RY_INSTALL_ALLOW_PARTIAL_UPGRADE=1 — switches Packages phase
                    from `pacman -Syu --needed` (Arch-recommended) to
                    `pacman -Sy --needed` (install-only; partial-upgrade risk).
                    Violates Arch policy; documented in README Runtime Variables.
                  - RY_INSTALL_PKG_REMOVE_CASCADE=1 — cascades installed reverse
                    deps into the removal set when a `PKGS_DEL` package is
                    blocked. Default skips with warn.
                Plus established gates retained at v7.3.5: RY_RUN_TIMEOUT (default
                $_RY_RUN_TIMEOUT_DEFAULT=3600), RY_INITRD_WARN_MB,
                RY_INSTALL_FORCE_BOOT_REBUILD, RY_INSTALL_SKIP_HARDWARE_CHECK,
                RY_INSTALL_WIRELESS_REGDOM (v7.1.0), NO_COLOR.
                Retired (regression detectors): RY_INSTALL_CONFIRM_BOOT_WIPE
                (v6.0 boot-wipe ack gate retired), RY_INSTALL_CONFIRM_SYSTEM_UPGRADE
                (v6.0 SYU gate retired).
                Help-text ENVIRONMENT block must enumerate all 8 active vars;
                README Runtime Variables table must match.
                Regression checks:
                  rg 'RY_INSTALL_ALLOW_PARTIAL_UPGRADE' → ≥2 hits (help + handler).
                  rg 'RY_INSTALL_PKG_REMOVE_CASCADE' → ≥2 hits (help + handler).
                  _py_extract _ry_show_help | grep -cE '"\s*(NO_COLOR|RY_RUN_TIMEOUT|RY_INITRD_WARN_MB|RY_INSTALL_)' → 8 hits (v7.3.5 help text uses printf '%s\n' with backslash-continued lines indented 8 spaces; each env entry is quoted, so anchor the regex on the opening quote-plus-whitespace rather than 2-space indent). Eight expected: NO_COLOR + RY_RUN_TIMEOUT + RY_INITRD_WARN_MB + RY_INSTALL_ALLOW_PARTIAL_UPGRADE + RY_INSTALL_FORCE_BOOT_REBUILD + RY_INSTALL_PKG_REMOVE_CASCADE + RY_INSTALL_SKIP_HARDWARE_CHECK + RY_INSTALL_WIRELESS_REGDOM.

  14.34 v5.0.1 → v6.0.0 REDUCTION (16 retirement-only checks, 395–410)
       The v6.0.0 release deliberately retired entire subsystems. Every check
       below is a 0-hit regression detector — restoring any retired symbol
       requires explicit changelog justification and contradicts the v6.0
       reduction contract (5994 → 4985 L, -1009).
       [ ] 395. Source-mode scaffolding partially retired: rg '_RY_INSTALL_SOURCED|_ry_namespace_cleanup' → 0 hits each (retired with v6.0 reduction). The bail-state sentinels `_RY_INSTALL_BAILING` and `_RY_INSTALL_LAST_EXIT` SURVIVE — they are retained inside `_ry_exit` (re-entry guard) and `_cleanup_on_exit` (final exit-code source); rg '_RY_INSTALL_BAILING' → ≥2 hits; rg '_RY_INSTALL_LAST_EXIT' → ≥3 hits. Triple-form `_ry_exit $CODE; and return $CODE; or return $CODE` retired; current `_ry_exit` is a ~20-line function with bail-reentry guard, header-vs-no-header log cleanup branch, and `_do_cleanup` call, NOT a single-line wrapper. Top-level dispatcher's `status stack-trace 2>/dev/null | string match -q '*from sourcing*'` early-abort replaces the prior sourced-vs-executed distinction.
       [ ] 396. Sudo-keepalive subsystem retired: rg 'SUDO_KEEPALIVE|_start_sudo_keepalive|_kill_sudo_keepalive|SUDO_KEEPALIVE_ERR' → 0 hits each (bare-word `keepalive` alternative dropped — too broad; would false-match comments or future use of the term in unrelated contexts). Sudo is cached once via `_ensure_sudo_cached` before forking parallel children; no background renewal child runs. Implication: long-running operations that exceed the sudo timestamp must re-prompt; the v6.0 design accepts this trade-off for the target unattended-install path where pacman/paru run synchronously under the cached ticket.
       [ ] 397. JSONL progress events retired: rg 'progress.*event|"event":"progress"' → 0 hits. Phase boundaries log via `_log "=== <phase> ==="` markers + `_progress` UI bar only; no machine-readable progress entries in the JSONL stream.
       [ ] 398. Log rotation retired: rg 'MAX_LOGS|log.*rotation|sort.*log_base|xargs.*rm.*log' → 0 hits in log-context. Logs accumulate in `~/ry-install/logs/$DATE_LABEL/`; pruning is the user's responsibility (README `Logs` section documents `find ... -mtime +30 -delete`). flock(1) dependency retired with the rotation system.
       [ ] 399. LVM detection retired: rg '_detect_lvm\b|pvs\b' → 0 hits. `lvm2-monitor.service` remains in MASK as a static decision; no runtime LVM probe gates its application.
       [ ] 400. Boot-wipe ack subsystem retired: rg 'BOOT_WIPE_AUTO_ACK|BOOT_WIPE_MARKER|_bwg_managed_only|RY_INSTALL_CONFIRM_BOOT_WIPE|boot-wipe-acknowledged' → 0 hits each. `SDBOOT_REMOVE_EXISTING=yes` is applied unconditionally; the v3.48.16/v4.6.6 ack-gate + auto-ack derivation are gone. Tradeoff: users with foreign bootloader entries (windows.conf, rescue.conf) must accept regeneration policy via README warning, not env-var override.
       [ ] 401. _reclaim_stale_lock helper split retired: rg '^function _reclaim_stale_lock' → 0 hits. Stale-PID reclaim is inline in `_acquire_lock` (~12 lines following `_acquire_lock_fresh` rc=2 branch). `_acquire_lock_fresh` retained as the atomic-mkdir core.
       [ ] 402. test -d /proc liveness retired: in `_acquire_lock` body, rg 'test -d /proc' → 0 hits. Reverted to `command kill -0 "$_stale_pid" 2>/dev/null` (1 hit expected). See check 127 for trade-off rationale.
       [ ] 403. bootctl-flag indirection (v6.x): direct `bootctl -x` /
                `bootctl -p` literal-string invocations replaced by the
                `_bootctl_dir` helper which calls `bootctl $flag`. `_resolve_boot_path`,
                `_RY_BOOT_PATH` cache, and `_resolve_esp` all SURVIVE the v6.0
                reduction (contrary to original v6.0 retirement claim — they
                were retained for XBOOTLDR + BLS Type #1 resolution).
                Regression checks:
                  rg 'bootctl -x' source-wide → 0 hits (literal string retired).
                  rg '_bootctl_dir' source-wide → ≥3 hits (helper + ≥2 call sites
                    in `_resolve_boot_path` and `_resolve_esp`).
                  rg '^function _resolve_boot_path' → 1 hit (survives).
                  rg '^function _resolve_esp' → 1 hit (survives).
                  rg '_RY_BOOT_PATH' source-wide → ≥3 hits (def + cache reads).
       [ ] 404. _ntsync_per_kernel_state retired: rg '_ntsync_per_kernel_state|_ntsync_check_installed_kernels' → 0 hits each. `_ntsync_state` remains as the single 5-state stdout helper.
       [ ] 405. Atomic-write TOCTOU re-stat as a separate function retired: the v6.2.2 `_atomic_write_file` post-write symlink re-check is now inlined as `_awf_symlink_check $dst $tmpfile $use_sudo post-write` (one call after `_awf_render_to_tmp`). The 4-call symlink TOCTOU defence (pre + post in sudo + user paths) collapsed into 2 calls via the `phase` argument. rg `_awf_symlink_check` → ≥2 hits (definition + ≥2 call sites with pre/post-write phases).
       [ ] 406. JSONL header pre-init: rg '_init_runtime.*JSONL\|JSONL.*_init_runtime' → 0 hits. JSONL header writes happen at the top-level dispatcher AFTER `_acquire_lock` succeeds and BEFORE `_init_runtime` runs (v6.2.11). Regression: header-before-lock would race with concurrent instances writing to the same log path; header-before-init means `_RY_HEADER_WRITTEN=true` is set early enough that `_cleanup_pipe` SIGPIPE log can fire during init.
       [ ] 407. _RY_HEADER_WRITTEN gate on SIGPIPE log (v6.5.11): `_cleanup_pipe` body must contain `set -q _RY_HEADER_WRITTEN; or return 0` BEFORE the `_log "SIGPIPE_RECEIVED..."` call. Suppresses SIGPIPE log when the JSONL header has not yet been written (e.g., SIGPIPE during preflight). _py_extract `_cleanup_pipe` | rg '_RY_HEADER_WRITTEN' → 1 hit.
       [ ] 408. `_run` 5-helper split (v6.2.13 + v6.x): rg '^function _run\b|^function _run_redact_cmd|^function _run_effective_timeout|^function _run_emit_stream|^function _run_resolve_timeout' → 5 hits. The orchestrator `_run` delegates to `_run_resolve_timeout` (env parse), `_run_effective_timeout` (per-cmd bypass list for pacman/paru/mkinitcpio/sdboot-manage/paccache), `_run_redact_cmd` (tmpfile path scrubbing under $TMPDIR), and `_run_emit_stream` ×2 (stdout + stderr). Regression: any single-function `_run` body > 100 lines indicates inlining drift; the 5-way split should keep each sub ≤50 lines.
       [ ] 409. User-bus detection via XDG_RUNTIME_DIR/bus + `systemctl --user is-system-running` (v6.1.0): `_has_user_bus_active` body must check `$XDG_RUNTIME_DIR/bus` socket existence (-S test) first, fall back to `systemctl --user is-system-running` accepting any state except `offline`. rg `^function _has_user_bus_active` → 1 hit; `_py_extract _has_user_bus_active | rg 'XDG_RUNTIME_DIR/bus'` → 1 hit.
       [ ] 410. Top-level array declarations one-element-per-continuation (v6.2.6): SYSTEM_DESTINATIONS, KERNEL_PARAMS, ENV_VARS, SYSCTL_VALUES, MASK, LOGIND_IGNORE_KEYS each declared with `set -g NAME \` on the header line and one element per indented continuation line. Counts via `rg -nA N '^set -g NAME' ry-install.fish | rg -c '^\d+-    '`. Regression: a single-line `set -g NAME a b c d e ...` form re-introduction degrades readability and breaks the audit's per-element line-grep.

  14.35 v6.0.0 → v7.3.3 EXPANSION (29 checks, 411–439)
       v6.x and v7.x added back targeted features above the v6.0 baseline.
       Every check below is a positive-presence detector — the symbol MUST
       exist at v7.3.5.
       [ ] 411. `_atomic_write_file` post-write symlink re-check (v6.2.2 TOCTOU
                fix). `_awf_symlink_check` called with both `pre` and `post-write`
                phase arguments in `_atomic_write_file` body. _py_extract
                `_atomic_write_file` | rg '_awf_symlink_check.*pre\b' → 1 hit;
                rg '_awf_symlink_check.*post-write' → 1 hit.
       [ ] 412. `_fstab_atomic_replace` findmnt --verify hard-fail (v6.2.2).
                `findmnt --verify --tab-file "$tmpfstab"` rc-checked; failure →
                _rm_tmp + _fail + return 1; success logs verify output via
                `head -n 3` for diagnostics. _py_extract `_fstab_atomic_replace`
                | rg 'findmnt --verify' → 1 hit.
       [ ] 413. `_ip_pacman_invoke` -Syyu retry gated on
                `RY_INSTALL_ALLOW_PARTIAL_UPGRADE` (v6.2.3). Default pacman
                invocation is `-Syu --needed --noconfirm`; with
                `RY_INSTALL_ALLOW_PARTIAL_UPGRADE=1` switches to
                `pacman -Sy --needed` (install-only). Retry path uses `-Syy`
                without `-u`. Regression: rg 'RY_INSTALL_ALLOW_PARTIAL_UPGRADE'
                → ≥3 hits (env read + help text + README).
       [ ] 414. `RY_INSTALL_PKG_REMOVE_CASCADE` env gate (v6.x). When `=1`,
                `_csp_filter_rdeps` cascades installed reverse deps into the
                removal set. Default (unset) skips a `PKGS_DEL` member with WARN
                when an out-of-set installed package rdeps on it. Regression:
                rg 'RY_INSTALL_PKG_REMOVE_CASCADE' → ≥3 hits; rg
                '^function _csp_filter_rdeps' → 1 hit; rg
                '^function _csp_remove_pkgs' → 1 hit.
       [ ] 415. `_run` per-cmd timeout-bypass list (v6.2.4 + cumulative
                expansions): `_run_effective_timeout` returns empty string
                (= disable) for argv[1] in {`pacman`, `paru`, `mkinitcpio`,
                `sdboot-manage`, `paccache`, `updatedb`, `pkgfile`} — 7 cmds.
                Long-running ops escape the global `RY_RUN_TIMEOUT` cap.
                _py_extract `_run_effective_timeout` | rg 'pacman|paru|mkinitcpio|sdboot-manage|paccache|updatedb|pkgfile' → 1 hit (all 7 on a single `contains --` line).
       [ ] 416. Tmpfile-path redaction under `$TMPDIR` (v6.2.4): `_run_redact_cmd`
                replaces `$TMPDIR`-prefixed paths with `<tmp>/` in the logged
                command string. Prevents leaking per-run mktemp paths into the
                JSONL audit trail. _py_extract `_run_redact_cmd` | rg 'TMPDIR' → ≥1 hit.
       [ ] 417. Capture cap 500 with `_TRUNCATED` sentinel (v6.2.7): `_run_emit_stream`
                truncates stdout/stderr captures at 500 lines (was 100), appending
                a single `_TRUNCATED: total_lines=$_total captured=$cap` marker.
                Marker uses `$label_tag` prefix (e.g. STDOUT_TRUNCATED,
                STDERR_TRUNCATED) so a single emit site serves both streams.
                rg '_TRUNCATED' → ≥1 hit.
       [ ] 418. JSONL header before `_init_runtime` (v6.2.11): top-level
                dispatcher writes the header event AFTER `_acquire_lock` succeeds
                and BEFORE `_init_runtime` runs. Order: lock → header → init.
                Regression: header inside `_init_runtime` body or before lock is
                a v6.2.11 violation.
       [ ] 419. `LOCK_DIR` chmod 700 (v6.2.11): `_acquire_lock_fresh` calls
                `command chmod -- 700 "$LOCK_DIR"` immediately after `mkdir`
                succeeds (between the mkdir success and the pidfile mktemp).
                Sentinel `_RY_LOCK_DIR_OWNED=true` hoisted ABOVE chmod (v7.0.13)
                so signal between mkdir and chmod still triggers cleanup.
                _py_extract `_acquire_lock_fresh` | rg 'chmod -- 700' → 1 hit;
                _py_extract `_acquire_lock_fresh` | rg 'set -g _RY_LOCK_DIR_OWNED' → 1 hit
                (set BEFORE chmod line); _py_extract `_acquire_lock_fresh` | rg
                'set --erase _RY_LOCK_DIR_OWNED' → 2 hits (pidfile-write-failed and
                mv-failed cleanup branches; total `_RY_LOCK_DIR_OWNED` references
                inside the helper body = 3).
       [ ] 420. Content-equality compare via `string collect` (v6.2.12):
                `_check_phase_files` reads expected via `_ry_content_bytes` and
                actual via `_installed_bytes`, both `| string collect --no-trim-newlines --allow-empty`.
                `string collect` preserves trailing newlines that command-substitution
                strips. Regression: rg 'string collect --no-trim-newlines' → ≥2 hits.
       [ ] 421. Emit functions use `printf` not `echo` (v6.2.12, refined v7.1.2 +
                v7.2.3): `_msg_print` color-branch, no-color branch, and no-tty
                branch all use `printf`. Prevents flag-injection where a level
                argument starting with `-` would be consumed by `echo` as a flag.
                _py_extract `_msg_print` | rg '\becho\b' → 0 hits (or only in
                explicit non-content positions); _py_extract `_msg_print` | rg
                'printf' → ≥3 hits.
       [ ] 422. `_csp_filter_rdeps` cascade per-pkg WARN demoted to INFO (v7.1.0):
                blocked-pkg-with-rdeps is INFO (per-pkg); aggregate `PKG_REMOVE_SKIPS`
                WARN fires once at phase end. _py_extract `_csp_filter_rdeps` | rg
                '\b_info\b' → ≥1 hit; rg 'PKG_REMOVE_SKIPS' → ≥1 hit.
       [ ] 423. `_if_nm_restart` WARN consolidation (v7.1.0): 3 WARN call sites
                collapsed to 1 WARN + 1 INFO. _py_extract `_if_nm_restart` | rg
                -c '\b_warn\b' → 1; _py_extract `_if_nm_restart` | rg -c '\b_info\b'
                → 1. Same pattern in `_post_nm` (2 WARN → 1 WARN + 1 INFO).
       [ ] 424. `_aur_verify_mt7925` post-install cross-check (v6.5.14):
                pacman -Q AND modinfo mt7925e must both resolve; paru `rc=0`
                alone is insufficient confirmation that the DKMS build succeeded.
                rg '^function _aur_verify_mt7925' → 1 hit; _py_extract
                `_aur_verify_mt7925` | rg 'modinfo mt7925e' → ≥1 hit.
       [ ] 425. `_vmh_order_checks` pair rules + fsck-last (v7.2.3): pair set
                must include `systemd:autodetect`, `autodetect:microcode`,
                `autodetect:modconf`, `systemd:sd-vconsole`, `systemd:keyboard`,
                `keyboard:sd-vconsole`, `modconf:kms`, `block:filesystems` (8 pairs).
                fsck-last as a separate check. _py_extract `_vmh_order_checks`
                | rg 'systemd:autodetect' → 1 hit; rg 'autodetect:microcode'
                → 1 hit; rg 'fsck' → ≥2 hits (pair list reference + last-check).
       [ ] 426. `_vre_fstab` token regex unified (v7.2.4): noatime / lazytime /
                commit=10 token checks use `(^|,)tok(,|$)` form (not substring).
                Fixes false-match where `lazytime` substring would match
                `nolazytime`. _py_extract `_vre_fstab` | rg '\\(\\^\\|,\\)' → ≥3 hits.
       [ ] 427. `_mr_copy_size_verify` cmp -s post-size check (v7.2.4): after
                size match, performs `cmp -s` byte content verify. _py_extract
                `_mr_copy_size_verify` | rg 'cmp -s' → 1 hit.
       [ ] 428. `_dc_erase_globals` includes `_RY_BOOT_TAINTED` (v7.2.4):
                symmetry with `INSTALL_HAD_ERRORS` — both error sentinels
                erased on cleanup. _py_extract `_dc_erase_globals` | rg
                '_RY_BOOT_TAINTED' → 1 hit.
       [ ] 429. `_RY_POST_HOOKS` 14-row dispatch table (v6.2.1 origin +
                cumulative). Replaces inline post-deploy switch. Glob patterns
                with `|tag` suffix; `_post_hook_for_target` iterates with
                `string match -q "$pat" -- "$target"`. v7.3.5 row count = 14
                (asserted by `_ir_validate_counts`). Regression: rg '^set -g
                _RY_POST_HOOKS' → 1 hit; rg '_RY_POST_HOOKS:14' inside
                `_ir_validate_counts` body → 1 hit; `count $_RY_POST_HOOKS` at
                runtime == 14.
       [ ] 430. `_RY_BOOT_CRITICAL_DSTS` invariant (v7.2.4): 4-element array
                listing boot-critical destinations the initramfs rebuild gate
                checks for drift before allowing `mkinitcpio -P`. Members
                (current): `/boot/loader/loader.conf`, `/etc/kernel/cmdline`,
                `/etc/sdboot-manage.conf`, `/etc/mkinitcpio.conf`. Asserted by
                `_ir_validate_counts` invariant `_RY_BOOT_CRITICAL_DSTS:4`.
                Regression: rg '^set -g _RY_BOOT_CRITICAL_DSTS' → 1 hit.
       [ ] 431. `EXPECTED_VULKAN_PKGS:3` invariant added (v7.x): runtime asserts
                3-element vulkan-pkg set. Members: vulkan-radeon,
                lib32-vulkan-radeon, lib32-mesa. Verify-runtime confirms
                installed; not in PKGS_ADD (chwd auto-installs on AMD profile —
                only lib32-mesa is duplicated into PKGS_ADD as a `--needed`
                idempotent backstop). Regression: rg 'EXPECTED_VULKAN_PKGS:3'
                inside `_ir_validate_counts` body → 1 hit.
       [ ] 432. `_RY_PKG_MANAGED_SERVICES:1` invariant added (v7.x): 1-element
                array tracking services that pacman scriptlets enable
                automatically (NetworkManager.service). Used to dedupe in
                `_configure_services_enable` to avoid double-enable. Regression:
                rg '_RY_PKG_MANAGED_SERVICES:1' → 1 hit.
       [ ] 433. ENV_VARS RADV rename (v7.0): `RADV_PERFTEST=transfer_queue` →
                `RADV_EXPERIMENTAL=transfer_queue`. Regression: rg
                '"RADV_EXPERIMENTAL=transfer_queue"' → ≥1 hit inside ENV_VARS
                body; rg '"RADV_PERFTEST=transfer_queue"' → 0 hits (regression);
                `RADV_PERFTEST="sam,nircache"` retained as a separate entry.
       [ ] 434. NM 1.56.0 compat — wifi.iwd.autoconnect=false dropped (v7.0):
                `_content__etc_NetworkManager_conf.d_99-cachyos-nm.conf` body
                contains `wifi.backend=iwd`, `wifi.powersave=...`, `level=WARN`
                only. Regression: rg 'wifi\.iwd\.autoconnect' → 0 hits.
       [ ] 435. avahi-daemon.{service,socket} masked (v7.0): MASK includes both
                `avahi-daemon.service` and `avahi-daemon.socket` (mDNS routed
                via systemd-resolved instead). Regression: rg
                'avahi-daemon\.service' → ≥1 hit in MASK; rg
                'avahi-daemon\.socket' → ≥1 hit in MASK.
       [ ] 436. realtime-privileges + cpupower in PKGS_ADD (v7.0 + v7.2.0):
                PKGS_ADD must contain `realtime-privileges` (PipeWire RT
                scheduling) and `cpupower` (cpufreq governor management).
                Regression: rg 'realtime-privileges' inside PKGS_ADD context →
                ≥1 hit; rg '\\bcpupower\\b' inside PKGS_ADD context → ≥1 hit.
       [ ] 437. cpupower-service.conf + cpupower.service deployment (v7.2.0):
                `/etc/default/cpupower-service.conf` in SYSTEM_DESTINATIONS;
                `cpupower.service` in EXPECTED_SERVICES; `_post_cpupower` post-hook
                + `_vrsv_chk_cpupower_governor` verify check. Under `amd_pstate=active`,
                governor=performance routes to EPP=performance internally.
                Regression: rg 'cpupower-service\.conf' → ≥3 hits (dest array +
                content generator + verify); rg '^function _post_cpupower' → 1
                hit; rg '^function _vrsv_chk_cpupower_governor' → 1 hit; rg
                'cpupower-epp\.service|cpupower_epp' → 0 hits (retired v7.2.1).
       [ ] 438. THP tmpfiles deployment + post-hook (v6.5.14 + v7.0.5):
                `/etc/tmpfiles.d/99-cachyos-thp.conf` in SYSTEM_DESTINATIONS;
                `_post_tmpfiles` post-hook applies via `systemd-tmpfiles --create`
                immediately on `--install-file` redeploy (avoids reboot
                dependency). `*/tmpfiles.d/*|tmpfiles` glob in `_RY_POST_HOOKS`.
                Regression: rg '99-cachyos-thp\.conf' → ≥3 hits; rg
                '^function _post_tmpfiles' → 1 hit; rg
                '\\*/tmpfiles\\.d/\\*\\|tmpfiles' → ≥1 hit.
       [ ] 439. wireless-regdom env gate (v7.1.0 + v7.1.1):
                `RY_INSTALL_WIRELESS_REGDOM=<CC>` writes `WIRELESS_REGDOM=<CC>`
                to `/etc/conf.d/wireless-regdom`. `_ry_apply_wireless_regdom`
                validates `^[A-Z]{2}$` (string trim + upper before regex),
                soft-fails on apply error (rc=1 → `_warn`, not preflight abort).
                Only `EXIT_USAGE` (malformed regex) propagates as preflight
                fail. `_ry_check_wireless_regdom` (verify path) anchors hint
                text on `Runtime variables` (v7.1.1 README anchor rename).
                Regression: rg '^function _ry_apply_wireless_regdom' → 1 hit;
                rg '^function _ry_check_wireless_regdom' → 1 hit; rg
                'RY_INSTALL_WIRELESS_REGDOM' → ≥3 hits (env read + help +
                README); rg '/etc/conf.d/wireless-regdom' → ≥1 hit.

  14.36 v7.3.x README ALIGNMENT (1 check, 440)
       [ ] 440. README phase-order alignment (v7.3.3): README `## Install Flow`
               table row 4 ("Phase 4 — Services") and `### Phase 4 — Services`
               prose must list the actions in the script's actual execution
               order: fstab rewrite → systemd-resolved restart → THP tmpfiles
               apply → PKGS_DEL removal → mask units → daemon-reload + enable.
               v7.3.3 fixed the prior README desync where systemd-resolved
               restart and THP tmpfiles apply were missing and mask/enable
               order was inverted. Cross-check: README phase-4 step sequence
               vs `_install_configure_services` body. Regression: README phase
               steps don't match script order → MED finding.

─── Phase 15: Supplemental Deep Checks (19 checks) ───────────

  15.1 FORMATTING
       [ ] 215. fish_indent --check passes exit 0. Canonical indentation.

  15.2 FUNCTION LENGTH ANALYSIS
       [ ] 216. Functions >50 lines: enumerate all. Acceptable for complex
               modes (script is multi-thousand LOC; v5.0.1 baseline 5,029).
       [ ] 217. Functions >100 lines: must be self-contained mode handlers.

  15.3 PROGRESS BAR — STEPS-ARRAY RETIRED (v4.1.9)
       [ ] 218. PROGRESS_STEPS / PROGRESS_TOTAL fully removed (v4.1.9): rg → 0 hits. _progress call sites still emit phase labels (Preflight, Packages, Configuration, Services, Boot, Finalize); enumerate via `rg -oP '^\s+_progress\s+\K[A-Z][a-zA-Z]+' | sort -u`. Regression detector: any rg hit on `PROGRESS_STEPS` or `PROGRESS_TOTAL` is a re-introduction.

  15.4 TMPFILE LEAK TRACING (v4.5.8 helper-based)
       [ ] 219. Tmpfile lifecycle now flows through helpers: `_atomic_write_file` (5 mktemp call sites internally) → `_rm_tmp` (sudo-aware delete + auto _untrack_tmpfile, 30 use sites) → `_is_symlink` (sudo-aware symlink probe, 3 use sites) → `_TRACKED_TMPFILES` registration. Verify: every `mktemp` (script-wide ≈ 22 sites) is paired with either `_rm_tmp` or `_untrack_tmpfile` on every failure branch. _atomic_write_file rolls all paths through `_rm_tmp $tmpfile $use_sudo` (≥7 occurrences inside the function). Per-function 7/8 + 5/11 baselines retired with the v4.5.8 helper extraction (was: pre-helper era when each function inlined mktemp+rm-f).

  15.5 ATOMIC FILE OPERATIONS
       [ ] 220. _ry_install_file: chmod BEFORE mv. Symlink TOCTOU guard
               (pre-write AND post-write test -L in both sudo/user paths).

  15.6 INJECTION SAFETY
       [ ] 221. _chk_grep: grep -qF --. printf: %s for user values.
               Subshell: only safe builtins in test command substitutions.

  15.7 VARIABLE INITIALIZATION
       [ ] 222. Every numeric var in test has prior set -l with numeric
               value. Init count ≥ test count for all 36 local vars.
               Fish builtins ($status) and script globals (MAX_LOGS)
               excluded — always initialized by runtime.
       [ ] 223. Word splitting N/A in fish. Unquoted $var in test is safe.

  15.8 PATH SAFETY
       [ ] 224. LOG_DIR uses $HOME. No hardcoded /home/ or
               /root/. XDG_RUNTIME_DIR: SSH sock with set -q + test -S.

  15.9 PROCESS LIFECYCLE
       [ ] 225. Sudo keepalive: &, PID captured, disown'd, killed in
               _do_cleanup, set --erase'd. kill -0 after spawn.
       [ ] 226. _run quoting: 0 unquoted $VAR in _run arguments.

  15.10 FLAG COVERAGE + DISPATCH CONSISTENCY (v4.1.3 argparse model)
       [ ] 227. argparse flags in main dispatcher (verify-static,
                verify-runtime, check, install-file, h/help, v/version,
                V/verbose) appear in _ry_show_help. Removed CLI flags
                (--all, --dry-run, --diff, --fix, --lint, --test-all,
                --completions, --logs, --force) are not enumerated in the
                argparse spec; argparse rejects unknown flags with a generic
                `[ERR]` message routed through `_pre_dispatch_exit $EXIT_USAGE`.
                No per-flag deprecated handler exists. README has no CLI-flag
                deprecation section; the "Deprecated flags — DO NOT re-introduce"
                header in README is scoped to deprecated environment variables
                (see check 112).
       [ ] 228. _ry_show_help text enumerates all exit codes present in
                EXIT_* globals: 0/1/2/3/4/5/10/129/130/131/143/134/138/140
                (drop retired 141; include 134/138/140 added v4.6.x via
                _cleanup_other for ABRT/USR1/USR2). Matches check 113 +
                actual help body. No stale 11 (EXIT_LINT removed v3.48.20).
                EXIT_GEN_* (11/12/13) are internal-only sub-codes squashed
                to EXIT_FAIL at consumer site, NEVER surfaced — not in help.
                v7.3.5 EXIT CODES rewording: help text now notes the fish 3.x
                `--on-signal` handler limitation — the parent process `$status`
                may show 0 instead of 130/143/etc when the signal is delivered
                during a fish builtin that swallows the signal; the canonical
                signal exit code is always recorded in the JSONL `footer.exit_code`
                field via the `_teardown signal $_sig_exit` path. Regression:
                _py_extract _ry_show_help | rg 'fish 3\.x --on-signal' → 1 hit;
                _py_extract _ry_show_help | rg 'footer\.exit_code' → 1 hit;
                _py_extract _ry_show_help | rg 'ERR_NO_DATA from systemctl' → 1 hit
                (v7.3.5 `--check` annotation about systemctl probe semantics).

  15.11 GLOBAL STATE CONTRACTS
       [ ] 229. INSTALL_HAD_ERRORS: initialized false, set true in
                error sites. Never reset mid-pipeline.
                Only mutated within install pipeline.
                MANIFEST_WRITE_FAILED (v4.1.2) separate channel — does
                not flip INSTALL_HAD_ERRORS.

  15.12 PATTERN CORRECTNESS
       [ ] 230. 0 unreachable code. or-return patterns correct.
                DESTINATIONS ↔ _ry_get_file_content cases match: case-statement
                count must equal `(count $SYSTEM_DESTINATIONS $USER_DESTINATIONS $SERVICE_DESTINATIONS)`.
                Current resolved total: 15 (11+3+1). Regression: count mismatch
                between case branches and DESTINATIONS arrays.
                Lock PID verify-after-write present (v3.6.3 flock reclaim).
       [ ] 231. string -- separator consistency throughout.

  15.13 PROFILE SYSTEM RETIRED (v4.5.0)
       [ ] 232. Profile subsystem fully removed: rg '_load_profile|_validate_profile|_ry_profile_|default-profile|_manifest_check_orphans|_manifest_write' → 0 functional hits (in-comment forensic notes inside `_init_runtime` description are exempt). gtr9_pro defaults inlined as top-level `set -g` block (PROFILE_NAME=gtr9_pro, PROFILE_DESC at L609-610 of source).
       [ ] 233. `_init_runtime` (≈63 lines) replaces the prior `_load_profile`/`_validate_profile` chain. Caches root UUID, validates hardware sanity, validates timing globals, precomputes tmp-dir cache. Called once near end of dispatch; `rg '^function _init_runtime' → 1`, `rg '_init_runtime' → ≥2` (definition + call site).

═══════════════════════════════════════════════════════════════
 PASS 2: RUNTIME (phase 12, 11 checks)
═══════════════════════════════════════════════════════════════

STEP 1: PRE-FLIGHT + DATA COLLECTION

  ```bash
  S=/home/claude/audit-src/script.fish
  O=/home/claude/audit-findings.txt
  if ! command -v fish >/dev/null 2>&1; then
    echo "[CRITICAL] fish not installed — PASS 2 SKIPPED" >> "$O"; exit 0
  fi
  FISH_VER=$(fish -c 'echo $version' 2>/dev/null | head -n 1)
  FISH_MAJOR=$(echo "$FISH_VER" | cut -d. -f1)
  FISH_MINOR=$(echo "$FISH_VER" | cut -d. -f2)
  if [ "${FISH_MAJOR:-0}" -lt 3 ] || { [ "$FISH_MAJOR" -eq 3 ] && [ "${FISH_MINOR:-0}" -lt 6 ]; }; then
    echo "[CRITICAL] fish $FISH_VER < 3.6 — PASS 2 SKIPPED" >> "$O"; exit 0
  fi
  SYNTAX_OK=1
  if ! fish --no-execute "$S" 2>/dev/null; then
    echo "[HIGH] script fails fish --no-execute — execution tests may produce false failures" >> "$O"
    SYNTAX_OK=0
  fi
  if [ "$SYNTAX_OK" -eq 0 ]; then
    echo "PASS 2 pre-flight: DEGRADED (fish $FISH_VER, syntax errors)"
    echo "  Tests will attempt but results may be unreliable."
  else
    echo "PASS 2 pre-flight: OK (fish $FISH_VER)"
  fi

  # Privilege handling — ry-install exits 2 (EXIT_USAGE) for (id -u)==0 at L66-68; if audit runs as root we drop via runuser (PASS2_FISH_USER env override, default nobody) into a 0700 tmpdir HOME (system-user default /nonexistent breaks fish config + log dir creation).
  FISH_PREFIX_ARGS=()
  if [ "$(id -u)" -eq 0 ]; then
    PASS2_USER="${PASS2_FISH_USER:-nobody}"
    if ! command -v runuser >/dev/null 2>&1; then
      echo "[CRITICAL] audit running as root and runuser(1) unavailable — ry-install exits 2 (EXIT_USAGE) for (id -u)==0 at L66-68; every PASS 2 test would false-fail. Re-run audit as non-root. PASS 2 SKIPPED." >> "$O"
      exit 0
    fi
    if ! id -u "$PASS2_USER" >/dev/null 2>&1; then
      echo "[CRITICAL] audit running as root and PASS2_FISH_USER='$PASS2_USER' does not exist — set PASS2_FISH_USER env to a valid non-root user, or re-run audit as non-root. PASS 2 SKIPPED." >> "$O"
      exit 0
    fi
    if ! runuser -u "$PASS2_USER" -- test -r "$S" 2>/dev/null; then
      echo "[CRITICAL] audit running as root and user '$PASS2_USER' cannot read $S — adjust script permissions or set PASS2_FISH_USER to a user with read access, or re-run audit as non-root. PASS 2 SKIPPED." >> "$O"
      exit 0
    fi
    PASS2_HOME=$(mktemp -d "${TMPDIR:-/tmp}/pass2-fish-home-XXXXXX")
    # Pre-create fish state dirs — 4 parallel --help/-h/--version/-v fish invocations would otherwise race to mkdir them; fish 3.7+ emits "warning-path: File exists" on mkdir race (even with -p) → false-fails checks 239/240 ("0 stderr bytes"). Creating subdirs up front eliminates the race entirely.
    mkdir -p "$PASS2_HOME/.local/share/fish" "$PASS2_HOME/.config/fish"
    # chown + 0700 (not 0777) — fish sources ~/.config/fish/config.fish even non-interactively; world-writable tmpdir would let a local attacker race config.fish between mkdir and first fish spawn → arbitrary code execution as $PASS2_USER. Hand ownership to that user at 0700; race closed.
    chown -R "$PASS2_USER:" "$PASS2_HOME"
    chmod -R 0700 "$PASS2_HOME"
    trap '[ -n "$PASS2_HOME" ] && rm -rf -- "$PASS2_HOME" 2>/dev/null' EXIT INT TERM HUP
    FISH_PREFIX_ARGS=(runuser -u "$PASS2_USER" -- env "HOME=$PASS2_HOME")
    echo "PASS 2: root detected — wrapping fish invocations via '${FISH_PREFIX_ARGS[*]}'"
  fi

  # NON-TARGET HARDWARE — expected non-zero in audit container (don't flag): --verify-runtime/--verify-static → 1 (no managed files); --check → 3 (preflight) or 10 (drift); no-args install → non-zero (no sudo/root UUID). Flag only if exit ≠ expected on target.
  # REMOVED MODES (v3.48.20–v4.1.0) — --lint --test-all --completions --logs --diff --dry-run --all --fix --force are not in the v5.0.1 argparse spec; argparse rejects them with a generic [ERR] message → EXIT_USAGE (2). No per-flag handler; rejection is structural.

  D=/home/claude/audit-data/runtime
  mkdir -p "$D"
  RUNTIME_TIMEOUT=30
  HELP_TIMEOUT=${HELP_TIMEOUT:-5}   # help/version paths should be ~0.02s warm; 5s still 200× headroom

  # Group A: stdout/stderr separation (--help, --version and short forms)
  timeout "$HELP_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" --help    >"$D/help-out.txt"  2>"$D/help-err.txt"  &
  timeout "$HELP_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" -h        >"$D/h-out.txt"     2>"$D/h-err.txt"     &
  timeout "$HELP_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" --version >"$D/ver-out.txt"   2>"$D/ver-err.txt"   &
  timeout "$HELP_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" -v        >"$D/v-out.txt"     2>"$D/v-err.txt"     &
  wait

  # Group B: exit code matrix (surviving modes + deprecated-flag rejection)
  (
    # B1: help/version/bogus
    for mode in "--help" "-h" "--version" "-v" "--bogus" "--verbose"; do
      case "$mode" in --help|-h|--version|-v) T="$HELP_TIMEOUT" ;; *) T="$RUNTIME_TIMEOUT" ;; esac
      timeout "$T" "${FISH_PREFIX_ARGS[@]}" fish "$S" $mode >/dev/null 2>&1; printf '%s → %d\n' "$mode" "$?"
    done
  ) > "$D/exit-b1.txt" &
  (
    # B2: verification/utility modes (non-target failures expected)
    for mode in "--verify-static" "--verify-runtime" "--check" \
                "--install-file" "--install-file relative/path" \
                "--install-file /etc/kernel/cmdline"; do
      timeout "$RUNTIME_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" $mode >/dev/null 2>&1; printf '%s → %d\n' "$mode" "$?"
    done
  ) > "$D/exit-b2.txt" &
  (
    # B3: deprecated flags — must all return EXIT_USAGE (2)
    for mode in "--all" "-a" "--dry-run" "-n" "--diff" "--fix" "--force" "-f" \
                "--lint" "--test-all" "--completions" "--logs"; do
      timeout "$RUNTIME_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" $mode >/dev/null 2>&1; printf '%s → %d\n' "$mode" "$?"
    done
  ) > "$D/exit-b3.txt" &
  (
    # B4: --exclusive enforcement — combining mode flags must return EXIT_USAGE (2)
    for mode in "--verify-static --verify-runtime" \
                "--verify-static --check" \
                "--check --install-file /etc/kernel/cmdline" \
                "--verify-runtime --install-file /etc/kernel/cmdline"; do
      timeout "$RUNTIME_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" $mode >/dev/null 2>&1; printf '%s → %d\n' "$mode" "$?"
    done
  ) > "$D/exit-b4.txt" &
  wait
  cat "$D"/exit-b{1,2,3,4}.txt > "$D/exit-codes.txt"

  # Group B2: stdout leak check for operational modes
  timeout "$RUNTIME_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" --verify-static  >"$D/vs-out.txt"    2>/dev/null &
  timeout "$RUNTIME_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" --verify-runtime >"$D/vr-out.txt"    2>/dev/null &
  timeout "$RUNTIME_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" --check          >"$D/check-out.txt" 2>/dev/null &
  wait

  # Group C: SIGPIPE + redirect bug. v4.6.16 inverted SIGPIPE semantics — _cleanup_pipe sets _RY_OUTPUT_BROKEN and returns 0 instead of `exit 141`. As a result, `--help | head -n 1` should now return PIPESTATUS[0]=0 across the board (regardless of help-text size vs pipe buffer): fish's PIPE handler short-circuits the writer, JSONL log absorbs the SIGPIPE event, and the script exits normally. Test still detects non-{0,141} exits (e.g., crash) and treats 141 as a v4.6.16 regression. _cleanup_pipe presence + non-fatal contract verified statically (checks 32 + 189 + 390).
  timeout "$HELP_TIMEOUT" "${FISH_PREFIX_ARGS[@]}" fish "$S" --help 2>&1 | head -n 1 >/dev/null
  printf -- "--help|head -n 1 → %d (0=buffer-fit, 141=SIGPIPE fired; both acceptable)\n" "${PIPESTATUS[0]}" > "$D/exit-c-sigpipe.txt"
  (
    fish -c '
      set result (string trim -- (cat -- /nonexistent/path 2>/dev/null))
      echo "result: [$result]"
    ' >"$D/redirect-out.txt" 2>"$D/redirect-err.txt"
  ) &
  (
    rg '141' "$D/help-out.txt" 2>/dev/null | head -5 > "$D/help-141.txt"
  ) &
  wait
  cat "$D"/exit-c-sigpipe.txt >> "$D/exit-codes.txt" 2>/dev/null

  # Group D: NO_COLOR enforcement on read-only modes (write modes need sudo+profile). Two passes: NO_COLOR=1 expect 0 ANSI; NO_COLOR= (empty) expect >0 ANSI per no-color.org v1.0.
  for mode in "--verify-static" "--verify-runtime" "--check" "--help"; do
    safename=$(echo "$mode" | tr ' ' '-')
    case "$mode" in --help) T="$HELP_TIMEOUT" ;; *) T="$RUNTIME_TIMEOUT" ;; esac
    timeout "$T" "${FISH_PREFIX_ARGS[@]}" env NO_COLOR=1 fish "$S" $mode 2>"$D/nocolor-$safename.txt"       >/dev/null &
    timeout "$T" "${FISH_PREFIX_ARGS[@]}" env NO_COLOR=  fish "$S" $mode 2>"$D/nocolor-empty-$safename.txt" >/dev/null &
  done
  wait
  ```

STEP 2: ANALYSIS

  ```bash
  S=/home/claude/audit-src/script.fish
  D=/home/claude/audit-data/runtime
  echo "=== STDERR BYTES (expect 0) ===" && wc -c "$D"/{help,h,ver,v}-err.txt
  echo "=== HELP STDOUT (expect >0) ===" && wc -c "$D"/{help,h}-out.txt
  echo "=== EXIT CODES ===" && bat --plain "$D/exit-codes.txt" 2>/dev/null || cat "$D/exit-codes.txt"
  echo "=== ANSI NO_COLOR=1 (expect 0) ===" && { rg -c '\x1b\[' "$D"/nocolor-[^e]*.txt 2>/dev/null || grep -cP '\x1b\[' "$D"/nocolor-[^e]*.txt 2>/dev/null || echo "none"; }
  echo "=== ANSI NO_COLOR= empty (expect >0 per no-color.org) ===" && { rg -c '\x1b\[' "$D"/nocolor-empty-*.txt 2>/dev/null || grep -cP '\x1b\[' "$D"/nocolor-empty-*.txt 2>/dev/null || echo "none"; }
  echo "=== OP MODE STDOUT BYTES (expect 0 except --check which must be silent) ===" && wc -c "$D"/{vs-out,vr-out,check-out}.txt 2>/dev/null
  echo "=== REDIRECT ===" && bat --plain "$D/redirect-err.txt" 2>/dev/null || cat "$D/redirect-err.txt"
  echo "=== HELP 141 (expect hit in help text) ===" && bat --plain "$D/help-141.txt" 2>/dev/null || cat "$D/help-141.txt"
  echo "=== VERSION CONTENT ===" && { VER_OUT=$(head -n 1 "$D/ver-out.txt" | sed 's/^v//'); VER_GLOBAL=$(rg -o 'VERSION "[^"]+"' "$S" | sed 's/.*VERSION "//; s/"$//' | head -n 1); echo "stdout: $VER_OUT"; echo "global: $VER_GLOBAL"; test "$VER_OUT" = "$VER_GLOBAL" && echo "MATCH" || echo "MISMATCH"; }
  echo "=== FLAG EQUIVALENCE ===" && { diff "$D/help-out.txt" "$D/h-out.txt" >/dev/null 2>&1 && echo "--help/-h: MATCH" || echo "--help/-h: MISMATCH"; diff "$D/ver-out.txt" "$D/v-out.txt" >/dev/null 2>&1 && echo "--version/-v: MATCH" || echo "--version/-v: MISMATCH"; }
  echo "=== DEPRECATED FLAGS (all must → 2) ===" && grep -E '^(--all|-a|--dry-run|-n|--diff|--fix|--force|-f|--lint|--test-all|--completions|--logs) →' "$D/exit-codes.txt"
  echo "=== EXCLUSIVE VIOLATIONS (all must → 2) ===" && grep -E '^--verify-static --|^--verify-runtime --|^--check --' "$D/exit-codes.txt"
  ```

STEP 3: CHECKLIST

  12.1 STDOUT/STDERR SEPARATION
       [ ] 239. --help: stdout content >0, 0 stderr; --help/-h match;
                help text references exit codes 0/1/2/3/4/5/10/129/130/131/141/143
       [ ] 240. --version: stdout version, 0 stderr; stdout content
                matches `set -g VERSION` value; --version/-v match
       [ ] 241. Read-only modes (--verify-static, --verify-runtime): UI to stderr,
                0 stdout bytes on success path; --check is fully silent
                (QUIET=true) — both stdout AND stderr 0 bytes on clean exit

  12.2 EXIT CODE MATRIX
       [ ] 242. 0: --help, -h, --version, -v; --check when clean.
                Non-target expected: --verify-static/-runtime return 1
                (missing configs), --check returns 3 (preflight) or 10 (drift).
                Always examine stderr to distinguish expected non-target
                failures from real bugs.
       [ ] 243. 2 (EXIT_USAGE): --bogus, --install-file without value,
                --install-file relative/path, --install-file -flag,
                any deprecated flag (--all, -a, --dry-run, -n, --diff,
                --fix, --force, -f, --lint, --test-all, --completions, --logs),
                any combination of mode flags (--verify-static --check etc.)
       [ ] 244. argparse --exclusive: verify-static/verify-runtime/check/install-file
                mutually exclusive; combining any two → exit 2 with
                `[ERR] Invalid arguments` message via argparse rejection.
       [ ] 245. 3 (EXIT_PREFLIGHT): --check when sudo unavailable, root
                UUID missing, or content pregeneration fails.
                10 (EXIT_DRIFT): --check when prereqs OK but managed files
                drift from embedded content.

  12.3 DEPRECATED / UNKNOWN FLAG HANDLING
       [ ] 246. argparse spec lists only surviving flags
                (`h/help v/version V/verbose verify-static verify-runtime check install-file=`
                with `--exclusive=verify-static,verify-runtime,check,install-file`).
                Unknown / removed flags (--all, -a, --dry-run, -n, --diff, --fix,
                --force, -f, --lint, --test-all, --completions, --logs, etc.) are
                rejected by argparse with a generic `[ERR] <argparse-msg>` (or
                `[ERR] Invalid arguments: $_ORIG_ARGV` if argparse stderr is empty),
                followed by `_ry_show_help` to stderr, then `_pre_dispatch_exit
                $EXIT_USAGE` (exit 2). No per-flag specific message; no flag
                silently succeeds; none fall through to install.

  12.4 NO_COLOR
       [ ] 247. Two-axis NO_COLOR conformance per no-color.org v1.0 (2022):
                (a) NO_COLOR=1 → 0 ANSI escapes across ALL modes
                (--verify-static, --verify-runtime, --check, --help);
                (b) NO_COLOR= (empty string) → ANSI count > 0 (empty value
                must NOT suppress color).

  12.5 REDIRECT BUG REGRESSION
       [ ] 248. `string trim -- (cat -- /nonexistent 2>/dev/null)` produces
                no stderr leak. Guards against Fish redirect-leak bug
                (check 143 runtime test).

  12.6 SIGPIPE + SIGNAL EXITS
       [ ] 249. Help | head -n 1 returns 0 (v4.6.16 inversion — was: {0, 141}).
                _cleanup_pipe sets _RY_OUTPUT_BROKEN=true and returns 0;
                fish does not exit 141. PIPESTATUS[0]=141 is now a regression
                signal (v4.6.16 contract violation), not an acceptable outcome.
                Help text documents 129/130/131/143 (HUP/INT/QUIT/TERM) +
                134/138/140 (ABRT/USR1/USR2). 141 SIGPIPE row dropped from
                help/README signals tables (v4.6.17). _cleanup_pipe non-fatal
                contract verified statically by checks 32 + 189 + 390.


═══════════════════════════════════════════════════════════════
 PASS 3: FINALIZE
═══════════════════════════════════════════════════════════════

STEP 1: COLLATE

  ```bash
  D=/home/claude/audit-data; O=/home/claude/audit-findings.txt
  test -s "$O" || echo "WARNING: No findings" > "$O"
  awk -F'[][]' '/^F-/ {s[$2]++; t++} END {for (k in s) printf "%s: %d\n", k, s[k]; printf "TOTAL: %d\n", t}' "$O"
  ```

STEP 2: SUMMARY TABLE

  Phase  | Pass | Checks | Pass | Fail | Info
  -------|------|--------|------|------|-----
    1      | 1    | 12     |      |      |
    2      | 1    | 11     |      |      |
    3      | 1    | 15     |      |      |
    4      | 1    | 12     |      |      |
    5      | 1    | 20     |      |      |
    6      | 1    | 30     |      |      |
    7      | 1    | 14     |      |      |
    8      | 1    | 19     |      |      |
    9      | 1    | 9      |      |      |
   10     | 1    | 11     |      |      |
   11     | 1    | 11     |      |      |
   12     | 2    | 11     |      |      |
   13     | 1    | 5      |      |      |
   14     | 1    | 232    |      |      |
   15     | 1    | 19     |      |      |
  TOTAL  |      | 431    |      |      |

  CRITICAL: N | HIGH: N | MED: N | LOW: N | INFO: N | BUG: N | STYLE: N

  SPECIAL HANDLING:
    Always PASS    137 (forbidden-pattern doc; never fires)
    Runtime only   143 (redirect regression) + PASS 2 checks (11)
    Expect 0 hits  83 (no modules-load.d), 171 (KVER init), 176 177 (functional;
                   comments exempt), 193 (dead globals), 257 259 260 263 268 273
                   283 289-292, 296 (deprecated test -o/-a),
                   307 (irqbalance RETIRED from MASK v4.0.0 — see 348),
                   325 (preempt=full retired from KERNEL_PARAMS v4.0.x; dmesg-only),
                   328 (somaxconn RETIRED from SYSCTL_VALUES v4.0.0 — see 344),
                   334 (--lint removed v3.48.20),
                   335 (_get_boot_time/_progress_skip/_ry_count_managed_cases inlined/merged/replaced),
                   348 (irqbalance off MASK v4.0.0),
                   357 (--test-all removed v4.1.0), 358 (--completions removed v4.1.0),
                   367 (no `finished` field), 368 (sha256 prefix retired v4.1.4),
                   370 (_phase_rcs/_rc_<phase> removed v4.1.4),
                   371 (hash_rcs + _any_hash_worker_timeout removed v4.1.4; 124/137/143 sentinels phantom),
                   377 (command sed 's/^ *//' → string trim --left),
                   380 (bare `>>"$LOG_FILE"` regression — all 6 JSONL writers signal-safe v4.1.6;
                        primary expects 6 paired hits, sub-check 0).
    Expect 0 hits — v4.1.9 → v4.5.8 retirements (regression detectors only):
                   PROGRESS_STEPS / PROGRESS_TOTAL (v4.1.9, checks 218 + 280),
                   _load_profile / _validate_profile / _ry_profile_* / default-profile
                   / _manifest_check_orphans / _manifest_write (v4.5.0, checks 232 + 233),
                   _check_service_active_enabled (v3.1.0+, check 175),
                   _find_pacnew_files / _get_boot_time (later versions, check 173),
                   _parse_systemctl_show / _gkeyfile_escape / _emit_step_time
                   / _content_hash / _RY_MANAGED_CASE_COUNT (later versions),
                   379 (legacy 'Profile destination key collision' label
                        retired with profile-system removal v4.5.0).
    Expect 0 hits — v4.5.25 → v5.0.1 retirements (regression detectors only):
                   nftables / nftables.service (v4.6.4, checks 345 + 346),
                   libva-mesa-driver / lib32-libva-mesa-driver (v4.6.5, check 345),
                   mkinitcpio-firmware-in-PKGS_ADD (v4.6.17 — relocated to AUR_PKGS, check 391),
                   ufw-in-PKGS_DEL (v4.6.7 — relocated to MASK, check 347),
                   141-as-exit-code anywhere (v4.6.16/.17 SIGPIPE retirement, checks 27/32/189/249),
                   case pipe arm in _teardown (v4.6.17, check 32 regression),
                   141 SIGPIPE row in help/README (v4.6.17, check 113 regression),
                   PROTON_NO_WM_DECORATION (v4.5.37 ENV_VARS drop, check 349),
                   ENABLE_LAYER_MESA_ANTI_LAG (v4.5.38 ENV_VARS drop, check 349),
                   busy_read / busy_poll / netdev_budget / kernel.split_lock_mitigate
                   / vm.swappiness in SYSCTL_VALUES (v4.6.x sysctl trim, checks 100 + 344),
                   _PROFILE_USES_NM (v4.5.33 renamed to _PROFILE_USES_WIFI_BACKEND),
                   `mv --` inside atomic-write helpers (v4.6.9 — replaced by mv -T / mv -Tf, check 118),
                   `kill -0` in _reclaim_stale_lock (v4.6.9 — replaced by test -d /proc, check 393),
                   command sed 's/^ *//' in _run (v4.1.4 — replaced earlier by string trim --left,
                        which itself was refactored away in v4.5.28 _run split).
    Expect 0 hits — v5.0.1 → v6.0.0 reduction retirements (regression detectors only):
                   _RY_INSTALL_SOURCED / _ry_namespace_cleanup (v6.0 source-mode
                        subsystem partially retired, check 395 — note: bail-state
                        sentinels `_RY_INSTALL_BAILING` and `_RY_INSTALL_LAST_EXIT`
                        SURVIVE inside `_ry_exit` re-entry guard and
                        `_cleanup_on_exit` final-exit-code source; expect ≥2 and ≥3
                        hits respectively, NOT 0),
                   SUDO_KEEPALIVE / _start_sudo_keepalive / _kill_sudo_keepalive /
                        SUDO_KEEPALIVE_ERR / keepalive (v6.0 entire subsystem retired, check 396),
                   "event":"progress" / MAX_LOGS / log rotation patterns
                        (v6.0 retired, checks 397 + 398),
                   _detect_lvm / pvs probes (v6.0 retired, check 399),
                   BOOT_WIPE_AUTO_ACK / BOOT_WIPE_MARKER / _bwg_managed_only /
                        RY_INSTALL_CONFIRM_BOOT_WIPE / boot-wipe-acknowledged
                        (v6.0 ack subsystem retired, check 400),
                   _reclaim_stale_lock as separate function (v6.0 inlined, check 401),
                   test -d /proc in _acquire_lock (v6.0 reverted to kill -0, check 402),
                   literal `bootctl -x` string (v6.x retired — replaced by
                        `_bootctl_dir -x` helper call; check 403 — note:
                        `_resolve_boot_path`, `_RY_BOOT_PATH` cache, and `_resolve_esp`
                        all SURVIVE for XBOOTLDR + BLS Type #1 resolution; expect
                        ≥1 hit on `_resolve_boot_path` function definition + ≥3
                        cache references, NOT 0),
                   _ntsync_per_kernel_state / _ntsync_check_installed_kernels
                        (v6.0 retired, check 404),
                   RY_INSTALL_CONFIRM_SYSTEM_UPGRADE (v6.0 SYU gate retired),
                   flock(1) all uses (v6.0 dropped — no log rotation, no lock reclaim flock).
    Expect 0 hits — v6.0.0 → v7.3.3 retirements (regression detectors only):
                   wifi.iwd.autoconnect=false (v7.0 NM 1.56.0 compat, check 434),
                   RADV_PERFTEST=transfer_queue (v7.0 renamed to RADV_EXPERIMENTAL=transfer_queue, check 433),
                   /etc/drirc / _content__etc_drirc / _grep_xml_tag / _post_drirc
                        (v7.2.0 retired, check 103 regression),
                   cpupower-epp.service / _vrsv_chk_cpupower /
                        _content__etc_systemd_system_cpupower-epp.service
                        (v7.2.1 retired, check 437),
                   _RY_AUR_PARTIAL fallback when count $AUR_PKGS ≤ 1
                        (v7.0.17 — sentinel set only when 0 < failed < count).
    Positive-presence (v7.3.3 expansion adds, regression if 0 hits — reverse of above):
                   _atomic_write_file post-write _awf_symlink_check (check 411),
                   _fstab_atomic_replace findmnt --verify (check 412),
                   _ip_pacman_invoke partial-upgrade gate (check 413),
                   _csp_filter_rdeps cascade gate (check 414),
                   _run 5-helper split (check 408 + 415),
                   _has_user_bus_active XDG_RUNTIME_DIR/bus probe (check 409),
                   _aur_verify_mt7925 modinfo cross-check (check 424),
                   _vmh_order_checks 8-pair rule set (check 425),
                   _vre_fstab `(^|,)tok(,|$)` regex (check 426),
                   _mr_copy_size_verify cmp -s (check 427),
                   _dc_erase_globals _RY_BOOT_TAINTED (check 428),
                   _RY_POST_HOOKS 14-row table (check 429),
                   _RY_BOOT_CRITICAL_DSTS 4-element array (check 430),
                   EXPECTED_VULKAN_PKGS:3 + _RY_PKG_MANAGED_SERVICES:1 invariants (checks 431 + 432),
                   avahi-daemon.{service,socket} in MASK (check 435),
                   realtime-privileges + cpupower in PKGS_ADD (check 436),
                   cpupower-service.conf + cpupower.service + _post_cpupower (check 437),
                   99-cachyos-thp.conf + _post_tmpfiles + */tmpfiles.d/*|tmpfiles glob (check 438),
                   _ry_apply_wireless_regdom + _ry_check_wireless_regdom +
                        RY_INSTALL_WIRELESS_REGDOM env (check 439). 
    Examine stderr --verify-static/runtime exit 1 on non-target: read actual stderr before
                   categorizing. --check exit 3 (preflight) or 10 (drift) expected.

  Verdict: deploy-ready | fix-required | redesign-needed

  Partial: save findings, mark SKIPPED phases, add COVERAGE line,
  still produce spec, verdict "incomplete — N/15 phases completed".

STEP 3: SPEC FILE

  SEVERITY: CRITICAL (data loss/security) | HIGH (functional bug) |
  MED (correctness) | LOW (style) | INFO (observation) |
  BUG (logic/regression) | STYLE (formatting/convention)

  Per finding (rendered as multi-line block, fields below):
    F-N | Severity | Phase | Checks | Lines | Category | Detail | Fix

  ```
  FIXES SPEC: <script> <old-ver> → <new-ver>
  Date: YYYY-MM-DD | Findings: N (N CRIT, N HIGH, N MED, N LOW, N INFO)

  --- FIX INSTRUCTIONS ---
  F-1  [SEV]  <summary>
       Checks: N,N  Lines: N,N  Category: <checklist item>
       Before: <exact text>
       After:  <exact replacement>
       Verify: <command>

  --- INFO (no action) ---
  I-1  [INFO]  <desc>  Checks: N  Lines: N

  --- VERSION ---
  Bump: <old> → <new>
  Changelog: v<new> <summary>.

  --- VERIFICATION ---
  1. fish --no-execute script.fish
  2. rg '<old-version>' script.fish README.md  (expect 0)
  3. rg '<new-version>' script.fish README.md  (expect N)
  ```

  Before/After must be exact + unique (str_replace requirement). If not unique,
  expand context. Spec self-contained. Version bump iff actionable fixes. INFO separate.

  UNIQUENESS VERIFICATION (run during + after spec generation; bash-only —
  uses local, [ ], ${var#prefix}; depends on PASS 1 STEP 2 shims):
  ```bash
  verify_unique() {
    local pat="$1"
    local c=$(rg -cF -- "$pat" /home/claude/audit-src/script.fish 2>/dev/null || echo 0)
    if [ "$c" != "1" ]; then
      echo "NON-UNIQUE ($c hits): $pat -- expand context"
      return 1
    fi
  }
  ```
  If non-unique (0 or 2+ hits), expand Before/After with surrounding
  lines until unique. Post-write full check:
  ```bash
  rg -o 'Before: .*' fixes-spec-*.txt | while IFS= read -r line; do
    pat="${line#*Before: }"
    c=$(rg -cF -- "$pat" /home/claude/audit-src/script.fish 2>/dev/null || echo 0)
    [ "$c" != "1" ] && echo "NON-UNIQUE ($c hits): $pat"
  done
  ```

STEP 4: PACKAGE

  ```bash
  : ${S:?set script name (e.g. ry-install)} ${V:?set version (e.g. 3.46.0)}
  OUT=/mnt/user-data/outputs
  Z=""
  for f in /home/claude/audit-${S}-${V}.txt /home/claude/fixes-spec-${V}.txt; do
    if [ -f "$f" ]; then cp "$f" "$OUT/"; Z="$Z $(basename "$f")"; else echo "SKIP: $f not found"; fi
  done
  cd /home/claude && [ -n "$Z" ] && zip "${OUT}/audit-${S}-${V}.zip" $Z
  ```

DELIVERABLES: audit-<script>-<version>.txt + fixes-spec-<version>.txt (zipped)
